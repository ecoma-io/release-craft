# Phase 2 contract — the deterministic release planner

> **Status: Phase 2 design asset (task P2-01 of
> [#16](https://github.com/ecoma-io/release-craft/issues/16), part of
> [#13](https://github.com/ecoma-io/release-craft/issues/13)).** This document
> is the contract the Phase 2 implementation and its suite are reviewed
> against, the same role [phase1-contracts.md](phase1-contracts.md) played for
> the kernel. It implements the planning half of
> [ADR-0002](../adr/0002-release-model-and-domain-vocabulary.md)'s Model C′
> exactly: a pure, deterministic, side-effect-free function
>
> ```text
> (repository observations, parsed changes, release history,
>  line configuration, component metadata)   →  ReleasePlan | DecisionRecord
> ```
>
> living in the package layer `src/` (decision-log D5). Nothing here may add a
> term outside the locked vocabulary, a provider concept, or an execution
> responsibility; each section cites the invariants and scenarios it serves.

## 1. Scope boundaries

The planner decides; it never performs. It commits nothing, tags nothing,
publishes nothing, calls no provider, mutates no state outside its return
value, executes no hook, and never infers completion from side effects
(invariant 3; E-01..E-09 belong to the execution phase). Negative outcomes are
returned as `DecisionRecord`s, never thrown as exceptions (invariant 4).
Construction-level invalidity (a kernel value's `*Error`) and planning-level
refusal (a record) are different failure classes and are separated at the
planning boundary.

## 2. The sixteen contract items

### 2.1 Planner input boundary

`PlanningInput` is the single argument. It is provider-neutral, serializable,
and closed: the planner reads nothing but this value. It carries, as explicit
data — never as environment, clock, filesystem, or network reads (invariant 2;
E-05):

- `policy`: the policy digest plus the named policy decisions this contract
  fixes (bump mapping id, ladder, lifecycle defaults, pre-1.0 dampening,
  self-reference marker namespace, tag-format configuration);
- `repository`: normalized commit and ref observations (§2.2);
- `history`: normalized tag observations (§2.13);
- `lines`: release-line configurations, declared or default-derived — the
  same schema either way (defaults as data; [release-model.md §5
  "The mechanism"](release-model.md#complexity-budget); no code path keyed on
  "simple mode");
- `components`: package metadata (names, manifest versions as projections,
  declared dependency ranges) for the package axis (PL-01..PL-03);
- `bootstrap`: the recorded bootstrap decision when present (S-02);
- operator intents that must be recorded when exercised ("release anyway" →
  an operator-forced record, never a routine release, S-01; `Release-As`
  footer semantics, compatibility boundary row 2);
- the declared channel registry when present (ADR-0012 decision 2, D36):
  channel observations — each channel's non-empty unique id and the line +
  version it currently points at — read by the planner when it names a
  promote's planned channel moves; absence is the pre-ADR-0012 posture and
  refuses nothing.

Repeated execution against an identical `PlanningInput` is deterministic and
produces an identical plan fingerprint (invariant 2; proven by running the
golden matrix twice).

### 2.1.a The manifest configuration surface

`PlanningInput` has one committed authoring surface: a closed configuration
document (JSON first) parsed into the planner's own vocabulary by
`parseManifest` (`src/planner/config.ts`), a thin boundary layer — config is
input, never a second semantic model. The document declares the declared half
of the input boundary: `policy`, `lines`, and — optionally — `components`,
`channels`, and `bootstrap`; the parsed value composes with the observed half
(§2.2, §2.13) into the single `PlanningInput` argument. Every parsed value is
the planner's own vocabulary key for key (`policy.digest`, `lines[].feedRef`,
`channels[].target.line`), so unknown keys are a pure closed-set check and are
refused loudly at every level — document, policy, line, component entry,
channel, target, bootstrap, withhold rule, stream policy, version band — never
silently dropped (the defect class #191 records for action inputs).

The door's refusals name every violation in one round-trip
(`InvalidManifestError`, the input seam's violation shape under a distinct
name): missing mandatory sections (`policy`, `lines`), malformed values (a
non-string `publishes` binding is refused, mirroring the `feedRef` check — it
never degrades into the default publish posture), and dangling references — a
`tagFormats` key naming an undeclared line, a `publishes` binding or
dependency edge naming an undeclared component, a channel target naming an
undeclared line. Components use the packages-map
pattern: an optional `defaults` block plus per-name entries, each field
resolved entry-first-then-defaults (an entry's `dependencies: []` is declared
absence, clearing inherited edges); `defaults` is the reserved inheritance
key, so a component by that name is not expressible in the map grammar.

Manifest versions ride as projections only (invariant 6): the document never
becomes computation truth — birth targets come from the recorded bootstrap,
bumps from history and policy. The determinism pin (§2.14) holds with
config-sourced inputs: two fresh parses of one document plan to deep-equal
outcomes.

**Migration note (release-please, REPLACE per baseline §17).** Release-craft
adopts the concepts, not the two-file format: `release-please-config.json` +
`.release-please-manifest.json` map onto one closed document — the packages
map becomes the `components` map (defaults + entries), the primary branch
becomes a declared line, the tag separator/template becomes
`policy.tagFormats` entries, and prerelease posture becomes the policy's
ladder/seed/dampening knobs. Changelog coordinates and extra-files
declarations have no manifest keys: their release-craft home is the
artifact-declaration surface of the execution layer, not the planner's input
boundary, and this contract does not claim them shipped.

### 2.2 Normalized repository observations

A `CommitObservation` is `{ sha, parents, message, committedAt, containingRefs }`;
a `RefObservation` is `{ name, head }`. Timestamps are input data used never
for ordering (E-10: ordering is topological/ancestral, never wall-clock). The
planner performs zero `git` calls; observations arrive precomputed. The
evaluated **range** of a line is that line's own last admissible release tag
to its feed ref head (invariant 6) — never a global cursor.

### 2.3 Change identity and lineage (fork 8 resolved conservatively)

Extraction maps commit observations to candidate `Change` values
(kernel `Change.of(id, lineage)`; identity is the id, never content —
invariant 9):

- id source, first match wins: the commit's `Change-Id:` footer; else the
  cherry-pick trailer's origin commit (recorded provenance); else the commit's
  own sha (also recorded provenance). No fuzzy matching, no content hashes.
- lineage records the chain (parent id, origin commit, origin line) so
  cherry-picks across lines resolve to one identity (M-03, M-04, M-06, M-09)
  and a conflicting backport — same id, divergent payload descriptors — stays
  one identity whose conflict is surfaced in the plan's explanation data
  (M-05), never silently merged away.
- extraction is deterministic: identical observations produce identical
  changes, in stable order.

### 2.4 Branch/ref attribution (M-01 invariant home)

Attribution of a change to a line is ancestry-based: the change's commit is
reachable from the line's feed ref within the line's range (§2.2). Ancestry is
the default evidence; when attribution is ambiguous (a commit inside two
lines' ranges with no lineage tiebreaker and no policy that decides), the
planner fails closed with a `refusal` record naming the ambiguous commits
(PL-05a; invariant 4). A change may legitimately be pending on several lines —
releasedness is per (line, change) (M-04, M-06); a change released on one line
is never thereby released on another. There is no "latest branch" shortcut and
no branch name is ever a line identity (invariant 7). This section is the
invariant home M-01 lacked (D9 obligation; recorded in the decision log).

### 2.5 Range calculation

Per line, from that line's own release history: the latest admissible tag on
the line (or the line's birth when none) bounds the range's lower end;
divergent maintenance lines compute independent ranges (M-07, S-03). Manifest
versions never bound a range (S-03).

### 2.6 Release-line selection

Lines exist because configuration declares them or the default derivation
creates the single implicit line fed by the default branch — one schema, one
planner path. Line selection for a change is attribution (§2.4); line
selection for a release is the line's own pending change set plus its policy.
Line identity is the stable configured id (invariant 7).

### 2.7 Bump resolution

The commit-type → `Bump` mapping is policy data (never in `Change` or the
kernel). Default policy: `feat` → minor, `fix`/`perf`/`refactor` → patch,
breaking (the header `!` or a breaking footer in the trailer block —
`BREAKING CHANGE:` or the `BREAKING-CHANGE:` alias, the value inline or
wrapped onto an indented continuation line, leaving the token line bare;
any type) → major; the footer is looked for in the trailer block only —
the message's last paragraph, the geometry §2.12 fixes for markers — never
in the subject or an earlier body paragraph.
`chore`/`docs`/`ci`/`test` without a breaking marker are not
release-triggering (S-01, PL-06 — recorded in the no-op cause detail, never
silently dropped). The breaking marker dominates filtering (PL-05). The
line's change-set bump is `Bump.max` over member changes; `ChangeSet.of`
records it. An empty group mints nothing (§2.9). Pre-1.0 dampening (breaking
bumps minor before `1.0.0`) is line-policy default (compatibility row 3).

### 2.8 Prerelease policy resolution (D9 pointer convention decided)

Streams are keyed by (line, target, identifier); sequence arithmetic is the
kernel's `advanceStream`/`streamVersion` (Phase 1 values), seeded `.0` when
the key is absent; a target move is a new key (P-05); two streams on one
target coexist (P-06). Ladder order (alpha → beta → rc) and identifier reset
are line-policy data; opaque identifiers are allowed by declaration.

**Released-pointer convention:** the pointer is the line's highest published
version by precedence — the kernel's `released` reading. A prerelease
publication with higher precedence moves it (M-08). The planner's mint check
is precedence-based: a planned mint that sorts above the pointer moves it,
and a mint that sorts below is a caller contract violation — the ladder
override that publishes below the pointer is legal only as explicit declared
policy (the D9 branch), which Phase 2 declares no knob for; the scenario
cases where a publication leaves the pointer standing (P-02/P-07) are
exactly the below-pointer mints Phase 2 refuses to plan. The pointer value a
plan computed from is recorded in the plan.

**First-mint seed (fork 17 resolved).** The first sequence number of a fresh
stream key is declared line policy: `seed: .0` — the kernel default, matching
`advanceStream`'s seeding — or `seed: .1` by explicit declaration. The matrix
stays verbatim: P-01/P-02/P-05/P-07 fixtures take the default; M-08/E-08
fixtures declare the `.1` policy. Which seed a plan used is recorded in the
plan's stream fields.

**Per-line stream policy (D18, ADR-0004).** A declared line may override
the admission posture and seed: `line.streams.allow` — `"all"` (the
default-as-data posture: every declared or ladder identifier is mintable),
`"none"` (the line is stable-only; a `prerelease` intent naming it becomes
a recorded refusal on the plan's `refusedIntents`, never a fallback to
stable — M-08's rejection half, and the rest of the plan is unaffected), or
an explicit identifier list (an opaque identifier is legal exactly by
declaration — fork 4's resolution; the ladder's fixed order stays global
promotion-slice data, fork 3). `line.streams.seed` overrides
`policy.prereleaseSeed` for the line's fresh keys. Which posture a plan
used is visible in its `refusedIntents` and in the streams' recorded seeds.

**Promotion precedence (#90, decision-log D38).** A promotion-shaped
decision (P-03: `release` with `bump: null` over an in-flight prerelease)
never carries a stream extension: the sibling `prerelease` demand(s) for the
promoted line are subsumed — the stable co-mint stands (D38 decides the
suppression does not bind a promotion), no
stream entry is planned, the promotion carries its full channel plan
(§2.11's amendment), and the decision record names the subsumed stream(s).
The suppression's single-intent posture is unchanged for every other
decision: a release decision's content still flows to the stream.

### 2.9 No-op, withheld, refused, blocked (negative outcomes are records)

- empty change set → `no-op` record: cause `no-release-worthy-changes`,
  evaluated range, policy digest, ignored-by-policy commits enumerated
  (S-01, PL-06; D9 obligation — never a minted version, never an exception);
- `withheld`: policy-filtered changes (PL-07) are deferred and enumerated in
  the record — filtering is deferral, never deletion (fork 9's discard
  protocol: the record is the home of the withheld set);
- `refused`: fail-closed attribution ambiguity (§2.4), operator-contradiction
  (S-01's "release 1.0.2 now" against a chore-only runway);
- `blocked`: unmet preconditions — bootstrap required without a recorded
  decision (S-02), stale plan detected under a changed world (PL-08, E-04,
  E-11);
- every record carries cause, evaluated range, and the policy digest that
  produced it (invariant 4; amendment A1);
- kernel construction rejections (`InvalidChangeSetError` and kin) surface
  as records at the planning boundary too — the planner catches them and
  returns the corresponding `refused` record; they are never re-thrown.

**Line lifecycle (D18, ADR-0004).** `lifecycle: "frozen" | "retired"`
refuses release-shaped planning for the line as a `refused` record
(`line-frozen` / `line-retired`): no targets, no streams, no release entry;
the rest of the plan is unaffected. The states are the declared vocabulary;
the refusal is the planning semantics. **Withhold rules (D18/PL-07,
ADR-0004).** A declared `withhold: {scope, reason}` defers the matching
release-triggering changes: the release range pins below the earliest
withheld commit (the un-released span keeps them — deferral is recoverable,
deletion is not), the withheld set is enumerated in the plan's explanation,
and a line left with nothing release-worthy yields the `withheld` decision
(`policy-filter`) instead of a mint.

### 2.10 Plan supersession

A plan may name the prior plan it supersedes (`supersedes: planId | null`);
supersession is a recorded relation, never an edit (invariant 5). A
regeneration that lands on emptiness closes the open plan with a **no-op
successor record** carrying `supersedes` — mixed-kind endpoints are legal
(D9 obligation). Executing-side re-verification is out of scope here; the
planner provides the fingerprint comparison data (stale recognition:
differing fingerprint or a target tag that now exists, invariant 6's global
tag-namespace check, PL-08/E-11).

### 2.11 Plan identity and fingerprint

`ReleasePlan.planId` is the plan's content fingerprint: canonical JSON
(recursively key-sorted) of the plan's semantic fields → SHA-256 →
`plan_sha256:<hex>`. Version equality implies nothing about plan equality
(E-11); identical inputs imply identical fingerprints (invariant 2). The plan
records its policy digest and inputs fingerprint so a stored plan can be
re-judged against a changed world (E-04). The fingerprint input tuple is
closed — policy digest, inputs digest, supersedes, and per line: line id,
target version, target tag, change set (range and member changes with id,
lineage, type, bump), stream states with the seed and pointer base used,
propagation edges, artifact declarations, preconditions, and explanation
data — so two implementations cannot ship different fingerprints for the
same plan.

**Amendment (ADR-0012 decision 2, D36).** The per-line tuple gains the
planned channel transitions when present — a promote over a minted
stable target names its moves, promoted-from edge, and stream close as
plan content — and the input's
policy-relevant projection gains the declared channel registry, so worlds
differing in declared channels carry different input fingerprints (and
therefore different plan identities) even where the planned content
coincides. Every non-promote plan leaves the field absent: absent is
byte-identical to the pre-ADR-0012 plan.

### 2.12 Self-reference exclusion (D9 obligation — named rule)

The planner's own bookkeeping commits are identified by the reserved trailer
namespace `Release-Craft:` and are excluded deterministically **before**
classification (PL-04): they never become fresh input, never extend a range's
change set. The excluded set is surfaced in the plan's/record's explanation
data — excluded is not invisible. A commit carrying the marker namespace in
malformed form fails toward explicit review (surfaced in the explanation
data), never silent exclusion (PL-04's posture).

### 2.13 Tag-history projection

`TagObservation[]` is the sole release-history truth (invariant 6; S-03, S-05,
M-02, M-07). Normalization: tag names are parsed to `Version` through the
kernel's grammar (no `v` prefixes; per-component tag formats are declared
input configuration — fork 11's naming knob). Admissibility: a tag joins a
line's history only when its normalized version falls in that line's
declared version band (`LineConfig.versionBand`, D15 — major, optionally
minor; absent admits every admissible tag); foreign or unattributable tags are
excluded from history **and surfaced** in the plan's explanation data (E-06
conservatism — adoption without attribution is refused at planning time).
Line state (released pointer, stream states) is rebuilt from tags at plan
time; any manifest-declared version is a projection whose drift is surfaced,
never consumed as truth (S-03).

**Known phase-2 limit (recorded by #197).** A tag whose prerelease suffix does
not compose the kernel's minting shape `target-identifier.sequence` — a lone
identifier (`1.5.0-alpha`) or a non-numeric tail — rebuilds no stream key, yet
the version still counts for the line's released pointer — and, through the
pointer, for the stable target the in-flight rules compute; it never enters
`stableBase`, which admits only suffix-less releases. The frozen `LineState`
carries no field that would surface such streamless admissible tags, so the
pointer/decision asymmetry they create is silent at this layer: a consumer
sees the pointer move with no stream state to explain it. This is a recorded
limit, not silent data loss — the tag remains in the line's admissible history
and the asymmetry is deterministic — and surfacing it is a `LineState` shape
revision deliberately deferred out of Phase 2 (`src/planner/state.ts` cites
this section).

### 2.14 Plan determinism

All nondeterministic facts are inputs (§2.1–2.2). The planner contains no
clock, randomness, environment, filesystem, or network access; ordering is
ancestral/topological (E-10); output serialization is canonical (§2.11).
Golden fixtures run the planner twice per scenario and require fingerprint
equality.

### 2.15 Dependency propagation (declared, provable in the negative)

When a release changes a package that has dependents, the plan records the
propagation edges and their topological order as declared content, and states
which packages did not move and why (invariant 14). Dependent bumps on
compatible ranges and breaking-on-non-releasing-type are declared propagation
policy (forks 12 and 10; PL-02, PL-05b). The package axis is active for
declared component metadata (PL-01..PL-03); per-package tag naming is declared
input configuration (fork 11's naming knob).

### 2.16 Provider-neutral output schema

Neither the plan nor any record names provider _state_ — branches, pull
requests, GitHub Releases, dist-tag moves, registry contents, or runners
(invariant 15's output-schema half). Coordinates and channel bindings are
opaque declared configuration labels (AR-03's registry coordinates included).
Refs appear only as the observations that fed the input and the bindings
execution will carry out. The plan and record shapes are defined to be
persistable — content-fingerprint key, `supersedes` edge — while persistence
itself stays adjacent to the planner (§6).

## 3. D9 carried obligations → owning sections

The nine obligation groups carried by decision-log D9, each with its
classification, owning section of this contract, and the amendment that rides
this PR (obligation text quoted verbatim in the decision log):

| #   | Obligation (D9, abbreviated)                                         | Classification        | Owning section    | Cites                            | Amendment riding this PR                                                            |
| --- | -------------------------------------------------------------------- | --------------------- | ----------------- | -------------------------------- | ----------------------------------------------------------------------------------- |
| 1   | Empty groups are decision records, never mints                       | Phase 2               | §2.9              | PL-06, S-01; inv 4               | none (inv-4 rewording already complete)                                             |
| 2   | The prerelease-pointer convention is Phase 2 line policy             | Phase 2               | §2.8              | M-08, P-02, P-07                 | invariant 6's stress list += M-08, P-02, P-07                                       |
| 3   | M-01 needs an invariant home                                         | Phase 2               | §2.4              | M-01; invs 4, 6                  | invariant 4's stress list += M-01                                                   |
| 4   | PL-04's self-reference exclusion needs a named rule                  | Phase 2               | §2.12             | PL-04; fork 8                    | invariant 3's stress annotation extended                                            |
| 5   | Plan supersession may terminate in a decision record                 | Phase 2               | §2.10             | PL-06; inv 5                     | invariant 5 sentence + vocabulary-table ReleasePlan cell admit mixed-kind endpoints |
| 6   | Invariant 10's final clause tightens at the execution contract       | Execution             | §7 (handoff note) | PR-02; inv 10                    | none                                                                                |
| 7   | Kernel values are projections of tag history (loader)                | Phase 2               | §2.13             | invs 6, 8; E-11                  | channel state ruled declared configuration (§6)                                     |
| 8   | Nightly would widen the kernel (fork 15, only if resolved line-side) | Conditional — carried | §6                | fork 15; P-02, P-05              | not adopted; amendment gate named (three surfaces)                                  |
| 9   | The change→released-versions relation has no vocabulary term         | Split                 | §2.13             | M-03, M-04, M-06, M-09; inv 1, 9 | descriptive planning-side data; persistence branch is ADR-0002-amendment-gated      |

## 4. Invariant enforcement map

The audit's map is reproduced below as this contract's enforcement index;
every row names the planner mechanism and the contract items (§2.1–2.16) that
carry it. The audit found four structural gaps inside an otherwise clean
surface; they are resolved here and land with this PR:

- **A1 — invariant 14 had no contract item.** Resolved: item §2.15 added.
- **A2 — invariant 15's output-schema half had no owning item.** Resolved:
  item §2.16 added.
- **A3 — invariant 3's purity sentence had no enforcement item.** Resolved:
  the planner-isolation gate (a suite proving no infrastructure import or
  ambient read reaches `src/planner/**`, modeled on Phase 1's
  provider-isolation test) plus the phase's adversarial review are named
  deliverables of the integration workstream.
- **A4 — two items lacked invariant stress coverage** (branch/ref
  attribution; self-reference exclusion). Resolved by the invariant homes
  fixed in §3, rows 3 and 4 — both release-model.md §4 edits ride this PR.

The audit's conflict register is resolved in §6.

**Scope.** One row per architectural invariant (RM §4, invariants 1–15), mapped to the Phase 2 planner mechanism that proves it executable and the mandatory Phase 2 contract item(s) it lands in. Phase 1's executable surface (P1C) is consumed, never re-claimed. Governing rule, ADR2 D3: _"Each Phase 1 and Phase 2 contract that lands must name the invariants it makes executable; 'later'-phased invariants are design commitments until execution is scheduled."_

---

### The map

| Invariant (stress · first provable, verbatim)                                                                                                                                                                                                                                                                                                                                                       | Phase 2 planner mechanism                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Mandatory Phase 2 contract item(s)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Gaps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1. Trivial-path zero declaration.** Stress: "S-01, S-03, S-04, S-05." First provable: "Phase 2 for the planning half (computed version, plan or decision record, zero declarations); the attempt-and-tag half is execution and proves out when execution is scheduled." (RM §4 I1; ADR2 limitations: "Invariant 1's planning half (zero declarations, plan or decision record) is Phase 2 work.") | **Input model + version planning** via defaults-as-data (RM §5): "the default configuration is a value in the same schema as any declared configuration — one line with a default id fed by the default branch, one default channel, the tag cursor as range policy, the Conventional Commits bump mapping — and the planner consumes it through exactly the same function it applies to a fully declared repository. There is no code path keyed on 'single line,' no boolean for 'simple mode.'" Bootstrap: "S-02 demands an explicit recorded choice of `1.0.0` or `0.1.0` — silently deriving one is not valid" (RM §5); forks 1–2 "Resolved by: default-policy design in Phase 2" (RM §6 q5). Second line free: "A breaking change on `main` creates line 2.x by policy default" (RM §5, S-05).                                                                                                                                                                                                                                                                                                                                                                                                  | **planner input boundary** (default configuration "is an input like any other", RM §5) · **release-line selection** (default line derivation; S-05 line 2.x by policy default) · **bump resolution** (default Conventional Commits mapping, RM §5; RM §7 row 1 "Default change classification in planning") · **no-op/withheld decisions** (chore-only runway "yields a recorded no-op decision, not an error (S-01)", RM §5; the S-02 recorded operator choice) · **range calculation** ("the tag cursor as range policy", RM §5) | Planning half fully landed. Attempt-and-tag half deferred per the invariant's own phase sentence (quoted above); safety machinery "present on this path by construction and invisible on it" (RM §5). No uncovered residue.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **2. Deterministic planning.** Stress: "E-05, E-10." First provable: "Phase 2." (RM §4 I2)                                                                                                                                                                                                                                                                                                          | **Input model + plan fingerprint.** "Given identical planning inputs — time and all policy digests included as input values — the planner returns a plan with an identical content fingerprint on every invocation, and never orders anything by wall-clock time." (RM §4 I2; ADR2 D1: "time and policy digests as input values".) Cross-cutting: "This is testable precisely because planning is deterministic (invariant 2): the default configuration is an input like any other, so the trivial path cannot drift from the general one." (RM §5.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **plan determinism** (primary owner) · **planner input boundary** (inputs "enumerated and frozen — time and policy digests arrive as input values, never as environment reads (E-05)", RM §3) · **plan identity/fingerprint** (identical fingerprint on every invocation)                                                                                                                                                                                                                                                          | None. The wall-clock-ordering ban must be stated as a rule over extraction/attribution ordering (any sort the planner performs), not just output assembly — content of the plan-determinism item, not a gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **3. Planning without side effects.** Stress: "E-05, PL-04 (the planner's own outputs are not foreign input), PL-08 (plans are consumed, never mutated)." First provable: "Phase 2." (RM §4 I3)                                                                                                                                                                                                     | **Input model + integration API.** "The planner mutates nothing outside its return value — no file, ref, pull request, registry, or network — so running it twice over the same inputs leaves the world exactly as it was." (RM §4 I3.) Enforcement named in the risk register: "Planning engine quietly acquires side effects … Purity gates + adversarial planner review in Phase 2H" (DL). Persistence boundary: "The planner itself stays pure; wherever decision records persist (fork 16), persistence lands as a thin package-layer writer around the planner — never inside `core/domain/`." (ADR2 consequences.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **planner input boundary** (world enters only as enumerated frozen inputs, RM §3) · **self-reference exclusion** (PL-04 stress: "the planner's own outputs are not foreign input"; DL D9: "PL-04's self-reference exclusion needs a named rule (Phase 2 contract). The planner's own bookkeeping commits must be deterministically identified and never auto-classified as release input; no invariant sentence carries it yet")                                                                                                   | **Soft gap:** no dedicated contract item owns the purity sentence itself; it distributes over planner input boundary + plan determinism, with enforcement living only in the DL risk register ("Purity gates + adversarial planner review in Phase 2H"). The contract doc should name the gate explicitly.                                                                                                                                                                                                                                                                                                                                                     |
| **4. Negative decisions are records.** Stress: "S-01, S-02, M-08, PL-01, PL-06, PL-07." First provable: "Phase 2." (RM §4 I4)                                                                                                                                                                                                                                                                       | **Decision records.** "Every no-op, refusal, block, and withholding is returned as a decision record carrying its cause, evaluated range, and the policy version that produced it — never silence, an exception, or a silently dropped change; a refusal crosses the planning boundary as the record's own `blocked`/`refused` outcome (PL-05a), and PL-01's 'unmapped commits are an error' names that record, not a thrown exception." (RM §4 I4; ADR2 D1: "first-class negative outcomes … with cause, evaluated range, and policy version".) DL D9 adds: "Construction-time rejections (`InvalidChangeSetError` and kin) surface as decision records at the planning boundary too — invariant 4's 'never an exception' clause"; "The planner converts `ChangeSet.empty()` into a `no-op`/withheld decision record (PL-06, S-01 — the latent version is minted nowhere) and never derives a version from the empty group's neutral `\"patch\"`."                                                                                                                                                                                                                                                   | **no-op/withheld decisions** (primary owner) · **planner input boundary** (the codomain `ReleasePlan \| DecisionRecord`, RM §3 signature) · **range calculation** (the record's "evaluated range" field) · **bump resolution** (PL-01 unmapped-commits → record, not exception)                                                                                                                                                                                                                                                    | None — the strongest-covered invariant. M-01's fail-closed refusal (DL D9: "Line attribution by ancestry with fail-closed refusal is the planner's first job and no invariant's stress list names it") must enter as a decision record here or fix I4/I6's coverage — see Gaps A4.                                                                                                                                                                                                                                                                                                                                                                             |
| **5. Plan identity is its fingerprint.** Stress: "PL-08, P-05, E-04, E-11." First provable: "Phase 2 (execution's re-verification of the fingerprint is invariant 13's half)." (RM §4 I5)                                                                                                                                                                                                           | **Plan fingerprint + supersession.** "Two plans with the same target version but different change sets, base bindings, or policy digests are distinct objects, and one supersedes the other through a recorded relation rather than being edited in place." (RM §4 I5.) ADR2 consequences Phase 2: "plan fingerprinting and supersession". Fork 14 pre-resolved: "supersession is one recorded relation, whatever invalidated the predecessor" (RM §6 q5). DL D9: "Plan supersession may terminate in a decision record (Phase 2). A regeneration that lands on PL-06 emptiness closes an open plan with a no-op successor — invariant 5's recorded relation must admit mixed-kind endpoints."                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **plan identity/fingerprint** (primary owner) · **plan supersession** (the "recorded relation"; one rule per fork 14; mixed-kind endpoints per DL D9)                                                                                                                                                                                                                                                                                                                                                                              | None in landing; one contract obligation: pin the fingerprint's exact input enumeration (target version, change sets, base bindings, policy digests per I5's own sentence; E-04/E-11 stress) — see Open questions.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **6. Line-scoped version truth.** Stress: "S-03, S-05, M-02, M-07, M-09." First provable: "Phase 2." (RM §4 I6)                                                                                                                                                                                                                                                                                     | **History loader + version planning.** "The next version on a line is derived from that line's own tag history — the manifest is a projection a plan may repair, never a source of truth — and no global _latest-version_ pointer participates in any computation. The global _tag namespace_ is a different structure and does participate: a plan verifies its target tag exists nowhere before execution (M-11), and regeneration detects the published version to re-target (PL-08)." (RM §4 I6.) Loader rule (DL D9): "Kernel values are projections of tag history (Phase 2 loader). The loader rebuilds line/channel values from tags at plan time; stored values are caches, never a second source of truth (invariant 6 × invariant 8 × E-11's execute-time re-read)." Guarantee form: "Next-version truth is the line's own tag history; the manifest is a projection (S-03, S-05, M-07)" (RM §3). RM §7 row 8: "line tag history outranks any manifest projection (S-03)".                                                                                                                                                                                                                 | **tag-history projection** (primary owner) · **normalized repository observations** (tags and manifests as observations) · **range calculation** (range derived from line tag history; tag cursor default) · **release-line selection** (truth keyed per line)                                                                                                                                                                                                                                                                     | None. The deliberate two-structure rule (line tag history vs global tag namespace) must appear in the contract item's definition, or the M-11/PL-08 clauses land ownerless.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **7. Line and channel identity is not a ref name.** Stress: "M-10, S-03, S-05, PR-04." First provable: "Phase 1 — identity equality across a feed rename is pure value behavior." (RM §4 I7)                                                                                                                                                                                                        | **Attribution (+ input model's line configuration).** Already executable — P1C ReleaseLine: "**No branch anywhere.** The value has no feed field; feed mappings are planning-side data (A5)." Phase 2 does not re-prove it; it operationalizes it: ref names enter only as observation data attributed to stable line ids; plans carry line ids only. Vocabulary: ReleaseLine is "fed by branches through recorded feed mappings" (RM §3).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **branch/ref attribution** (feed mappings as planning-side data; M-01 ancestry attribution with fail-closed refusal, DL D9) · **release-line selection** (selection by stable id, never by ref name)                                                                                                                                                                                                                                                                                                                               | Coverage asymmetry (DL D9): "M-01 needs an invariant home (Phase 2 contract). Line attribution by ancestry with fail-closed refusal is the planner's first job and no invariant's stress list names it; the Phase 2 contract either adds it to invariant 4's stress list or extends invariant 6's sentence." The item has an owner but its invariant anchor does not — see Gaps A4.                                                                                                                                                                                                                                                                            |
| **8. Prereleases are streams, not flags.** Stress: "P-01, P-02, P-04, P-06, P-07." First provable: "Phase 1 — stream-key and ordering value semantics over the kernel's `compare`. The stream state is kernel data carried by the `ReleaseLine` value; 'prerelease stream' as a planning concept names the planner's view over that state, not a second home." (RM §4 I8)                           | **Version planning (prerelease policy resolution) + history loader.** Phase 1 proved the value semantics (P1C ReleaseLine: "Sequences are bounded by construction — seeded at 0, advanced by one, never set from outside"); Phase 2 supplies the policy over them. Forks routed to Phase 2: "Stream restart on target move; fixed ladder or open identifiers (forks 3, 4). Resolved by: the line-policy schema in Phase 2 (P-05, P-02, M-08)" (RM §6 q6). Seed split (P1C): "The `.0` seed is the matrix's own convention … fork 17 records the `.1` bodies — E-08, P-01 — as planner-side next-from-tags computations, not recordings." Pointer convention (DL D9): "The prerelease-pointer convention is Phase 2 line policy. M-08 moves the released pointer to a prerelease; P-02/P-07 publish streams with the pointer silent. The kernel records either; the planner picks one and records it, and must resolve P-02's ladder-override branch, which publishes a version that sorts below the pointer and therefore cannot move it." RM §7 row 14 (break): "Streams compute the next prerelease per (line, target, stream) — the asymmetry is corrected by construction (invariant 8 …; P-04)." | **prerelease policy resolution** (primary owner: ladders, stream advancement, restart-on-target-move, pointer convention) · **release-line selection** ("per-line policy, including prerelease forbidden on one line while required on another (M-08, P-07)", RM §5) · **tag-history projection** (planner-side next-from-tags sequence computation, fork 17) · **bump resolution** (P-04: mid-RC `feat` bumps the sequence — classification feeding stream advancement)                                                           | None. Four distinct policy decisions converge on this one item (forks 3, 4, 17, pointer convention) — the contract item must enumerate them or each becomes an unowned sub-decision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **9. Change identity survives transport.** Stress: "M-03, M-04, M-05, M-06, M-09, PL-04." First provable: "Phase 1 — identity matching over recorded provenance is pure value behavior (the marker convention is adapter-side, open fork 8)." (RM §4 I9)                                                                                                                                            | **Extraction + change identity handling.** Phase 1 proved the value semantics (P1C Change: "Identity is never derived from content, shas, or hashes"; ChangeSet: "Same id with different lineage is still one identity: the conflict is a planning fact (to be surfaced there), not two entries"). Phase 2 consumes: extraction parses markers into ids; the planner computes releasedness — "releasedness is per (line, change)" (RM §3, Change row) — and surfaces the M-05 conflicting-lineage case as a planning fact. "under the invariant that no fuzzy matching is ever admitted (M-05)" (RM §6 q4). Fork 8 routing: "The cherry-pick trailer, a footer change-id, or both. Resolved by: the Change value's provenance parsing in Phase 1's adapter work" (RM §6 q4); P1C out-of-scope: "The change-id marker convention (fork 8) and decision-record storage (fork 16) — adapters/Phase 2 decisions respectively."                                                                                                                                                                                                                                                                            | **change identity and lineage handling** (primary owner; includes the M-05 consume-lineage decision, "planning-side" per DL D9) · **normalized repository observations** (parsed changes arrive as frozen inputs)                                                                                                                                                                                                                                                                                                                  | Fork 8 ownership is stated two ways: RM §6 q4 says "Phase 1's adapter work"; P1C marks it out of Phase 1's scope. Phase 1 shipped no extraction adapter, so marker parsing necessarily lands with (or before) Phase 2 extraction — see Open questions.                                                                                                                                                                                                                                                                                                                                                                                                         |
| **10. Promotion never fabricates content.** Stress: "P-03, PR-01, PR-02, PR-03." First provable: "later — the digest records it compares are Phase 1 values, the promotion machinery is execution." (RM §4 I10)                                                                                                                                                                                     | **Deferred to execution** — quoted reason above (RM §4 I10); reinforced by ADR2 consequences: "Execution arrives as a separate design that consumes plans and must carry invariants 10–13" and ADR2 limitations ("promotion-without-fabrication" among the five unexecutable invariants).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | — (none mandatory)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No gap: deferral explicit and quoted. Phase 2 residue only where a promotion-adjacent flow re-enters planning: fork 6 resolution — "a first-class re-release, if ever wanted, is a new release through the same planner" (RM §6 q5); ChangeSet "may be empty and inherited (PL-06, P-03)" (RM §3). Both flow through change identity handling and no-op/withheld decisions, already owned.                                                                                                                                                                                                                                                                     |
| **11. Claim before mutation.** Stress: "E-07, E-08, M-11." First provable: "later." (RM §4 I11)                                                                                                                                                                                                                                                                                                     | **Deferred to execution** — "First provable: later" (RM §4 I11); "claim" is a locked execution concept (RM §3: "release attempt, execution ledger, claim, artifact generation, and evidence are execution concepts"). Fork 13: "Resolved by: the execution phase's claim design, replayed against E-07/E-08" (RM §6 q1).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | — (none mandatory)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No gap. Phase 2 supplies the claim's _scope values_, not the claim: the plan carries "preconditions" (RM §3 ReleasePlan row) including "a plan verifies its target tag exists nowhere before execution (M-11)" (RM §4 I6 → **tag-history projection**), and the prerelease claim scope "(line, target, stream)" is exactly the key **prerelease policy resolution** resolves. Residues inside already-owned items, not new landings.                                                                                                                                                                                                                           |
| **12. Attempts are the idempotency key.** Stress: "E-02, E-03, AR-01." First provable: "later." (RM §4 I12)                                                                                                                                                                                                                                                                                         | **Deferred to execution** — "First provable: later" (RM §4 I12); "What execution is. A separate, stateful domain that _consumes_ plans and never recomputes them" (RM §3). RM §7 row 17: idempotent re-run "Strengthened: idempotency keyed by (attempt, step) with verified done-ness, not ambient inference (invariants 12, 13 …)".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | — (none mandatory)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No gap: clean deferral; the planner's only relation is that attempts consume plans (RM §3 ReleasePlan row: "Produced by the planner; consumed by attempts").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **13. Partial failure is ledger-recoverable, and attribution gates adoption.** Stress: "E-01, E-02, E-06, E-09, AR-05, AR-06." First provable: "later." (RM §4 I13)                                                                                                                                                                                                                                 | **Deferred to execution** — "First provable: later" (RM §4 I13); ADR2 limitations: "partial-failure recovery" among the five invariants "unexecutable today and stay so until the execution phase is scheduled". Fork 5: "Resolved by: the execution phase against E-01, E-02, AR-05" (RM §6 q3).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | — (none mandatory)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No gap. One Phase 2 prerequisite is named by the docs: "execution's re-verification of the fingerprint is invariant 13's half" (RM §4 I5) — **plan identity/fingerprint** is what makes execution-side re-verification possible; and the loader's "stored values are caches, never a second source of truth" anticipates "E-11's execute-time re-read" (DL D9). Both live in items already owned by I5/I6.                                                                                                                                                                                                                                                     |
| **14. Dependency propagation is declared and provable in the negative.** Stress: "PL-02, PL-03, PL-05." First provable: "Phase 2, gated on the package axis being scheduled (the matrix stamps the monorepo scenarios hypothetical-future)." (RM §4 I14)                                                                                                                                            | **Propagation.** "When a release changes a package that has dependents, the plan records the propagation edges and their topological order as declared content, and can state which packages did not move and why." (RM §4 I14.) Adjacent planning policy routed to Phase 2: "Breaking-on-non-releasing-type; dependent bumps on compatible ranges (forks 10, 12). Resolved by: planning policy in Phase 2 (PL-05b, PL-02)" (RM §6 q9). Activation: fork 11 "Resolved by: the scheduling decision that activates PL-01..PL-05 and M-11's tag-namespace rule" (RM §6 q8). RM §7 row 6 (preserve); row 10 (adapt: "Generic propagation over a declared dependency graph, not tied to npm or cargo (PL-02 …)").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **(Primary gap — see next column.)** Partial absorption only: **bump resolution** (forks 10/12 dependent-bump and breaking-on-non-releasing-type policy) · **no-op/withheld decisions** (negative half: "can state which packages did not move and why") · **planner input boundary** ("component metadata" is a named planning input, RM §3 signature).                                                                                                                                                                           | **GAP A1 (primary finding):** I14 is first-provable in Phase 2, but the fixed 14-item contract list contains **no propagation item** — nothing owns "the plan records the propagation edges and their topological order as declared content". Currently latent (gated on the package axis, per the invariant's own phase sentence); the moment the gate opens, the contract as listed cannot carry the invariant. Either add an item or pin the declared-content half to named items with the gate stated.                                                                                                                                                     |
| **15. Provider isolation.** Stress: "M-10, E-06, PR-04, AR-03." First provable: "Phase 2 for the planner's output schema; the kernel half holds by inspection of `Version` today and must be asserted by Phase 1's tests for every new value — ADR-0001's import ban polices imports, not field naming." (RM §4 I15)                                                                                | **Integration API + input model (output-schema half).** "Neither the kernel's values nor the planner's outputs name provider _state_ — branches, pull requests, GitHub Releases, dist-tag moves, registry contents, or runners; providers appear only as adapters that feed observations in and execute bindings out. Coordinates and channel bindings are opaque declared configuration labels (AR-03's registry coordinates included), never references a value or plan could dereference or mutate." (RM §4 I15.) RM §7 row 12 (break): "Providers are adapters; plans and attempts name no GitHub concept (invariant 15)".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **planner input boundary** ("providers appear only as adapters that feed observations in and execute bindings out") · **normalized repository observations** (provider facts enter normalized, never as live provider state) · **branch/ref attribution** (branch names as attribution data; identity remains the line id, I7's landing)                                                                                                                                                                                           | **GAP A2 (secondary finding):** the invariant's Phase 2 half is "the planner's **output schema**", but no fixed contract item owns the plan's output schema as such — the items above cover only the input side ("feed observations in **and execute bindings out**"). Assign explicitly (two-sided planner input boundary, or a new item), or I15's Phase 2 claim lands ownerless. Phase 1 already delivered its half: P1C "Provider isolation, executable — for every exported value, the test asserts the value's field names contain no provider vocabulary (`branch`, `ref`, `pr`, `pull`, `github`, `dist-tag`, `registry`, `runner`)" — not re-claimed. |

---

### Reverse index: contract item → invariants (items §2.1–§2.14 audited; §2.15–§2.16 added in reconciliation — A1/A2 below)

| Contract item                        | Owned by invariants (evidence)                                                                                                                                                      |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| planner input boundary               | I1 (defaults as data, RM §5), I2 (time/policy digests as values), I3 (enumerated frozen inputs), I4 (codomain), I14 (component metadata input), I15 (adapters feed observations in) |
| normalized repository observations   | I6 (tag history), I9 (parsed changes), I7 (ref observations for attribution), I15 (provider-normalized)                                                                             |
| change identity and lineage handling | I9 (primary), I10 (P-03 empty inherited change set, via planner)                                                                                                                    |
| branch/ref attribution               | I7 (feed mappings), I4 (M-01 fail-closed refusal as record), I15 (refs as data, not identity)                                                                                       |
| range calculation                    | I6 (range from line tag history), I4 ("evaluated range" in records), I1 (tag cursor default)                                                                                        |
| release-line selection               | I7 (stable ids), I8 (per-line prerelease policy), I1 (default line, S-05)                                                                                                           |
| bump resolution                      | I1 (default CC mapping), I4 (PL-01 unmapped → record), I8 (P-04 sequence bump), I14 (forks 10/12 policy)                                                                            |
| prerelease policy resolution         | I8 (primary: forks 3/4/17, pointer convention), I11 (residue: (line, target, stream) claim scope values)                                                                            |
| no-op/withheld decisions             | I4 (primary), I1 (S-01 no-op, S-02 recorded bootstrap choice), I14 (negative propagation statements), I5 (no-op successor closing a superseded plan, DL D9)                         |
| plan supersession                    | I5 (primary; fork 14 single rule; mixed-kind endpoints per DL D9), I6 (regeneration re-targets on published version, PL-08)                                                         |
| plan identity/fingerprint            | I5 (primary), I2 (fingerprint stability), I13 (prerequisite for execution's re-verification, RM §4 I5 note)                                                                         |
| self-reference exclusion             | I3 (PL-04 stress parenthetical: "the planner's own outputs are not foreign input"; DL D9 named-rule obligation)                                                                     |
| tag-history projection               | I6 (primary), I8 (fork 17 next-from-tags), I11 (M-11 target-tag-nonexistence precondition)                                                                                          |
| plan determinism                     | I2 (primary), I1 (trivial path testable only via determinism, RM §5), I3 (purity corollary)                                                                                         |

**Direction B result: no orphan contract items.** Every one of the 14 items then in the contract (§2.1–§2.14) is claimed by at least one invariant. The two weakest-linked items — **self-reference exclusion** (anchored only in I3's stress parenthetical; "no invariant sentence carries it yet", DL D9) and **branch/ref attribution** (I7 plus the M-01 obligation that "no invariant's stress list names", DL D9) — are exactly the two whose invariant anchors need strengthening, not new items.

---

### Gaps

**Direction A — invariants without a Phase 2 landing _and_ without an execution deferral:** none strictly; every invariant has a mechanism+items, a Phase 1 landing consumed by Phase 2, or a verbatim "later/execution" deferral (I10–I13, and I1's attempt-and-tag half). Four structural gaps remain inside that clean surface:

- **A1 (primary). Invariant 14 — Phase 2-provable, no contract item.** The fixed list has no propagation item; "the propagation edges and their topological order as declared content" is ownerless. Latent while gated on the package axis ("the matrix stamps the monorepo scenarios hypothetical-future"), but the contract must either add an item or pin the declared-content half to named items before the gate can open.
- **A2. Invariant 15's output-schema half has no owning item.** The invariant's phase sentence makes "the planner's output schema" the Phase 2 deliverable; the 14 items then in the contract cover its input side only.
- **A3. Invariant 3's purity sentence has no enforcement item.** It distributes over planner input boundary + plan determinism; the only named enforcement is the risk register's "Purity gates + adversarial planner review in Phase 2H" (DL). The contract should name the gate as a deliverable.
- **A4. Two contract items lack invariant stress coverage** (inverse-direction check): **branch/ref attribution** — M-01 "needs an invariant home … no invariant's stress list names it" (DL D9; fix: add to I4's stress list or extend I6's sentence); **self-reference exclusion** — "no invariant sentence carries it yet" (DL D9). Per ADR2 D3, an item whose invariant anchor is missing cannot be named as making any invariant executable.

**Direction B — contract items no invariant needs:** none (reverse-index table above). All 14 items audited (§2.1–§2.14) have owners; §2.15–§2.16, added in reconciliation, map to invariants 14 and 15 (A1/A2). A4 records the two whose invariant-side anchoring is thin.

---

1. **Fork 16 — where decision records and plans persist** (RM §6 q2: "Resolved by: Phase 2's decision records needing a home — the constraint that it stays outside `core/domain/` (ADR-0001) is already fixed"; ADR2 consequences: "a thin package-layer writer around the planner"). The contract's items cover the planner function, not its persistence seam; the contract must decide whether storage is in scope or explicitly adjacent.
2. **Fork 8 ownership conflict (minor documentary tension).** RM §6 q4 routes the change-id marker convention to "Phase 1's adapter work"; P1C lists it out of Phase 1's scope ("adapters/Phase 2 decisions respectively"). Phase 1 shipped no adapter, so marker parsing must land with Phase 2 extraction or earlier adapter work — needs explicit routing in the contract.
3. **Fingerprint input enumeration.** I5's sentence names target version, change sets, base bindings, policy digests; E-04/E-11 stress it. The contract item must pin the exact input tuple, or two implementers can ship different fingerprints for the same plan.
4. **Prerelease pointer convention** (DL D9): "the planner picks one and records it" — M-08 (pointer moves) vs P-02/P-07 (pointer silent) is a genuine policy fork the contract must close, plus P-02's ladder-override branch that "publishes a version that sorts below the pointer and therefore cannot move it."
5. **Supersession endpoints** (DL D9): "invariant 5's recorded relation must admit mixed-kind endpoints" — a plan superseded _by a decision record_ must be representable; conflicts with any supersession relation typed plan→plan only.
6. **Forks 1–2 (bootstrap default, chore-only history)** — "Resolved by: default-policy design in Phase 2 … S-02's demand for an explicit recorded decision is not negotiable away" (RM §6 q5). Open parameter, named owner, non-negotiable constraint.
7. **Forks 10/12 vs package-axis gating**: RM §6 q9 routes PL-05b/PL-02 planning policy to Phase 2 unconditionally, while I14's phase sentence and fork 11 (RM §6 q8) gate PL-01..PL-05 on a scheduling decision. The contract must state which parts of bump resolution are Phase 2 regardless of the package axis.
8. **Seed convention residue** (P1C): `.0` is recorded state; `.1`-bodies are "planner-side next-from-tags computations, not recordings" — the prerelease policy item must encode this split or re-derive seeds wrongly.

---

### Sources consulted

- `docs/design/release-model.md` — §3 ("What planning is" signature; vocabulary rows Change, ChangeSet, ReleaseLine, ReleasePlan; companion planning concepts; identity separations), §4 ("Architectural invariants", I1–I15 verbatim), §5 ("Complexity budget": trivial path, "The mechanism: defaults as data", "What a complex repository may explicitly declare", "The honest cost"), §6 ("Unresolved questions" q1–q11), §7 ("release-please compatibility boundary" rows 1–18).
- `docs/adr/0001-domain-kernel-and-semantic-version.md` — decisions 1–10 (boundary law, layered purity, grammar, safe-integer bound, equals/compare, decision 7 deferral as amended by D6, alias seam, external-consumer tests, decisionRef deferral), "Limitations, stated honestly", "Consequences".
- `docs/adr/0002-release-model-and-domain-vocabulary.md` — decisions 1–7, "Refused alternatives", "Limitations, stated honestly" (five unexecutable invariants; open parameters), "Consequences" (Phase 1/Phase 2 scope; "Each Phase 1 and Phase 2 contract that lands must name the invariants it makes executable").
- `docs/design/phase1-contracts.md` — design rules, per-value contracts (`Bump`, `Change`, `ChangeSet`, `ReleaseLine`, `Channel`, `Artifact`), barrel/alias seam, ADR-0001 amendments carried, "Test obligations", "Out of scope (and where it stays)".
- `docs/design/decision-log.md` — "Carried obligations (from the Phase 1 adversarial review, D9)" (empty groups, prerelease pointer, M-01, PL-04 self-reference, supersession endpoints, invariant-10 tightening, tag-history loader, nightly, change→released-versions relation), "Rejected alternatives" R1–R2, "Risk register" (purity gate, Phase 2H), "Scenario coverage".

## 5. Scenario inventory

The inventory below is the authoritative planning-side index over all 53
scenarios: who owns the decision (planner full/partial/execution-only), the
verbatim expected outcomes, the inputs each scenario needs, and the fixture-
owning workstream. Coverage summary from the audit: **30 full / 7 partial /
16 execution-only.** The matrix file
([release-scenarios.md](release-scenarios.md)) remains the fixture source of
truth — on any divergence the matrix wins and this index is updated in the
same PR.

Fixture-owning Phase 2 workstreams (the task names used in the table),
defined so ownership is auditable:

- **inputs+extraction** — §2.1, §2.2, §2.3 (input boundary, observations,
  change identity/lineage);
- **attribution** — §2.4 (ancestry attribution, fail-closed refusal);
- **tag-history+ranges** — §2.5, §2.13 (loader, ranges, projections);
- **bump+decisions** — §2.7, §2.9 (bump policy, negative-outcome records);
- **version-planning** — §2.6, §2.8 (line selection, prerelease policy);
- **propagation** — §2.15 (package axis, edges, negative proof);
- **plan-identity** — §2.10, §2.11 (supersession, fingerprint);
- **integration** — §2.12, §2.14, §2.16 + the public API and the
  planner-isolation gate (§4, A3).

Phase 3 workstreams (line-policy, feeds, streams, lifecycle, multi-line,
promotion, channels) own their scenarios' fixtures per the table; the
phase-boundary rulings that place each mechanic are in §6.

**Scope.** Built from `docs/design/release-scenarios.md` read in full (canonical vocabulary, all 53 scenario bodies, stress analysis, open-forks section), with fork→phase resolution from `docs/design/release-model.md` §6 (Unresolved questions) and decision-log D9. ADR-0002's locked vocabulary is used verbatim; no new terms are coined. Scenario-local lifecycle labels that appear in quotes (`pending-withheld`, `orphaned-prerelease`, `abandoned-by-human`, `satisfied-externally`, `adopted-from-A`) are the matrix's own state names, not new vocabulary.

**Reading conventions / assumptions.**

1. **Verbatim quotes**: `Expected release decision` and `Expected versions` are reproduced exactly (backticks, `**`/`_` emphasis, arrows, em-dashes preserved). The source hard-wraps fields across lines; wraps are joined with single spaces — no wording, punctuation, or emphasis altered. These are the golden-fixture expectations.
2. **Planner-owned** = who computes/records the scenario's expected decision (decision ownership), independent of which phase builds the fixture. `partial` rows name the split.
3. **Fixture task** = single best-fit owner from the assignment's taxonomy; secondary sub-fixtures noted inline. `§6` = release-model.md §6 (Unresolved questions) — numbering derived from internal citations (D7 cites §3 for the vocabulary table, D8 cites §7 for the compatibility boundary, which fixes §6 = Unresolved questions).
4. **Fork column** lists every fork whose citation list names the scenario plus explicit in-body fork mentions; `P0` = settled by release-model.md itself (Phase 0), `P1`/`P2`/`P3` = phases, `EXE` = execution phase.
5. Task names (inputs+extraction, attribution, …) are the orchestrator's Phase 2 work-stream names; no repo doc defines them yet (assumption stated, flagged in Conflicts).

---

### Table 1 — Scenario inventory (53 rows)

Fork resolver shorthand: **P2** = Phase 2, **P3** = Phase 3, **EXE** = execution phase, **P1** = Phase 1, **P0** = settled by release-model.md itself.

| ID + title                                                             | Planner-owned                                                                                                                                                                                         | Expected release decision (VERBATIM)                                                                                                                                                                                                                                                                                                                                                                               | Expected versions (VERBATIM)                                                                                                                                                                                                                                                                     | Inputs the planner must receive                                                                                                                                                                                                                                                                                                                                   | Fixture owner (task)                                                                                                                                                                                                                                                             | Open fork(s) cited                                                                                                        |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| **S-01** — Chore-only runway: the recorded no-op                       | **full**                                                                                                                                                                                              | "no release, no plan, no release PR; a decision record `no-op, cause: no release-worthy changes` (chore/docs/ci are not release-triggering by default — an open policy fork)."                                                                                                                                                                                                                                     | "manifest stays `1.0.1`; the latent `1.0.2` is minted nowhere."                                                                                                                                                                                                                                  | Tags `1.0.0`, `1.0.1`; manifest `1.0.1`; three commits since `1.0.1` (`chore: bump devdep floor`, `docs: fix typo in quickstart`, `ci: cache the store directory`); release-type policy (default exclusions); operator intent "cut whatever is due" + override-allowance policy; policy version (binds the decision-record identity)                              | P2: **bump+decisions** (no-op/override decision-record kinds; identity binds range `1.0.1..HEAD` + commit list + policy version)                                                                                                                                                 | 1 (P2), 16 (P2)                                                                                                           |
| **S-02** — First release ever: two absent truths                       | **full**                                                                                                                                                                                              | "release (the first), gated on a recorded bootstrap decision; the release plan targets the chosen version over the whole history as change set (bounded by repository start, not a tag)."                                                                                                                                                                                                                          | "`1.0.0` (stable-first API) or `0.1.0` (initial-development ramp; SemVer FAQ names both) — either is valid, silently derived is not. Manifest `0.0.0` never leaks as the release version."                                                                                                       | Zero tags; manifest `0.0.0`; full history (`feat: core loop`, `feat: cli scaffold`, `fix: parser edge case`, chores); absence of initial-version policy; operator intent "make the first release"; recorded bootstrap decision (version, who, when)                                                                                                               | P2: **version-planning** (bootstrap default policy; empty-tag-history range = repository start; line birth version)                                                                                                                                                              | 2 (P2)                                                                                                                    |
| **S-03** — Manifest drift: the hotfixes that never came home           | **full**                                                                                                                                                                                              | "release; patch on line 1.x; plan scoped to `1.9.5..HEAD` **on main** — hotfix commits inside `1.9.1`–`1.9.5` are already released and are not re-listed."                                                                                                                                                                                                                                                         | "`1.9.6`. Naive manifest-truth computes `1.9.1` — an existing tag: collision (re-tag refused) or the fix is never released."                                                                                                                                                                     | main manifest `1.9.0` (stale); tags `1.9.0` (main) + `1.9.1`–`1.9.5` (from `release/1.9`); recorded line→branch feed mapping; commit `fix: guard empty config` on main; operator intent "release the fix from main"                                                                                                                                               | P2: **tag-history+ranges** (line tag history outranks manifest; per-line released range excludes hotfix commits; `manifest-repair` annotation + drift flag)                                                                                                                      | —                                                                                                                         |
| **S-04** — Release-worthy without changelog-worthy                     | **full**                                                                                                                                                                                              | "release; patch on line 1.x."                                                                                                                                                                                                                                                                                                                                                                                      | "`1.0.5` (Conventional Commits: `fix` → patch). The bump-driving classification and the note-producing classification are independent; conflating them either skips the release or fabricates notes."                                                                                            | Tags through `1.0.4`; manifest `1.0.4`; config excluding scope `internal` from changelog notes (note-classification knob only); commits `fix(internal): harden token scrubbing`, `fix(internal): close race in session cache`; operator intent "release"; exclusion-config version (names the decision record)                                                    | P2: **bump+decisions** (dual per-change classification: bump-relevant vs note-relevant; empty-notes marker as recorded outcome)                                                                                                                                                  | —                                                                                                                         |
| **S-05** — Major cut while the maintenance line lives                  | **full**                                                                                                                                                                                              | "release; major on the new line 2.x; no interference with line 1.x."                                                                                                                                                                                                                                                                                                                                               | "`2.0.0` (breaking → major; minor and patch reset). Two live lines whose tags interleave in time: `2.0.0`, then later `1.9.6` — global tag order is not version order."                                                                                                                          | main manifest `1.9.0`; tags through `1.9.5`; live branch `release/1.9` with pending backport (`fix: backport safe default`); commit `feat!: require config schema v2` on main; operator intent "cut 2.0.0 from main"; line registry (both lines, feed branches, heads)                                                                                            | P2: **version-planning** (line 2.x creation; per-line independent next-version; couples to P3 lifecycle line registry)                                                                                                                                                           | —                                                                                                                         |
| **P-01** — Alpha increments: lexicographic order lies                  | **full**                                                                                                                                                                                              | "release; prerelease on line 1.x, stream `alpha`, same target 1.2.0."                                                                                                                                                                                                                                                                                                                                              | "`1.2.0-alpha.10`. A string-sorting tool reads head `alpha.9 > alpha.10` and re-publishes `alpha.9`, colliding with the existing tag. The kernel half of this trap (numeric comparison, safe-integer bounds) is already owned by ADR-0001."                                                      | Tags incl. `1.2.0-alpha.9` (stream head; recomputable by SemVer precedence over all `1.2.0-alpha.*` tags); manifest `1.1.7`; commit `fix: retry idempotency keys`; operator intent "continue the alpha stream"; existing-tag anomaly rule (hand-pushed `alpha.10` → next is `alpha.11`)                                                                           | P2: **version-planning** (stream-head recompute from tags by precedence; numeric increment of stream head)                                                                                                                                                                       | 17 (P2, by citation — `.1`-first would be planner policy over tags; kernel seeds `.0`)                                    |
| **P-02** — Same target, three costumes: alpha → beta → rc              | **full**                                                                                                                                                                                              | "release (prerelease re-stream); the change set is identical to the alpha run's — a publication without content change, represented deliberately."                                                                                                                                                                                                                                                                 | "`1.2.0-beta.0`, then `1.2.0-rc.0`; resetting the number per identifier is spec-consistent because the identifier dominates precedence."                                                                                                                                                         | Tag `1.2.0-alpha.3`; no new commits; fixed ordered stream-ladder policy (alpha → beta → rc) + override rule (beta while `rc.1` live → refused by default, override needs recorded operator decision); per-stream lifecycle state; operator intent "move stabilization to beta", later "promote to rc"                                                             | P2: **version-planning** (re-stream mint; content ↔ publications mapping queryable; ladder-override refusal)                                                                                                                                                                     | 4 (P2), 17 (P2)                                                                                                           |
| **P-03** — RC to stable across a zero diff                             | **partial** (planner: decision + target + inherited change set; execution/P3 promotion: `promoted-from` edge, stream close, channel membership)                                                       | "release (stable) with an inherited change set; explicitly not a no-op despite the empty diff."                                                                                                                                                                                                                                                                                                                    | "`1.2.0` (SemVer §11.3: `1.2.0-rc.1 < 1.2.0`)."                                                                                                                                                                                                                                                  | Tag `1.2.0-rc.1` at C1; no commits after C1 (zero diff); rc.1's change set id + source commit (the equivalence audited); operator intent "promote rc.1 to stable" / "release 1.2.0"; strict policy (stable must contain exactly what was validated; post-rc commit → `rc.2` first)                                                                                | P2: **version-planning** (stable mint over inherited change set; zero-diff is not emptiness); P3 **promotion** owns the edge/channel half                                                                                                                                        | —                                                                                                                         |
| **P-04** — Feat mid-RC: the boolean breaks                             | **full**                                                                                                                                                                                              | "release; prerelease, stream `rc`, line 1.x."                                                                                                                                                                                                                                                                                                                                                                      | "`1.2.0-rc.2`. Both naive outcomes are wrong: \"not a prerelease anymore → publish 1.2.0\" ships an unreviewed feature inside the reviewed version; \"restart at rc.0\" produces a version that sorts below the live `rc.1` (identical cores: `rc.0 < rc.1`) and inverts history."               | Tag `1.2.0-rc.1` at C1; commit `feat: add webhook retries` (C2); operator intent "next RC"; per-stream sequence state; immutability rule (rebuild rc.1 refused; existing `rc.2` tag → `rc.3`)                                                                                                                                                                     | P2: **version-planning** (sequence +1 on same target; plan change set grows to include C2)                                                                                                                                                                                       | —                                                                                                                         |
| **P-05** — Breaking mid-RC: the target moves under you                 | **full**                                                                                                                                                                                              | "release; prerelease on the new target; supersession record for the abandoned 1.2.0 target."                                                                                                                                                                                                                                                                                                                       | "`2.0.0-rc.0`. Any `2.0.0-*` outranks all `1.2.0-*` by core comparison, so history never inverts."                                                                                                                                                                                               | Tag `1.2.0-rc.1` (target 1.2.0); breaking commit `feat!: rename the config schema` (C2, as evidence for the recompute event); stream-restart policy choice (keep identifier + reset sequence vs restart ladder — chosen explicitly); operator intent "continue stabilization"; plans targeting 1.2.0 (to invalidate)                                              | P2: **version-planning** (target recompute 1.2.0 → 2.0.0; trajectory re-key; supersession/invalidation of 1.2.0 plans; abandoned-stream state) — invalidation mechanics shared with **plan-identity**                                                                            | 3 (P2), 14 (P0 doctrine / P2 params), 17 (P2)                                                                             |
| **P-06** — Two streams, one target                                     | **full**                                                                                                                                                                                              | "release; **targeted** prerelease on stream `rc` of line 1.x."                                                                                                                                                                                                                                                                                                                                                     | "`1.2.0-rc.2` only. A single \"next version\" pointer bumps the wrong stream, bumps both, or errors on ambiguity — all unacceptable."                                                                                                                                                            | Tags `1.2.0-alpha.4` AND `1.2.0-rc.1`; commit `fix: race in the scheduler`; per-line stream table (label → head version, base commit, lifecycle state); operator intent "advance rc only" (omitted stream → ambiguity error listing live streams, never a guess); collapse-detection rule (breaking commit while both live → both re-key to new target)           | P2: **version-planning** (stream-keyed targeting; per-stream heads + one shared line target)                                                                                                                                                                                     | —                                                                                                                         |
| **P-07** — RC-first on the maintenance line, under a 2.x main          | **full**                                                                                                                                                                                              | "release; prerelease on line 1.x, fed by branch `release/1.2` — branch and line are distinct (N1)."                                                                                                                                                                                                                                                                                                                | "`1.2.4-rc.0`. A global-pointer tool computes `2.1.1-rc.0` (wrong line, wrong base) or `1.2.4` directly (skips the policy-mandated RC)."                                                                                                                                                         | main at `2.1.0` (manifest 2.1.0); branch `release/1.2` at `1.2.3`; line policy "maintenance fixes ride an RC before stable"; commit `fix: backport safe default` on `release/1.2`; operator intent "release it as an RC first, from the maintenance line"; cross-line provenance edge (backport later on main → `2.1.1` references the maintenance release)       | P2: **version-planning** (line-scoped prerelease computation; two coexisting next-version derivations); line-RC policy = P3 **line-policy** input                                                                                                                                | 17 (P2, by citation — mints `rc.0`)                                                                                       |
| **M-01** — A fix on the maintenance line is not a main commit          | **full**                                                                                                                                                                                              | "release line `1.9`; `main` has an empty effective change set and is a no-op (see PL-06)."                                                                                                                                                                                                                                                                                                                         | "`1.9.0` → `1.9.1`; `main` remains `2.3.0`."                                                                                                                                                                                                                                                     | Plan request with NO explicit line argument; branches `main` (tag `2.3.0`, at C3) and `1.9` (tag `1.9.0`, at C9); commit F `fix(parser): handle empty input` pushed to `1.9` only; ancestry (the branch contains F — never author date or message scope); per-line released-version pointers + repository line registry                                           | P2: **attribution** (ancestry-based line attribution, fail-closed refusal on misattribution — D9 obligation: this needs an invariant home in the Phase 2 contract)                                                                                                               | —                                                                                                                         |
| **M-02** — Three active lines release independently                    | **full** (decision is planner's; the multi-line pass fixture is P3 per taxonomy)                                                                                                                      | "release `1.9`, `2.2`, and `main`; no decision depends on or blocks another."                                                                                                                                                                                                                                                                                                                                      | "`1.9.1` (fix), `2.2.4` (fix), `2.4.0` (feat)."                                                                                                                                                                                                                                                  | One plan request covering all lines; commits F1 (`fix:`) on `1.9`, F2 (`fix:`) on `2.2`, F3 (`feat:`) on `main`; per-line tags (`1.9.0`, `2.2.3`, `2.3.0`); per-line released-version pointers                                                                                                                                                                    | P3: **multi-line** (one pass → three independent releases; each line's range evaluated separately; no global next-version scalar)                                                                                                                                                | —                                                                                                                         |
| **M-03** — One logical fix on three lines: cherry-pick vs propagate    | **full**                                                                                                                                                                                              | "release all three lines."                                                                                                                                                                                                                                                                                                                                                                                         | "`main` per its own pending change set; `2.2` → `2.2.4`; `1.9` → `1.9.1`."                                                                                                                                                                                                                       | Commits F (main), F′ (`2.2`), F″ (`1.9`, adapted content); cherry-pick provenance (the `(cherry picked from commit …)` trailer or equivalent change-id marker — fork 8); the three lines' tags; per-line ranges; later maintenance→main merges (dedupe explicitly per lineage)                                                                                    | P2: **attribution** (change identity ≠ SHA ≠ content hash; machine-readable provenance; change → released-versions mapping)                                                                                                                                                      | 8 (P1)                                                                                                                    |
| **M-04** — Clean backport: main must not move                          | **full**                                                                                                                                                                                              | "release `1.9` only; `main` unchanged (F remains pending)."                                                                                                                                                                                                                                                                                                                                                        | "`1.9.0` → `1.9.1`; `main` remains `2.3.0` with F pending."                                                                                                                                                                                                                                      | Pending fix F on main (unreleased); clean cherry-pick F′ on `1.9` with provenance; backport intent (implicit in cherry-pick or explicit in request); per-line released sets                                                                                                                                                                                       | P2: **attribution** (releasedness per (line, change); lineage links never suppress)                                                                                                                                                                                              | —                                                                                                                         |
| **M-05** — Conflicting backport: same change, divergent payloads       | **full**                                                                                                                                                                                              | "release `1.9` (F′); `main` releases F with its own pending change set later."                                                                                                                                                                                                                                                                                                                                     | "`1.9.1` for the backport; main's next per its own pending change set."                                                                                                                                                                                                                          | F and F′ (different SHA, different patch) with provenance linking them; both lines' tags; lineage graph (many-to-many, commits↔changes across lines); fuzzy-matching refusal rule                                                                                                                                                                                 | P2: **attribution** (lineage survives content divergence; consume declared lineage or treat as unrelated-but-similar)                                                                                                                                                            | 8 (P1)                                                                                                                    |
| **M-06** — Backport of an already-released change                      | **full**                                                                                                                                                                                              | "release `1.9`; no re-release or re-tag on `main`."                                                                                                                                                                                                                                                                                                                                                                | "`1.9.1` — allocated in `1.9`'s own version history; neither suppressed because F is released elsewhere, nor interacting with `2.3.0`."                                                                                                                                                          | F′ with provenance to already-released F (released on main as `2.3.0`); both lines' tags; per-line released sets; per-line version allocation                                                                                                                                                                                                                     | P2: **attribution** ("released" predicate over (line, change); lineage recorded for traceability, not suppression)                                                                                                                                                               | —                                                                                                                         |
| **M-07** — Divergent maintenance line: commits main never had          | **full**                                                                                                                                                                                              | "release `1.9`; the feat drives a minor bump even on a maintenance line (an alternative fix-only maintenance policy must then reject D loudly instead of demoting it silently)."                                                                                                                                                                                                                                   | "`1.9.0` → `1.10.0`."                                                                                                                                                                                                                                                                            | Plan request for `1.9`; the line's tag `1.9.0`; branch head; commits D (`feat:` main-never-had) + F1 discovered from `1.9`'s own range (never a main-relative diff)                                                                                                                                                                                               | P2: **tag-history+ranges** (per-line range = line's own last release tag → line's own head; no ancestry-inclusion assumption)                                                                                                                                                    | —                                                                                                                         |
| **M-08** — Per-line policies: prerelease here, forbidden there         | **full**                                                                                                                                                                                              | "`main` → prerelease; `1.9` → stable patch; the prerelease request on `1.9` is rejected with the policy reason (recorded, not retried)."                                                                                                                                                                                                                                                                           | "`2.4.0-rc.1` on `main`; `1.9.1` on `1.9`. The kernel computes bumps but not next-prerelease — that operation is line policy per ADR-0001's deferred seam; this scenario is its first consumer."                                                                                                 | Breaking change B (`feat:` + `BREAKING CHANGE:` footer) on main; fix F on `1.9`; both line policies (main: `rc` stream allowed; `1.9`: stable-only); operator's explicit prerelease request for `1.9`; per-line policy records incl. stream allowance; next-prerelease operation (ADR-deferred seam)                                                              | P3: **line-policy** per assignment taxonomy — but §6 item 6 resolves forks 3/4 via "the line-policy schema in Phase 2" (CONFLICT, see below); decision shapes are a P2 **bump+decisions** consumer                                                                               | 4 (P2), 17 (P2, by citation — "first-ever `2.4.0-rc.1`")                                                                  |
| **M-09** — Fix released on maintenance before main receives it         | **full**                                                                                                                                                                                              | "release `main`."                                                                                                                                                                                                                                                                                                                                                                                                  | "`2.3.0` → `2.3.1` — correct even though `1.9.1` already shipped the same fix to a different audience."                                                                                                                                                                                          | F″ on main with lineage to released F (released on `1.9` as `1.9.1`); main's tag `2.3.0`; per-line released sets; rule: version precedence across lines implies nothing about content                                                                                                                                                                             | P2: **attribution** (lineage recorded cross-line, suppressive only within a line)                                                                                                                                                                                                | —                                                                                                                         |
| **M-10** — Line renamed or retired                                     | **partial** (planner: release decision under rename + withheld record; P3 lifecycle: line identity/rename/retire state)                                                                               | "rename → release `1.9-lts` normally (`1.9.1`). Retire → no release; F withheld."                                                                                                                                                                                                                                                                                                                                  | "`1.9.1` on the renamed line; none for the retired line."                                                                                                                                                                                                                                        | The ref rename `1.9` → `1.9-lts` (tracked as rename, not create+delete); the retirement decision (policy state change); post-retirement commit F; line entity keyed by stable id with lifecycle (`active`, `frozen`, `retired`); pending-withheld record (commit, reason, timestamp)                                                                              | P3: **lifecycle** (line identity ≠ ref name; retirement ≠ deletion); P2 **bump+decisions** owns the withheld-change record half (fork 9)                                                                                                                                         | 9 (P2)                                                                                                                    |
| **M-11** — Same version number on two lines                            | **full**                                                                                                                                                                                              | "rejected plan; no release."                                                                                                                                                                                                                                                                                                                                                                                       | "none applied; the error names the colliding version, both commits, and both lines."                                                                                                                                                                                                             | The invalid plan (candidate tag `1.9.1` at a different commit on main); existing tag `1.9.1` at C9x on line `1.9`; intended target commit; global tag→commit map re-read at execution time + per-line version history (both maps checked)                                                                                                                         | P2: **plan-identity** (plan precondition: target tag must not exist at any commit, verified before execution)                                                                                                                                                                    | 11 (carry — resolver is the package-axis scheduling decision, phase unnamed)                                              |
| **PL-01** — Monorepo: one package changed, one package released        | **full**                                                                                                                                                                                              | "release `lib-a` only."                                                                                                                                                                                                                                                                                                                                                                                            | "`lib-a` `1.2.0` → `1.2.1`; `app` and `lib-b` unchanged."                                                                                                                                                                                                                                        | Commit `fix(lib-a): guard empty config` touching only `packages/lib-a/src/**`; package layout; explicit total scope/path → package mapping (misses loud, unmapped = error); `lib-a` released `1.2.0`; per-package released-version pointers; package graph as plan input                                                                                          | P2: **attribution** (commit → package via explicit total mapping; scope + path verified)                                                                                                                                                                                         | 11 (carry; explicit in body: "an open naming fork" on per-package tag naming)                                             |
| **PL-02** — Dependency propagation: the changed package has dependents | **full**                                                                                                                                                                                              | "case 1 — release `lib-a`; dependents per explicit policy (recommended: bump only dependents whose manifests change, so lockfile truth matches the registry). Case 2 — release `lib-a` `2.0.0`; `lib-b` (range-widening = minor); `app` likewise."                                                                                                                                                                 | "case 1: `lib-a` `1.2.1` (+ dependent patches per policy). Case 2: `lib-a` `2.0.0`, `lib-b` `1.5.0`, `app` `3.1.0`."                                                                                                                                                                             | The commits per case; workspace dependency graph; manifests' range expressions (`^1.2.0` etc., read at plan time); dependent-bump policy (fork 12); topological propagation order (`lib-a` → `lib-b` → `app`)                                                                                                                                                     | P2: **propagation** (range math over declared edges; message-only bumping refused)                                                                                                                                                                                               | 12 (P2)                                                                                                                   |
| **PL-03** — Unrelated isolation: neighbors must not move               | **full**                                                                                                                                                                                              | "release `tool-x` (`0.3.0` → `0.3.1`); docs-only commit filtered (docs/chore are non-releasing types by policy, not by hard-coding)."                                                                                                                                                                                                                                                                              | "`tool-x` `0.3.1`; all others unchanged; a second plan run over the docs commit → no version anywhere."                                                                                                                                                                                          | Commits `fix(tool-x): correct CLI exit code` + `docs(lib-a): expand README`; dependency graph (no reverse edges from `tool-x`); release-type policy table (docs filtered by policy); negative-evidence output ("why nothing else moved") as first-class plan output                                                                                               | P2: **propagation** (non-impact demonstrable: absence of reverse-dependency edges + explicit type filter)                                                                                                                                                                        | —                                                                                                                         |
| **PL-04** — The release PR's own output is not a change                | **full**                                                                                                                                                                                              | "no release attributable to R; if other pending commits exist the plan proceeds without R; otherwise PL-06's empty-change-set behavior applies."                                                                                                                                                                                                                                                                   | "none caused by R."                                                                                                                                                                                                                                                                              | Commit R (release-bot-authored, deterministic marker — a `Release-As:`/bot trailer, exact convention = fork 8); release-bot identity convention; range scanning that classifies bookkeeping commits before change detection; ambiguous cases (human-edited changelog) surface for decision, never auto-classify                                                   | P2: **inputs+extraction** (bookkeeping classification upstream of change detection; self-reference/fixed-point guard)                                                                                                                                                            | 8 (P1 for the provenance-marker convention proper; D9 adds a P2 obligation to name the bot-marker rule — fork-8-adjacent) |
| **PL-05** — Ambiguous commits: scope clash, breaking chore, merge      | **full**                                                                                                                                                                                              | "(a) no release — blocked pending config; (b) major bump or blocked-with-guidance; (c) a normal release from the deduplicated set."                                                                                                                                                                                                                                                                                | "(a) none; (b) `2.0.0` if the breaking-marker rule is adopted; (c) per the deduplicated set."                                                                                                                                                                                                    | (a) commit `fix(core): …` with scope `core` mapping to two packages + the scope→package table; (b) commit `chore(build): drop node 18` with `BREAKING CHANGE:` footer + release-type policy; (c) a range containing merge commit M whose branch commits are also individually in range + the range-walk definition (first-parent vs topological, recorded policy) | P2: **attribution** (shape (a): total-mapping-or-fail); shape (b) sub-fixture in **bump+decisions** (footer semantics independent of type filters); shape (c) sub-fixture in **inputs+extraction** (range membership as reachability set; merges structural, not change-bearing) | 10 (P2)                                                                                                                   |
| **PL-06** — Empty change set: every commit ignored by policy           | **full**                                                                                                                                                                                              | "no release; the plan exits with `no-op` plus the reason (per-commit classification proving emptiness)."                                                                                                                                                                                                                                                                                                           | "none; `2.3.0` remains the released version."                                                                                                                                                                                                                                                    | Range since `2.3.0` containing only `docs:` and `chore(ci):` commits; release-type policy; range-cursor state (cursor does NOT advance past filtered commits until released or explicitly discarded); per-commit classification as emptiness evidence                                                                                                             | P2: **tag-history+ranges** (range-cursor semantics — per S-01's lineage note "PL-06 owns range-cursor semantics"); the `no-op` decision kind itself lands in **bump+decisions**                                                                                                  | 1 (P2), 9 (P2)                                                                                                            |
| **PL-07** — Change withheld by the line's package policy               | **full**                                                                                                                                                                                              | "if the withheld commit was the only pending change → no release (PL-06 behavior); otherwise release the remaining packages without `app`."                                                                                                                                                                                                                                                                        | "none for `app` on `1.9`; other packages per their pending change sets."                                                                                                                                                                                                                         | Commit `fix(app): prevent startup crash on old kernels` on `1.9`; the line's package-allow policy (`app` frozen on `1.9`); withheld state keyed by (commit, line, rule); range bookkeeping that keeps withheld commits inside the un-released span (unfreeze → re-enters pending change set)                                                                      | P2: **bump+decisions** (withholding records — §6 item 10: "Phase 2's withholding records"); cursor coupling to **tag-history+ranges**                                                                                                                                            | 9 (P2)                                                                                                                    |
| **PL-08** — Stale release plan: the change set grew mid-flight         | **full**                                                                                                                                                                                              | "no immediate release; plan regeneration; the release happens when the regenerated PR merges."                                                                                                                                                                                                                                                                                                                     | "target stays `1.5.0` (minor over `1.4.2`) — but the plan identity changes (P1 → P2) because the change set changed. Version equality must not imply plan equality."                                                                                                                             | Open release PR "release 1.5.0" built from plan P1 frozen at C1 (one `feat:`); new merge C2 `feat: export plugin api`; fingerprint inputs (tip SHA + sorted policy-relevant commit ids + computed bumps — policy-ignored commits do not invalidate); concurrent-merge retarget rule (P1 merges → P2 retargets `1.5.1`); PR ↔ plan linkage                         | P2: **plan-identity** (plan id ≠ target version; change-set hash; base-commit pointer; durable supersession P1 → P2; fingerprint verified immediately before execution)                                                                                                          | 14 (P0 doctrine / P2 params)                                                                                              |
| **AR-01** — One release, three artifacts, all succeed                  | **execution-only** (planner decision trivial; per-artifact state machine + ledger completion predicate are execution)                                                                                 | "release."                                                                                                                                                                                                                                                                                                                                                                                                         | "`1.5.0`; the tag is placed once, at the release commit; the three artifacts all bind to it."                                                                                                                                                                                                    | Commit `abc123` carrying `feat: add streaming export`; pipeline configuration (npm tarball, GitHub Release, container image); artifact identity (kind, coordinates, digest) per output                                                                                                                                                                            | Execution (baseline for E-01/E-02; release complete only when all three artifact records `published` in the ledger)                                                                                                                                                              | —                                                                                                                         |
| **AR-02** — Image depends on npm; registry propagation delay           | **partial** (planner: artifact dependency DAG + verify precondition declared in the plan; execution: visibility verification + timeout → partial-release recovery)                                    | "release with an explicit verify step; npm publish → verify → image build, never publish → build."                                                                                                                                                                                                                                                                                                                 | "`1.5.0`; image pins the exact version and records the tarball's sha512 at build time."                                                                                                                                                                                                          | npm publish completion; image build start; CDN/registry propagation lag; verification method (metadata fetch + tarball digest check); hard-timeout boundary handing off to E-02's ledger resume                                                                                                                                                                   | Execution (cross-artifact dependency edges; observed-digest ↔ expected-digest binding)                                                                                                                                                                                           | —                                                                                                                         |
| **AR-03** — Artifact version scheme differs from release version       | **execution-only** (planning decision trivial; channel semantics are P3)                                                                                                                              | "release."                                                                                                                                                                                                                                                                                                                                                                                                         | "`2.0.0`; the moving tags are channels, not versions — the mutable/immutable split is explicit."                                                                                                                                                                                                 | Release `2.0.0`; image tag policy `{2.0.0, v2, latest}` (container coordinates, exempt from the `v`-prefix ban governing tags the kernel parses); per-tag event log (from-digest, to-digest, cause, release id)                                                                                                                                                   | P3: **channels** (moving tags moved as recorded channel events; digest is identity)                                                                                                                                                                                              | —                                                                                                                         |
| **AR-04** — Nightly and stable from the same commit                    | **partial** (planner: the ruling "nightly: a scheduled build, not a release (no version allocation on the line)" + the stable release decision; P3 channels/feeds: channel moves, nightly scheduling) | "nightly: a scheduled build, not a release (no version allocation on the line — the base `1.5.0` is computed, not minted); stable: release `1.5.0`."                                                                                                                                                                                                                                                               | "`1.5.0-nightly.20260905.abc123` (a valid SemVer prerelease: identifier `nightly`, dot-separated build info) and `1.5.0`. Nightly naming must satisfy SemVer §11's identifier rules — dots inside the prerelease are allowed, and the timestamp must be allocated deterministically (see E-10)." | Cron schedule; the release `1.5.0`; channel policy (`stable` release-triggered, `nightly` cron-triggered); line's last released version `1.4.1` (nightly base computed from it); commit `abc123`; nightly builds recorded with base version + commit for lineage proof                                                                                            | P3: **channels** (nightly-vs-release classification touches **version-planning**'s allocation rule — fork 15)                                                                                                                                                                    | 15 (P3 scheduling of channels and artifacts)                                                                              |
| **AR-05** — Orphan artifact: published but unattributed                | **execution-only** (planning-relevant fragment = the whole decision field: one release total; adoption, not re-publish)                                                                               | "one release `1.5.0` total; B's ledger absorbs A's completed step as `adopted-from-A`."                                                                                                                                                                                                                                                                                                                            | "`1.5.0`, published exactly once on npm."                                                                                                                                                                                                                                                        | Attempt A's partial reality (npm registry state); attempt B's plan (same target, same source); absence of A's ledger; publish-existence probe; attribution evidence (registry metadata: publish timestamp, commit, attestation); integrity-mismatch hard stop (digest ≠ B's build → version burned, retarget `1.5.1`)                                             | Execution (write-ahead ledger discipline; reconciliation procedure)                                                                                                                                                                                                              | 5 (EXE)                                                                                                                   |
| **AR-06** — GitHub Release draft with stale notes                      | **partial** (planner: notes/change-set regeneration over the union range; execution: draft state machine, attributed overwrite)                                                                       | "release `1.5.0` with notes covering `b1..b2` — the union, since `b2` includes `b1`'s span."                                                                                                                                                                                                                                                                                                                       | "`1.5.0`; the published notes match the final change set."                                                                                                                                                                                                                                       | The draft's stored notes (computed against base `b1`); new base `b2`; change sets for both bases; attempt ownership (overwrite rights follow attempt ownership, never name equality); foreign-draft rule (escalate, never silently overwrite)                                                                                                                     | Execution (draft = attempt-owned scratch state; absent/draft/published/failed per artifact with per-state attribution)                                                                                                                                                           | —                                                                                                                         |
| **E-01** — Crash after the tag push: half a release                    | **execution-only**                                                                                                                                                                                    | "policy-bound, but not arbitrary: tag-present + plan-valid → complete-in-place (finish artifacts, channels, ledger); tag-present + plan-invalid → escalate (a human decides delete-tag vs repair); the void-and-skip path (leave the tag, mark the release abandoned, next version `1.5.1`) is the fallback when completion is unsafe. The tag is treated as a claim on `1.5.0` — see E-07."                       | "complete-in-place: `1.5.0` published. Void: no published `1.5.0`; the next release is `1.5.1` (tag namespace stays unique; burned tag recorded)."                                                                                                                                               | The tag's existence; the ledger tail (write-ahead may hold the tag step as completed); the plan (valid?); backend states; detection mapping tag existence → ledger state → chosen recovery                                                                                                                                                                        | Execution                                                                                                                                                                                                                                                                        | 5 (EXE), 16 (P2 — decision-record/ledger home)                                                                            |
| **E-02** — Amputated mid-publication: resume the ledger                | **execution-only**                                                                                                                                                                                    | "release completes via resume; the release record spans A and B."                                                                                                                                                                                                                                                                                                                                                  | "`1.5.0` published once; npm's immutability means the completed step must be adopted, never re-run."                                                                                                                                                                                             | The write-ahead ledger (npm — completed; GitHub Release — started; channels — pending); registry state; the original plan with artifact digests recorded at build time; per-step idempotency keys (step kind + artifact coordinates); done-vs-conflict distinction                                                                                                | Execution                                                                                                                                                                                                                                                                        | 5 (EXE), 16 (P2)                                                                                                          |
| **E-03** — The tag appeared: replay, satisfaction, conflict            | **execution-only**                                                                                                                                                                                    | "tag at the intended commit + consistent evidence → satisfied-externally: P completes as a no-op with external provenance recorded (ledger-first done-ness: done is a property of the recorded state, not of who executed it). Tag at a **different** commit, or partial/inconsistent evidence → conflict: refuse and escalate; never silently re-plan (that is E-11's separate trap) and never silently proceed." | "satisfied: `1.5.0` (external, adopted). Conflict: none applied."                                                                                                                                                                                                                                | Plan P (targeting `1.5.0` on main); the tag + the commit it points to; optional external evidence (a changelog commit, an npm publish by another actor); idempotency key (plan id + target); consultation order: commit identity, evidence completeness, plan preconditions                                                                                       | Execution (satisfied/conflict as first-class reconciliation outcomes, not exceptions)                                                                                                                                                                                            | 14 (P0 doctrine / P2 params)                                                                                              |
| **E-04** — Policy flips under a stored plan                            | **execution-only**                                                                                                                                                                                    | "if P satisfies D2 → proceed (recorded: revalidated under D2). If not → blocked with the delta (which precondition, which gate, what is missing); nothing consumed."                                                                                                                                                                                                                                               | "proceed: `1.5.0`. Blocked: none — the tag is not placed, nothing partial exists."                                                                                                                                                                                                               | Stored plan P (target `1.5.0`, evidence bundle E1, policy digest D1); amended policy D2; gate states at t1; plan↔policy-version binding making the delta computable                                                                                                                                                                                               | Execution (revalidation, never cached verdicts; blocked plan stays resumable)                                                                                                                                                                                                    | 14 (P0 doctrine / P2 params)                                                                                              |
| **E-05** — Retry must reproduce the same plan (time is an input)       | **partial** (planner: plan purity — enumerated inputs incl. frozen time, plan hash as identity proof; execution: resume + equality proof)                                                             | "release `1.5.0` exactly as planned at T0; the retry is a continuation, not a recomputation."                                                                                                                                                                                                                                                                                                                      | "`1.5.0`; the ledger's plan hash at T2 equals the one at T0 (equality proof recorded)."                                                                                                                                                                                                          | The plan from T0 (notes dated 2026-09-05, digests recorded); wall-clock time at T0 and T2; recorded digests; execution-time recomputation limited to precondition verification, never value recomputation                                                                                                                                                         | P2: **plan-identity** (purity contract: plan = pure function of enumerated inputs — repo state at a commit, config, policy digest, frozen time); execution owns the resume fixture                                                                                               | —                                                                                                                         |
| **E-06** — The foreign tag: resume-by-inference trap                   | **execution-only**                                                                                                                                                                                    | "no automatic continuation. Options presented (adopt the tag as the release, void it and re-tag bare `1.4.2`, abort): attribution (who/what created it) decides, and a human provides it."                                                                                                                                                                                                                         | "adopt: `1.4.2` released (with the tag-name anomaly recorded, and a normalization policy applied — the `v` form is what the kernel's parser rejects, ADR-0001 decision 4). Void: tag deleted, release proceeds as `1.4.2` with the bare tag."                                                    | The plan (target `1.4.2`); the foreign tag `v1.4.2` at `abc123` (the `v`-prefix anomaly itself); absence of a matching ledger record; attribution-beats-observation rule (state adopted only with provenance)                                                                                                                                                     | Execution (blocked-on-foreign-state outcome with human-resolution path)                                                                                                                                                                                                          | —                                                                                                                         |
| **E-07** — Two CI jobs release the same line concurrently              | **execution-only**                                                                                                                                                                                    | "one release `1.5.0`; the loser records `abandoned(follower of R1)`."                                                                                                                                                                                                                                                                                                                                              | "`1.5.0` published once."                                                                                                                                                                                                                                                                        | Both attempts (R1, R2 planning `1.5.0` from `abc123` seconds apart); the shared line; the tag namespace (tag push as CAS); the registry; claim binding (line, next version, attempt id) acquired before any mutation; ownership re-verified after every step (fencing)                                                                                            | Execution                                                                                                                                                                                                                                                                        | 13 (EXE; explicit in body: "parameters are an open fork")                                                                 |
| **E-08** — Two runs compute the same rc number                         | **execution-only**                                                                                                                                                                                    | "two prerelease publications with distinct sequence numbers, ordered by claim acquisition; the second run's changelog covers its own commit only (its base is abc123's publication)."                                                                                                                                                                                                                              | "`1.6.0-rc.1` (abc123) and `1.6.0-rc.2` (def456) — or an explicit conflict for the second if policy serializes rc runs."                                                                                                                                                                         | Both runs (commits `abc123`, `def456`); the shared empty `1.6.0-rc.*` tag set; claim primitive scoped (line + stream + target); bounded retry on CAS loss (loser recomputes max-by-precedence over the stream); injectivity check at write time                                                                                                                   | Execution (allocation protocol; NOTE: the `.1`-first mint here vs `.0`-first in P-02/P-05/P-07 is exactly fork 17 — planner policy over tags, P2)                                                                                                                                | 13 (EXE), 17 (P2, by citation)                                                                                            |
| **E-09** — Human intervention: abort, PR edit, force-push              | **execution-only**                                                                                                                                                                                    | "no completion of the old attempt; a new plan (target `2.0.0`) after explicit operator confirmation; the abort is honored as authoritative."                                                                                                                                                                                                                                                                       | "`1.5.0` unpublished (tag orphaned, recorded); next release `2.0.0` from `def456`."                                                                                                                                                                                                              | The abort; the PR edit (minor → major intent); the force-push `def456`; the ledger; the orphaned tag `1.5.0`; human-action precedence over automation; orphan registry (tag, commit, disposition)                                                                                                                                                                 | Execution (the new plan for `2.0.0` is computed by the planner, but the scenario's decision surface — honoring the abort, dispositioning the orphan — is execution)                                                                                                              | 14 (P0 doctrine / P2 params)                                                                                              |
| **E-10** — Clock and ordering anomalies                                | **partial** (planner: topology-ordered walk/range policy over anomalous history; execution: nightly stamp frozen at a defined moment, never used for ordering)                                        | "releases compute normally over the anomalous history; the nightly's version string embeds the stamp (`1.5.0-nightly.20260905.abc123`-style) but no behavior keys off the stamp's ordering."                                                                                                                                                                                                                       | "per the pending change sets; nightly stamps are labels, not sequence numbers."                                                                                                                                                                                                                  | The anomalous history (child C2 author date 2026-08-20 < parent C1 2026-09-01; committer date correct); the nightly stamping policy; the import with timestamps normalized to 2027-01-01; walk policy (topological, first-parent for release ranges — PL-05c, recorded policy); stamp uniqueness via commit sha                                                   | P2: **inputs+extraction** (topology-ordered range walk — a planner extraction policy); the nightly-stamp half is Execution                                                                                                                                                       | 15 (P3, by citation)                                                                                                      |
| **E-11** — Plan from stale state: hotfix interleave                    | **partial** (planner: re-derivation P2 (target vs new base, new range); execution: staleness validation trigger + executing P2)                                                                       | "re-planned P2: target stays `1.5.0` (its bump is computed against the _new_ base `1.4.2`, and the pending change set justifies the same minor), range `1.4.2..xyz789`; P1 superseded (PL-08's supersession semantics, triggered at execution instead of at PR time)."                                                                                                                                             | "`1.5.0` — but the version is **not** the plan: same target, different content set than P1 planned. (Had the hotfix been `1.5.0`-shaped or the new commits breaking, the target itself would move — E-04/PL-08 mechanics.)"                                                                      | Plan P1 (frozen range `1.4.1..abc123`, target `1.5.0`); new released version `1.4.2` (hotfix interleave); new head `xyz789` (two commits landed); released-version pointer re-read at execution; plan bound to (base version, range, target, change-set hash) — all four re-verified                                                                              | P2: **plan-identity** (base-version freshness + fingerprint available at execute time); execution owns the trigger fixture                                                                                                                                                       | 14 (P0 doctrine / P2 params)                                                                                              |
| **PR-01** — Promote without rebuild: the bytes are the contract        | **execution-only**                                                                                                                                                                                    | "promotion as release event (P-03's mechanics) with a strict no-rebuild contract."                                                                                                                                                                                                                                                                                                                                 | "`1.2.0`; every artifact digest equals the rc.1 digest it was promoted from — digest equality is the promotion's precondition."                                                                                                                                                                  | The rc artifacts (content digests recorded at rc publication); the promotion request; verify-before-bind (fetch rc artifacts, compare digests, then move channels); promotion as a distinct ledger operation type (bind, not build)                                                                                                                               | P3: **promotion**                                                                                                                                                                                                                                                                | —                                                                                                                         |
| **PR-02** — Promote with rebuild: two bodies, one version name         | **execution-only**                                                                                                                                                                                    | "promotion with rebuild, recorded as such (a first-class operation, not a silent deviation from PR-01's default)."                                                                                                                                                                                                                                                                                                 | "`1.2.0`; version name shared by two generations, distinguished by generation id and digest — the version is not the bytes (N6 inverted: this is the model _admitting_ the gap, not hiding it)."                                                                                                 | The rc digests; the rebuild requirements (signing cert rotated, provenance attestation required on stable only); the source commit (rebuild fails loudly if it moved); generation table per release (gen-1 rc bytes, gen-2 stable bytes, `same-source-rebuild-of` provenance)                                                                                     | P3: **promotion** (D9 obligation: invariant 10's final clause — "never publishes new bytes" → "never publishes _unrecorded_ new bytes" — tightens at the execution contract)                                                                                                     | —                                                                                                                         |
| **PR-03** — Stale evidence: promotion must re-prove                    | **execution-only**                                                                                                                                                                                    | "blocked: `evidence-stale → revalidation-required → validation-failed`. Nothing is consumed; the release stays planned."                                                                                                                                                                                                                                                                                           | "none minted; `1.2.0` untagged."                                                                                                                                                                                                                                                                 | Evidence bundle E1 with T0 timestamps; the TTL policy (7 days); the request time (T0 + 9 days); the failed revalidation (dependency advisory landed mid-window); evidence store queryable by (digest, kind, validated-at); covered-digest binding surviving into E-04's revalidation                                                                              | P3: **promotion** (TTL ownership itself = execution phase per §6 item 11)                                                                                                                                                                                                        | 7 (EXE)                                                                                                                   |
| **PR-04** — Backward channel move: rollback as a recorded event        | **execution-only**                                                                                                                                                                                    | "no new release; one channel-mutation event (a), one CAS-guarded binding move (b), one rejected stale command (c)."                                                                                                                                                                                                                                                                                                | "`1.2.0` remains published and fully resolvable — rollback hides, never erases."                                                                                                                                                                                                                 | The rollback request (target channel, from `1.2.0`, to `1.1.9`); the channel event log; the replayed stale command ("move stable to 1.5.2"); current channel states; expected-prior-state CAS on every move; idempotency keys on commands; served-window query                                                                                                    | P3: **channels**                                                                                                                                                                                                                                                                 | 6 (P0 — settled by release-model.md to the default: recorded channel move)                                                |
| **PR-05** — One release, many channels, over time                      | **execution-only**                                                                                                                                                                                    | "one release, two sequential channel memberships; the stable promotion is automatic under the policy but recorded with its trigger evidence."                                                                                                                                                                                                                                                                      | "`1.3.0`; `next` and `stable` both serve it after T1+7d (until something newer ships)."                                                                                                                                                                                                          | The release `1.3.0`; the two channels (`stable`, `next`); the rollout policy (dwell 7 days); the promotion trigger (timer + green health check); membership edges (release, channel, from, to, cause); retraction as recorded event                                                                                                                               | P3: **channels**                                                                                                                                                                                                                                                                 | —                                                                                                                         |

---

### Table 2 — Open forks (17 in the matrix; release-model.md §6's count corrected by this contract) and what Phase 2 owes

| #   | Subject (one line)                                                                                                                                 | Resolver phase per release-model.md §6 (verbatim basis)                                                                                                                                                                                                                                  | Phase 2 contract: resolve now or carry?                                                                                                                                                                                     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Chore/docs/ci-only history → recorded no-op vs a "chore release" policy (S-01, PL-06)                                                              | §6 item 5: "Fork 1 (chore-only history: recorded no-op versus a chore-triggered release) is carried by the same Phase 2 default-policy design"                                                                                                                                           | **Must resolve now** (default-policy design)                                                                                                                                                                                |
| 2   | Bootstrap default → `1.0.0` vs `0.1.0`, and where the bootstrap decision is recorded (S-02)                                                        | §6 item 5: "Resolved by: default-policy design in Phase 2 — S-02's demand for an explicit recorded decision is not negotiable away"                                                                                                                                                      | **Must resolve now**                                                                                                                                                                                                        |
| 3   | Stream restart on target move → keep identifier + reset sequence (`2.0.0-rc.0`, matrix default) vs restart ladder at `alpha` (P-05)                | §6 item 6: "Resolved by: the line-policy schema in Phase 2 (P-05, P-02, M-08)"                                                                                                                                                                                                           | **Must resolve now** — but see Conflict C2 (assignment's taxonomy lists line-policy under Phase 3)                                                                                                                          |
| 4   | Fixed ladder vs open identifiers — is `alpha → beta → rc` the only legal progression? (P-02, M-08)                                                 | §6 item 6: same "line-policy schema in Phase 2"                                                                                                                                                                                                                                          | **Must resolve now** (same Conflict C2)                                                                                                                                                                                     |
| 5   | Half-published recovery doctrine → complete-in-place vs void-and-skip past the tag boundary (E-01, E-02, AR-05)                                    | §6 item 3: "Resolved by: the execution phase against E-01, E-02, AR-05"                                                                                                                                                                                                                  | **May carry** (execution-phase owned; P2 records only the decision-record shape)                                                                                                                                            |
| 6   | Rollback posture → channel-move rollback only, or first-class "re-release" (PR-04)                                                                 | §6 item 5: "resolved to its default here — rollback is a recorded channel move that hides, never erases (PR-04); a first-class re-release, if ever wanted, is a new release through the same planner"                                                                                    | **Settled (P0)** — carry as fixed default; nothing left for Phase 2                                                                                                                                                         |
| 7   | Evidence TTL ownership → policy file vs gate config; per-kind TTLs (PR-03)                                                                         | §6 item 11: "Resolved by: the evidence-bundle design in the execution phase (PR-03)"                                                                                                                                                                                                     | **May carry** (execution-phase owned)                                                                                                                                                                                       |
| 8   | Change-id marker convention → cherry-pick trailer vs footer change-id vs both (M-03, M-05; PL-04 adjacent)                                         | §6 item 4: "Resolved by: the Change value's provenance parsing in Phase 1's adapter work — under the invariant that no fuzzy matching is ever admitted (M-05)"                                                                                                                           | **May carry fork proper** (P1-owned); **must** name the PL-04 bot-marker rule now (D9 carried obligation: "fork 8 adjacent")                                                                                                |
| 9   | Range-cursor deferral → cursor invariant confirmed; open part is the discard protocol (who may drop a withheld change) (PL-06, PL-07, M-10)        | §6 item 10: "Resolved by: Phase 2's withholding records (PL-07, M-10)"                                                                                                                                                                                                                   | **Must resolve now**                                                                                                                                                                                                        |
| 10  | Breaking-on-non-releasing-type → force major bump (recommended) vs hard-error demanding retype (PL-05b)                                            | §6 item 9: "Resolved by: planning policy in Phase 2 (PL-05b, PL-02)"                                                                                                                                                                                                                     | **Must resolve now**                                                                                                                                                                                                        |
| 11  | Per-package tag naming → `lib-a-1.2.1` vs `lib-a/v1.2.1` vs other, under the kernel's bare-semver parse (PL-01, M-11)                              | §6 item 8: "Resolved by: the scheduling decision that activates PL-01..PL-05 and M-11's tag-namespace rule" (phase unnamed; monorepo stamped hypothetical-future)                                                                                                                        | **May carry** — but M-11's global tag-namespace precondition is not monorepo-gated; see Conflict C4                                                                                                                         |
| 12  | Dependent bumps on compatible ranges → always / never / only-when-lockfile-changes (PL-02 case 1)                                                  | §6 item 9: "Resolved by: planning policy in Phase 2 (PL-05b, PL-02)"                                                                                                                                                                                                                     | **Must resolve now** (with the propagation fixture)                                                                                                                                                                         |
| 13  | Claim mechanism parameters → git-tag CAS vs lease+fencing vs lock service; retry bounds (E-07, E-08)                                               | §6 item 1: "Resolved by: the execution phase's claim design, replayed against E-07/E-08"                                                                                                                                                                                                 | **May carry** (execution-phase owned)                                                                                                                                                                                       |
| 14  | One stale-plan policy or several → single invalidation framework with per-cause parameters vs distinct paths (P-05, PL-08, E-03, E-04, E-09, E-11) | §6 item 5: "resolved to invariant 5's single rule: supersession is one recorded relation, whatever invalidated the predecessor"                                                                                                                                                          | **Doctrine settled (P0)**; Phase 2 **must** implement the single-relation rule and **may** parameterize detection per cause (change set/base/target/policy) — the fingerprint input list is a contract decision, not a fork |
| 15  | Nightly as line-release vs artifact class → does a nightly allocate on the line, or purely channel-scoped build? (AR-04, E-10)                     | §6 item 7: "Resolved by: the scheduling of channels and artifacts (AR-04, E-10)" (phase unnamed → P3 per the assignment's taxonomy)                                                                                                                                                      | **May carry** — but AR-04's quoted ruling ("no version allocation on the line … computed, not minted") is already a golden expectation; see Conflict C6                                                                     |
| 16  | Ledger/decision-record storage → repo files / notes refs / external store; persistence vs recomputation (E-01, E-02, S-01)                         | §6 item 2: "Resolved by: Phase 2's decision records needing a home — the constraint that it stays outside `core/domain/` (ADR-0001) is already fixed"                                                                                                                                    | **Must resolve now** (home only; stays outside `core/domain/`)                                                                                                                                                              |
| 17  | First mint of a fresh sequence → `.0` (P-02/P-05/P-07; kernel's recorded seed) vs `.1` (E-08, P-01's head-at-9, M-08's first-ever `rc.1`)          | **Not in §6** (added after §6 was written). Per fork 17's own text + D9: "the kernel seeds at `.0` and a `.1`-first convention, if adopted, is planner policy over tags (recorded by decision-log D9)"; D9: "fork 17 (.0 vs .1 first mint) is recorded in the matrix, kernel keeps `.0`" | **Must resolve now** (planner policy over tags; every first-mint fixture S-02/P-02/P-05/P-07/M-08/E-08 depends on it)                                                                                                       |

---

- **C1 — Fork-count drift.** The assignment and `release-model.md` §6 both say _sixteen_ open forks; `release-scenarios.md` now lists **17** (fork 17 added via decision-log D9, 2026-09-06). §6's intro sentence ("The matrix's sixteen open forks are the master list") predates fork 17. The contract (ADR-0003) should cite 17 and note §6 as slightly stale.
- **C2 — line-policy phase placement.** §6 items 5–6 resolve forks 1, 2, 3, 4 via "the default-policy design in Phase 2" and "the line-policy schema in Phase 2" — but the assignment's task taxonomy lists `line-policy` (and `streams`) as **Phase 3** tasks. Either the contract moves the line-policy schema into Phase 2, or it re-parents those fork resolutions to Phase 3 and Phase 2 fixtures for P-02/P-05/M-08 carry the default as a fixed input. M-08's own body ("that operation is line policy per ADR-0001's deferred seam; this scenario is its first consumer") pins the seam's _consumer_ in the planner regardless.
- **C3 — Multi-line split.** The taxonomy puts `multi-line` in Phase 3, but per-line independence is already a Phase 2 design constraint: S-05 ("a single global 'next version' pointer now computes either `2.0.1` or `1.9.6` — wrong for one line each time"), M-01 (fail-closed ancestry attribution — a D9 carried obligation demanding an invariant home in the Phase 2 contract), and M-02's failure mode are planner-core. The contract should state explicitly which multi-line mechanics Phase 2 proves (per-line evaluation, attribution, releasedness predicate) vs what Phase 3 adds (line registry/feeds/discovery, one-pass orchestration).
- **C4 — M-11's global tag namespace vs fork 11's deferral.** Fork 11's resolver is the package-axis scheduling decision (hypothetical-future), but M-11's collision check ("a plan verifies its target tag does not exist at any commit before execution"; global tag→commit map alongside per-line version history) is not monorepo-gated. Phase 2 should own the global-namespace precondition even while per-package tag _naming_ stays carried.
- **C5 — `.0` vs `.1` first mint inside the golden expectations.** The matrix's verbatim expectations disagree at the convention level: P-02/P-05/P-07 mint `beta.0`/`rc.0`/`rc.0` while E-08 expects `1.6.0-rc.1` "from the same empty tag set" and M-08 expects first-ever `2.4.0-rc.1`. Fork 17 exists for exactly this; D9 records the kernel at `.0` with the `.1`-first option as planner policy over tags. Golden fixtures cannot be written for both readings until the Phase 2 contract resolves fork 17 (possibly as "advance-style vs first-mint" distinction — that framing is the fork's to settle, not assumed here).
- **C6 — Fork 15 vs AR-04's quoted ruling.** AR-04's Expected release decision already fixes "nightly: a scheduled build, not a release (no version allocation on the line — the base `1.5.0` is computed, not minted)", while fork 15 is formally open and §6 assigns its resolution to unnamed channel/artifact scheduling. If Phase 3 later resolves fork 15 the other way, the AR-04 golden fixture breaks; the contract should either pin AR-04's ruling as the default or mark that fixture conditional.
- **C7 — S-03 title drift.** The scenario index lists S-03 as "Manifest drift behind maintenance tags"; the body heading is "Manifest drift: the hotfixes that never came home". Table 1 uses the body heading. Cosmetic, but golden-fixture naming should pick one.
- **C8 — D9 carried obligations owed to this contract** (so the planner contract does not miss them): (1) empty groups are decision records, never mints (S-01/PL-06; `Bump.max`'s neutral `"patch"` never mints); (2) the prerelease-pointer convention is Phase 2 line policy — including P-02's ladder-override branch, which publishes a version sorting below the pointer and therefore cannot move it under the kernel's highest-by-precedence `released` verdict; (3) M-01 needs an invariant home (extend invariant 4's stress list or invariant 6's sentence); (4) PL-04's self-reference exclusion needs a named rule; (5) plan supersession may terminate in a decision record (mixed-kind endpoints under invariant 5); (6) invariant 10's "never publishes new bytes" tightens to "never publishes _unrecorded_ new bytes" at the execution contract (PR-02).
- **C9 — Task-name provenance.** The eight Phase 2 task names are the assignment's vocabulary; no repo document defines them yet. This inventory maps scenarios to them by best fit of each scenario's distinguishing mechanics; the Phase 2 contract should publish the task definitions so fixture ownership is auditable.

### Sources consulted

- `docs/design/release-scenarios.md` — read in full (lines 1–2269): canonical vocabulary, all 53 scenario bodies (verbatim field extraction cross-checked by targeted line-anchor reads of every `Expected release decision` / `Expected versions` field), stress analysis, "Open forks this matrix deliberately retains" (17 forks, read raw at lines 2152–2240).
- `docs/design/release-model.md` — §Unresolved questions read raw (lines 506–554) for fork→phase resolution; section headers enumerated; §7 compatibility-boundary rows and invariant citations grepped for cross-reference.
- `docs/design/decision-log.md` — D4, D5, D7, D8, D9 (raw, lines 40–90) incl. "Carried obligations (from the Phase 1 adversarial review, D9)" for the Phase 2 opening checklist.
- Referenced but not read (vocabulary lock taken from the scenarios doc's restatement): `docs/design/release-taxonomy.md`, `docs/adr/0002-release-model-and-domain-vocabulary.md`, `docs/adr/0001-domain-kernel-and-semantic-version.md`.

## 6. Open questions — resolved rulings for this phase

The audits' conflict registers, resolved (decision-log D10–D14 carry the
trail; ADR-0003 the decisions):

1. **Fork-count drift (C1).** The matrix retains **seventeen** open forks
   (fork 17 recorded by D9); release-model.md §6's "sixteen" is corrected in
   this PR.
2. **Line-policy phase placement (C2).** The line-policy **schema** is Phase 2
   contract data (§2.6, §2.8 — lines are input either declared or
   default-derived, one schema, no code path keyed on "simple mode"); feeds,
   discovery, lifecycle, and the registry surface are Phase 3 (PR-6). The
   P-02/P-05/M-08 fixtures may land with Phase 3's stream workstream using
   this schema.
3. **Multi-line split (C3).** Phase 2 proves per-line evaluation, ancestry
   attribution, and the per-line releasedness predicate (S-05, M-01, M-02 are
   planner-core). Phase 3 adds the line registry/feeds/discovery and one-pass
   multi-line orchestration.
4. **Global tag namespace (C4).** The target-tag-must-not-exist precondition
   (M-11) is Phase 2-owned and not monorepo-gated; per-package tag naming
   stays declared input configuration (fork 11's naming knob carried).
5. **Fork 17 — first-mint seed (C5, IC8).** Resolved as **declared seed
   policy**: a fresh stream key's first sequence is `seed: .0` (kernel
   default) or `seed: .1` by declaration. The matrix text stays verbatim —
   P-01/P-02/P-05/P-07 take the default; M-08/E-08 fixtures declare `.1`.
   The fork entry's own recorded shape ("a `.1`-first convention, if adopted,
   is planner policy over tags") is formalized, not amended.
6. **Nightly / fork 15 vs AR-04 (C6).** AR-04's ruling is pinned as the
   default: nightly is a scheduled artifact-class build, no line allocation.
   Fork 15 stays carried; resolving it line-side later triggers the
   three-surface amendment named by the D9 audit (vocabulary table, kernel
   value shape, invariant 8), loudly.
7. **S-03 title drift (C7).** The canonical name is the body heading
   ("Manifest drift: the hotfixes that never came home"); the index line is
   corrected in this PR.
8. **D9 obligations (C8).** All nine mapped — §3.
9. **Task-name provenance (C9).** The Phase 2 workstream definitions are
   published in §5; fixture ownership is auditable against them.
10. **Persistence (fork 16, IC1).** Persistence is explicitly adjacent: Phase 2
    ships the pure planner and defines persistable shapes (§2.16); the thin
    package-layer writer is execution-phase work.
11. **Fork 8 routing (IC2).** The marker convention lands with Phase 2
    extraction (§2.3); Phase 1 shipped no adapter.
12. **Forks 1–2 — bootstrap and chore-only defaults (IC6).** The planner
    refuses to choose a bootstrap version: S-02's recorded operator decision
    is demanded as input, never defaulted (`1.0.0` vs `0.1.0` is the
    operator's call). Chore-only runways produce the recorded no-op (S-01,
    fork 1).
13. **Forks 10/12 vs package-axis gating (IC7).** Bump-resolution core (type
    →bump mapping, `Bump.max`, breaking dominance, pre-1.0 dampening) is
    Phase 2 unconditionally; the propagation policy lands with the active
    package axis (PL-01..PL-03 are Phase 2 mandates); per-package tag naming
    is declared configuration.
14. **The PR-5 adversarial-review wave (C10).** Two fresh independent
    reviews of the assembled planner produced the resolution set recorded
    as decision-log **D17** and ADR-0003 decision 18: bootstrap-authoritative
    birth targets (S-02), the in-flight-target rule over a prerelease-held
    pointer (P-04 equal-precedence keeps the target and its sequence; P-05
    moves and resets), stable co-mint suppression under a prerelease intent
    (P-07/M-08 publish stream-only), the P-03 promotion vocabulary
    (`release` with `bump: null` and an inherited empty change set; pending
    → operator-contradiction refusal), the S-01 forced record (no mint),
    plan-level explanation data (§2.11/§2.12/§2.13 — excluded is not
    invisible), the inputsFingerprint policy-relevant projection (PL-08:
    policy-ignored commits never invalidate a stored plan), and the door's
    release-mapping posture: the declared component graph stays the active
    §2.15 input (D16 stands — multi-component worlds are not refused); what
    the door refuses is fabricating the line↔component release mapping
    (PL-01's declared-future seam), and M-08's rejection half plus its
    two-release single pass ride PR-6/PR-7 with the line-policy knobs and
    the binding (ruling 2's phase placement, applied).

## 7. Handoff to execution — recorded, not owned

Restated so the execution phase cannot miss it (D9 obligation 6): invariant
10's final clause — "never publishes new bytes under an existing version
identity" — tightens at the execution contract to "never publishes
_unrecorded_ new bytes"; PR-02's deliberate rebuild, recorded as a new
artifact generation, is the sanctioned escape the invariant's own generation
clause already implies. The planner owns no part of that amendment.
