# Phase decision log — the orchestrator's working record

This file is the coordination log for the phased construction of the release
engine ([#13](https://github.com/ecoma-io/release-craft/issues/13)). It records
the state of architectural thinking as it moves: the current hypothesis, what is
settled, what is deliberately still open, what was rejected, and what each
workstream depends on. Decisions that survive review are promoted into ADRs;
this log keeps the trail that led there — including the trails that dead-ended.

Rules for this file:

- One row per decision, with the evidence that justified it.
- Nothing is deleted when it is rejected — it moves to "Rejected alternatives"
  with the reason.
- Vocabulary changes are never silent: any term redefined here must be reflected
  in the scenario matrix before Phase 1 implements it.

## Current architecture hypothesis

**Decided (2026-09-05, decision D4).** The release model is
**Model C′ = Model C + amendments A1–A7** — a release is the stateful
execution of a _Release Plan_ on a _release line_, with planning and execution
as separate domains. The full definition, vocabulary lock, invariants, and
complexity budget are recorded in `release-model.md` and ADR-0002, which
survived the adversarial design review; the decision and its evidence are
recorded under D4 below.

Candidate mental models that were evaluated:

- **Model A** — a release is a version-on-branch concept. **Rejected.**
- **Model B** — a release is a linear pipeline. **Rejected.**
- **Model C** — a release is the stateful execution of a _Release Plan_.
  **Survives with amendments → adopted as C′.**

## Decisions accepted

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Consequence                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| D0  | Phase 0 ships research/design documents only; no production release-engine code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Issue #13 scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | The kernel's population stays `Version` until Phase 1.                                                                               |
| D1  | Work proceeds through PRs per phase (Phase 0 = PR-A), main stays protected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Repository rulesets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Serial phase gates; no direct pushes.                                                                                                |
| D2  | Prose carries no numeric inventory claims it cannot keep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Audit §3.7 — the stale-count class; ADR-0001's "75 tests" was verified false (vitest: 74).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Counts live in the owning artifact; every PR that changes a counted thing sweeps the audit's §3.7 table.                             |
| D3  | Any new source root changes `SOURCES` in the same PR.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Audit §3.5 — a root outside the two globs escapes every coverage threshold silently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Recorded as a Phase 1/2 checklist item; candidate hardening deferred with evidence.                                                  |
| D4  | The release model is **Model C′ = Model C + amendments A1–A7** (Model C as defined in `_proposals/model-c.md`, amended by its own A1–A7). Settles U1, U2, U3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Three independent evaluations against the 53-scenario matrix: A rejected (25 FAILS over 6 classes — PR-01..05, E-01/02/03/06, AR-01..06, P-02/05/06, M-03/10/11; amended to competency it is no longer A), B rejected (15 FAILS over 5 classes; required amendments delete defining properties P1/P2 — worst outcome E-09, completing an aborted release, is P2's default), C survives (zero killers; the matrix's two hardest stress families — attempt+ledger+claims, plan-as-first-class-object — are native structure). Triangulation: the A and B evaluators' own proposed alternatives independently converge on C's skeleton. | `release-model.md` + ADR-0002 (synthesis); Phase 1 primitives = the vocabulary's kernel-value kinds; Phase 2 planner consumes plans. |
| D5  | The planning engine lives in the package layer (`src/`), consuming kernel values — audit route 1. Settles U5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Audit §3.4 (zero structural cost, `type-package` row already allows it) + D4: planning is pure but consumes repository observations as data — package concern; the kernel keeps only value semantics.                                                                                                                                                                                                                                                                                                                                                                                                                                | Phase 2 planner lands in `src/`; no new source root unless justified, and then D3 applies.                                           |
| D6  | The kernel entrypoint becomes a barrel (`core/domain/index.ts`) once Phase 1 adds its second primitive; the same PR amends ADR-0001 decision 8 (barrel) **and** annotates decision 7's rationale ("the kernel does not know channels exist") and the consequences' parenthetical on lines/channels/transitions outside `core/domain/` — which govern policy resolution, not the locked value shapes (adversarial review MAJOR-3). Settles U7.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Audit §3.1 friction table — N primitives × 3 tool declarations each is friction without benefit; one barrel keeps the alias seam at one specifier. ADR-0001 read literally would file `Channel`/`ReleaseLine` values in `core/domain/` as a violation; the amendment reconciles values-in/policy-out in the winning document itself.                                                                                                                                                                                                                                                                                                 | PR-B carries all three ADR-0001 amendments; all three declarations still name one entrypoint.                                        |
| D7  | Phase 1 builds exactly the vocabulary's kernel-value kinds: `Change`, `ChangeSet`, `ReleaseLine`, `Channel`, `Artifact` beside `Version`. Settles U4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `release-model.md` §3 vocabulary table (kind column) + ADR-0002 consequences — ReleasePlan is a planning concept (Phase 2), Release/Promotion/Transition are execution concepts, Hook is a deferred adapter.                                                                                                                                                                                                                                                                                                                                                                                                                         | Phase 1 contracts use the locked terms verbatim; the barrel (D6) lands with the first new primitive.                                 |
| D8  | The release-please compatibility boundary is the synthesis's committed table. Settles U6.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `release-model.md` §7 — 18 rows distilled from the baseline's 40-row classification; ADR-0002 §7 records the same boundary in decision register.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | "Preserve" means behavior survives with release-craft's own mechanisms, not config migration; verified per-behavior in later phases. |
| D9  | Phase 1's kernel semantics survive the adversarial review (2026-09-06) with two in-file fixes: `advanceStream` refuses build-carrying targets (a suffix composed after the build part is not a prerelease to the grammar — the sequence would stop ordering by precedence, invariant 8's Phase 1 claim), and the provider-isolation matcher closes camelCase/separator splits (`prNumber`) and walks prototype accessors. Verdicts: `released` is highest-by-precedence and may hold a prerelease (M-08), agnostic on whether policy moves it on a prerelease publication; `empty()`'s `"patch"` is `Bump.max`'s neutral element, never a minted version (S-01/PL-06); lifecycle and stream arithmetic are value semantics with policy outside; fork 17 (.0 vs .1 first mint) is recorded in the matrix, kernel keeps `.0`; release-model wording amended (invariant 4 vs PL-01, invariant 6's global pointer vs the global tag namespace, §3's line "and a policy"); ADR-0001's single-value clauses annotated. | Five independent adversarial reviewers (one broad kernel review + one per question) over `release-model.md`, all 53 scenario bodies, ADR-0002, the five values, and every suite; the coordinator re-verified each finding against the sources directly. 15/15 invariants consistent with the matrix; no Phase 0 decision reversed — the two model-level findings (fork 17, the wording clashes) are documentation amendments, not decision changes.                                                                                                                                                                                  | The carried obligations below are Phase 2's opening checklist; nothing blocks the planner.                                           |
| D10 | The planner owns the prerelease-pointer convention: the pointer is a projected convenience recomputed from history, moved only by a release of higher precedence (M-08); it stands otherwise (P-02, P-07); an override sorting below the pointer is expressible only as a recorded operator decision input — the pointer stands, the override is explicit, the plan records both. Invariant 6's stress list gains M-08, P-02, P-07 (ADR-0003 decision 8).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D11 | The planner's self-reference rule: commits carrying the reserved `Release-Craft:` trailer namespace are excluded deterministically **before** classification (PL-04), surfaced — never invisible; a malformed marker in the namespace fails toward explicit review. Invariant 3's stress annotation names the rule; fork 8's marker convention lands with Phase 2 extraction (`Change-Id:` footer → cherry-pick origin → commit sha, lineage recorded, conflicts surfaced). (ADR-0003 decision 11; contract §2.3, §2.12.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D12 | Plan identity is the content fingerprint: canonical JSON (recursively key-sorted) of the plan's semantic fields → SHA-256 → `plan_sha256:<hex>`; the fingerprint input tuple is closed and enumerated in the contract (§2.11). Supersession is legalized over mixed-kind endpoints — a plan may be superseded by a later plan or closed by a decision record (PL-06) — so invariant 5's sentence and the vocabulary table's `ReleasePlan` cell are amended in the same PR. (ADR-0003 decisions 9–10.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D13 | Fork 17 is resolved as **declared seed policy** without touching the matrix: a fresh stream key's first sequence is `seed: .0` (the kernel default, matching `advanceStream`) or `seed: .1` by explicit declaration; P-01/P-02/P-05/P-07 fixtures take the default, M-08/E-08 fixtures declare `.1`. The fork entry's recorded shape is formalized, not amended (ADR-0003 decision 7; contract §2.8, §6 ruling 5).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D14 | Phase-boundary rulings from the audits: the line-policy **schema** is Phase 2 data while feeds/discovery/lifecycle/registry are Phase 3; Phase 2 proves per-line evaluation, ancestry attribution, and the releasedness predicate while the line registry and multi-line orchestration are Phase 3; the global tag-namespace precondition (M-11) is Phase 2-owned; persistence is adjacent (fork 16); the package axis is active for declared component metadata with per-package tag naming as declared configuration (forks 11/12); bootstrap is a refusal, not a default (forks 1–2); the nightly artifact class stays carried with AR-04's no-line-allocation ruling pinned (fork 15); S-03's canonical title is its body heading and release-model §6's fork count is corrected to seventeen. (ADR-0003 decisions 2, 12–13, 15; contract §6.)                                                                                                                                                               |
| D15 | A release line's tag-admission namespace is the declared `LineConfig.versionBand` — major, optionally minor (§2.13; S-03, M-07): band equality admits a tag into the line's history, an unparseable or out-of-band tag is foreign and surfaced (E-06); absence admits every admissible tag (single-line repos). No grammar is inferred from line ids or branch names; the band is configuration data completed in PR-3 (ADR-0003 decision 16).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

| D16 | The component axis's propagation input is declared dependency metadata on `ComponentMeta` (`dependencies?: readonly ComponentDependency[]`, §2.15; PL-01–PL-03): the package graph arrives as closed input read from manifests — never discovered from the filesystem (invariant 2). Range math (`^`/`~`/exact) decides range-widening; dependents widen transitively in topological order; negative evidence (`no-reverse-dependency`, `range-compatible`) is a first-class plan output (PL-03). A dependency naming an undeclared component is a caller contract violation (ADR-0003 decision 17). |

| D17 | PR-5's fresh adversarial review (two independent reviewers) and its resolutions: (1) The first release of a line targets the recorded bootstrap version verbatim when `bootstrap` names one (S-02: "either is valid, silently derived is not") — the decision's bump still records the change class. (2) While a prerelease holds a line's pointer, the in-flight-target rule governs (P-04/P-05): the stable candidate recomputed from the line's stable base and the in-flight target (`bumpPatch` of the pointer) — the higher by precedence wins; at or under the in-flight class the target and its sequence stand (1.2.0-rc.2), a heavier joining change moves the target and resets the sequence (2.0.0-rc.0). (3) A `prerelease` intent on a line suppresses the stable co-mint for that run (P-04/P-05/P-07/M-08: the stream is the publication; the would-be stable is the streams' target). (4) A `promote` intent (P-03) over an in-flight prerelease is a `release` decision with an inherited (empty) change set and `bump: null`, targeting the pointed-at release; with release-worthy pending changes it is an operator-contradiction refusal — never a silent absorb. (5) A `release-anyway` intent over a quiet line is a `forced` record (S-01: "recorded as operator-forced, never as a routine release") — the forced mint is declared-policy territory. (6) Explanation data (foreign tags, identity conflicts, excluded commits) surfaces on the plan (`ReleasePlan.explanation`) — excluded is not invisible (§2.12/§2.13/E-06). (7) `inputsFingerprint` covers the input's policy-relevant projection (the extracted release-triggering change set, refs, tags, lines, components, bootstrap, intents, policy digest): policy-ignored commits do not invalidate a stored plan (PL-08). (8) The declared component graph stays the active §2.15 input per D16 — multi-component worlds are not refused. What the door refuses is fabricating the line↔component release mapping (PL-01's declared-future seam): with releases pending and the mapping ambiguous — more than one declared component, or more releasing lines than the one declared component can carry — assembling a `ReleasePlan` would invent releases (an unexecutable range-widening edge pins this failure), so that input is a caller contract violation naming the binding gap. Zero-release plans propagate honestly over an empty release list (negative evidence per declared component); per-line decisions, targets and streams stay computable at their layers; the direct `planPropagation` contract is unchanged. Where a releasing line mints several streams, its release entry carries the highest stream target by precedence — the same determinism rule as the line's pointer (§2.13); the remaining mints stay planned in full, and the entry version becomes observable with the binding (PR-6/PR-7). M-08's rejection half (per-line prerelease allowance) and its two-release single pass are PR-6/PR-7 deliverables with the line-policy knobs and the binding. |

| D18 | Phase 3's line-policy slice (issue #23, ADR-0004): (1) per-line stream policy `LineConfig.streams` — `allow` (`"all"` default posture / `"none"` stable-only / declared identifier list; opaque identifiers legal by declaration, fork 4; the ladder stays global promotion-slice data, fork 3) and `seed` overriding `policy.prereleaseSeed` per line. `allow: "none"` turns a `prerelease` intent into a plan-recorded refusal (`ReleasePlan.refusedIntents`, a fingerprinted tuple member) — M-08's rejection half: recorded, never a stable fallback; the rest of the plan is unaffected. (2) Line lifecycle gains planning semantics: `frozen`/`retired` lines refuse release-shaped planning with `line-frozen`/`line-retired` causes — no targets, no streams, no release entry. (3) `LineConfig.withhold` (PL-07): matching changes are deferred — the release range pins below the earliest withheld commit so the un-released span keeps them; the withheld set is enumerated in the plan explanation; nothing else release-worthy yields the existing `withheld` decision (`policy-filter`). (4) `LineConfig.publishes` names the declared component a line's releases bind to — the executable completion of D17(8)'s posture: the door's anti-fabrication refusal (probed: the M-08 two-line/one-component world throws naming the mapping gap) dissolves exactly when every releasing line declares its binding; undeclared or ambiguous mappings still refuse. `planPropagation`'s signature and semantics are unchanged. |
| D19 | Phase 3's close-out (issue #25): the adversarial pass and the 53-scenario reconciliation. (1) M-11 gained its plan-level gate — the tag namespace is global (invariant 6), so two lines minting the same tag in one pass is a self-conflicting plan (each line's tag-absent precondition falsifying the other's); the door refuses before assembly with `version-collision` on `AttributionRefusal` (the shape already carries the door's plan-level refusals), naming the tag, both lines, and both head commits; per-line `tagFormats` keep split-namespace worlds legal (pinned both ways in `test/planner/version-collision.test.ts`; probe-discovered defect — two components with the same next version previously planned with colliding tags). (2) Probe-resolved silences pinned as goldens: an inert withhold rule is a no-op (`withheld: []`, the release proceeds); `bootstrap` × withhold composes (birth mints from the bootstrap commit, other demands defer as recorded refusals with `explanation.withheld` entries); duplicate prerelease intents are idempotent (one `refusedIntents` record covers single and duplicate demand); a `prerelease` intent without `lineId` is the input-contract door refusal (`intents[N].lineId`), so "global stream demand" attacks are unreachable by construction. (3) Seed/sequence isolation is per line: the same stream identifier on two lines keys the seed (`"1"` declared vs the `"0"` default) and the sequence counter per line — `rc.1` and `rc.0` mint independently. (4) The supersession linkage (PL-08/E-11/P-05's supersedes half) stays execution-side: the pure door always emits `supersedes: null` (D14, §6) — regeneration is a new planId at the same target, and the P1→P2 edge is the persistence layer's adjacent concern; the door's whole surface is the P2 shape re-pin. (5) AR-04's nightly ruling is pinned structurally: the closed §2.1 input cannot express a schedule, so "no version allocation on the line" holds by construction — no nightly fixture is built (the `1.2.4-nightly.0` literal in the suite is fork 4's opaque identifier, a different surface). (6) AR-02's artifact-dependency-DAG fragment is deferred to the execution phase with a sweep note: declaring artifact order in the plan needs the artifact-coordinate vocabulary execution owns (§2.16 keeps coordinates opaque; `PlanLine.artifacts` carries declared labels only) — a provider-shaped precondition now would breach the closed posture. (7) No channels amendment: the promotion/channels slice is contract-contradicted (Table 1 rows 491–495 rule PR-01..05 execution-only; §2.1's input list is closed; §2.16's opaque-label posture already implemented via `PlanLine.artifacts`) — the gate stays with the execution phase. The full 53-scenario inventory and gap reconciliation live in `docs/design/matrix-coverage.md`. |
| D20 | Phase 4's execution contract (issue #27, ADR-0005, `phase4-execution-contract.md`): execution lives in `src/execution/` as the planner's sibling consumer in the package layer (ADR-0002 decision 2; no new Moon project or boundary row). Attempt identity is `attempt_sha256:<hex>` over canonical `{ planId, ordinal }`, the ordinal allocated atomically by an attempt register port — one plan, two attempts is the recorded retry shape (E-05). The attempt state machine is `planned → executing → { published | satisfied-externally | failed | superseded | abandoned }` plus `blocked(cause)` suspension, state names quoted from the matrix's own scenario bodies; terminal is terminal, and `abandoned`/`superseded` have no outgoing edge — human aborts never auto-recover (E-09). Claim scopes are a closed three-variant set (`stable-version { lineId, version }`, `prerelease-sequence { lineId, target, streamId, sequence }`, `release-line { lineId }`) carried with store-allocated opaque claim tokens. Fork 13 is resolved **at the domain level**: the claim–verify–write protocol over a claim store port, the store's atomic accept as the deterministic collision adjudication (no wall-clock, no arrival order), idempotent same-holder re-acquisition, and bounded `maxRetries` sequence retry ending in an explicit conflict record (E-07, E-08) — the physical tag-CAS primitive stays open for the Phase 8 adapter ADR. Steps are `(attemptId, stepKey)` over the closed canonical eight-stage sequence `plan → claim → prepare → validate → commit → tag → publish → verify`, `tag` the no-return boundary (E-01); hooks and artifacts extend it by their own ADRs. Step states are E-02's `pending | started | completed | failed`; every change is an append-only transition record carrying guards, claim token, `{ attemptId, actor }` attribution, an opaque evidence ref, and a caller-supplied `recordedAt` that is metadata, never ordering truth (E-10). Replay classification is pure with exactly seven outcomes — `advance`, `noop`, `satisfied-externally`, `conflict`, `claim-lost`, `blocked`, `refused` — contract violations throw, runtime races record (the planner's split). No-mutation-before-claim is guard-table enforcement, invariant 11's executable home. `src/execution/` is clock-free, randomness-free, environment-free (isolation gate executable from day one). |

| D21 | Phase 5's ledger contract (issue #30, ADR-0006, `phase5-ledger-contract.md`): the ledger lives in `src/execution/` as an `ExecutionLedger` port plus the in-memory `MemoryLedger` reference — keyed by `(attemptId, stepKey)`, append-only, deep-frozen, write-ahead at step granularity; fork 16 stays open, the persistence binding is Phase 8's. Resume is pure classification over the ledger tail (`resume(from)` / `complete` / `stale` / `escalate`), never recomputation; the equality proof compares the attempt's carried `planFingerprint` against the recorded first record. Crash classification follows E-01's doctrine (complete-in-place / escalate with the human decision as a recorded value / void-and-skip as the recorded fallback). Adoption is attribution-gated with `adopted-from:<attemptId>` absorption records; bare existence never adopts; the disposition registry owns orphans and never auto-resolves. `verifyEvidence` is pure (verified / conflict / unverified); the content fingerprint canonicalization is `content_sha256:<hex>` over canonical JSON — ADR-0005 decision 8's deferral discharged. The blocked loop closes through `resolveBlocked` resolution records (E-04). The ledger owns the record-path replay door: a terminal-attempt step request through the ledger surface records `refused`, where the kernel's path throws. |

| D22 | Phase 6's hooks-as-steps contract (issue #33, ADR-0007, `phase6-hooks-contract.md`): the hook lives in `src/execution/` as a declared value — an id, an anchor (before/after one canonical stage), declared preconditions (claim requirements, evaluated as the kernel's guards and recorded on the start record) and postconditions (recorded proofs — non-empty evidence or a content fingerprint — checked before the completion record) — with the effect always caller-injected; the engine never stores or invents user code and executes nothing but that seam. `StepKey` widens to `StageKey | HookStepKey` (`hook:<id>`); the effective step list interleaves hooks at their anchors in declaration order; `ReleaseAttempt` gains an optional frozen `hooks` field — excluded from `attemptIdentity` (`attempt_sha256` over `{planId, ordinal}`), the plan value and fingerprint untouched. Hook steps obey the ledger discipline: write-ahead start, never re-executed, replay via classification — kill-anywhere extends over hook boundaries and resume may continue at a hook step. Plan-mutating hooks are refused by construction: the observation type offers no plan-shaped field and the engine exposes no plan-mutation port; deep-freeze stays defence in depth. A failed postcondition is fail-closed recorded — the step record lands `failed` and the attempt blocks `blocked(validation)` naming the hook id, closed only by Phase 5's §2.7 resolution loop. The scheduler is classification-driven (no timers, events, or environment reads; determinism by double-run; the isolation gate extends). The taxonomy's Hook row moves DEFER → LOCK; the hooks runtime (executing real user code against real systems) stays with the adapters (Phases 8–9). |

| D23 | Phase 7's artifact-graph contract (issue #36, ADR-0008, `phase7-artifacts-contract.md`): artifact steps extend the step seam the closed sequence anticipated (ADR-0005's insertion-rule requirement) — a declared value (id, anchor, one guard name, the declared (kind, coordinates) pair, `dependsOn` siblings, the hook's recorded-proof postconditions) with the producer always caller-injected; the engine computes no digest, it records what the producer returned. `StepKey` widens to `StageKey | HookStepKey | ArtifactStepKey` (`artifact:<id>`); same-anchor ties break in declaration order across both extension kinds; `ReleaseAttempt` gains the optional frozen `artifacts` field — excluded from `attemptIdentity` as hooks are. The generation is the attempt's recorded artifact set, immutable, one per attempt — a deliberate rebuild under one version identity is a new attempt and a new generation recorded beside the old (PR-02's sanctioned escape; D9 obligation 6's machinery); identity within a generation is the digest (the domain triple's `sameContent` posture), a differing digest on the same `artifact:<id>` is E-02's `conflict`. The AR-02 fragment (D19(6)) closes: the `dependsOn` DAG is closed declared input validated at the `openAttempt` door (unknown sibling and cycles refuse), and its verify precondition is the dependency digests being recorded in the same generation first — a missing proof is the kernel's recorded refusal, not a re-run. The publish stage completes only over a completed generation (S-04, AR-01); failed postcondition proofs block `blocked(validation:artifact:<id>:<cause>)` closed by the resolution loop. Coordinates are labels, never dereferenced (AR-03, invariant 15); fork 15's artifact half resolves as the nightly artifact step (no version allocation, AR-04 pinned execution-side), its channel-scheduling half stays carried; evidence freshness (PR-03) stays carried; no channel move exists in the phase (PR-04's door untouched). |
| D24 | Phase 8's git-binding contract (issue #39, ADR-0009, `phase8-git-binding-contract.md`): the git binding is an adapter layer beside the engine (`src/adapters/git/`) implementing the ports as they exist — the `ExecutionLedger`, the `AttemptRegister`, the `ClaimStore`, and the artifact producer — owning no vocabulary of its own; persistence is git-native with four guarantees per scope (ledger, register, decision records — one discipline, no second format): canonical form (the fingerprints' form, no envelope), forward-only history (non-extension writes refused at the door before git sees them), byte-exact reload (`classifyResume` over the reloaded tail equals classification over the original), and the mapping invisible through the port (engine code never names a ref, an object, or a path; the reference mapping — one ref per scope, one commit per append, first-parent walk, fast-forward-only ordinal — pins with the implementation PR). The tag-push CAS is the claim store's git-backed accept: git's ref creation as the atomic accept — tag absent at the recorded base → created; present → refused with the conflict recorded (`abandoned(follower-of:<winner>)`); force updates, deletions, re-creations refuse at the same door; tags remain the authoritative record (ADR-0009 decision 4's tag door; D34). The namespace door sits at the ref, before any write: tags outside the attempt's declared namespaces refuse naming tag, namespace, and source — the physical half of M-11's gate, reading only the attempt's carried values (E-08's carried enforcement). The git-backed producer computes the artifact digest from recorded content — never working-tree state — stable across identical content; the digest arrives at the engine opaque, exactly ADR-0008 decision 2. No provider behavior (Phase 9's ADR-0010 owns the GitHub adapter, E2E); the isolation gate extends to the new layer (engine green with the binding absent); determinism re-proven two ways — double-run over fresh repositories and persist–reload equivalence. Fork 16 resolves at the contract level (release-model's unresolved question 2 carries the update); E-07/E-08 become owned (phase 8) with fixture pins in the implementation PR. Amended before implementation, loud in both documents: the accept's atomic boundary is exactly one ref — the scope's claim ref (blob = the claim's canonical record), present-ref denials name the winner read back from the ref; the physical tag is minted at the binding's own door (`mintTag` — token-verified, supplied target, create-if-absent), never inside `acquire`; a stable-version claim is a record (release no-ops, verify stays held), non-tag claims are leases (CAS delete); and the namespace refusal is the port's one reviewed widening — `ClaimDenied` gains optional `holder` and `refusal: "namespace"`, a policy refusal has no winner. Superseded on the claim mapping by D31 (ADR-0011, issue #49): the per-scope claim ref and the lease CAS-delete give way to the per-line register and the whole-set CAS (the accept's atomic boundary, the release shape, and the empty-register posture all move); the rest of this row — the guarantees, the doors, the producer, the isolation — stands.
| D25 | Phase 9's GitHub-adapter contract (issue #50, ADR-0010, `phase9-github-adapter-contract.md`): the GitHub adapter is a new layer under `src/adapters/github/` consuming the git binding's barrel only (`src/adapters/git/index.js`) — never its internals, never the engine's — adding remote synchronization (push of claim refs and minted tags), GitHub Release creation and verification for published tags, and E2E reconciliation (discovery of pre-existing remote state, comparison against the binding's recorded state). The adapter opens on an already-opened `GitBinding` and supplied credentials, never on ambient auth or wall-clock. Every remote write carries idempotency identity; failure classes (`ok` / `refused` / `transport-failure` / `ambiguous`) are returned values, never exceptions. The binding is the source of truth; the remote is a projection — the adapter never writes to the binding and never creates claim state from remote discovery. The per-line lock redesign (issue #49) is explicitly out of scope. |
| D26 | Proposed (phase 9.2 — this row merges as its ratification): the binding's read seam (issue #54, the Phase 9 contract §2.7, amending ADR-0010 decision 2 and ADR-0009 decision 7's surface list): the remote projection reads the binding through a read-only seam on the binding's own surface — `GitBinding.repo` (the opened configuration's repository path) and `GitBinding.refs` (`RefRead`: recorded claim refs under the claim-ref namespace and tags within the declared namespaces, each with the object it names — the claim record's blob (D24), the tag's commit (peeled for annotated tags); pure `for-each-ref` reads — no write, no `HEAD` resolution, no working-tree state) — so the adapter's transport-level git runs against exactly the binding's repository without the adapter learning a caller-chosen path, without credentials, network, or environment entering the binding (ADR-0009 decision 7 stands), and without breaking the barrel-only rule. Weighed and rejected: a remote-operation door on the binding (moves network I/O and credentials into it) and a caller-injected transport runner (object-source by wiring convention, not structure). |
| D27 | Proposed (phase 9.3 — this row merges as its ratification): the changelog seam (issue #57, the Phase 9 contract §2.8, amending ADR-0010 decision 6 and §2.3's refusal vocabulary): the release body is resolved from recorded state only — the binding's public surface gains `GitBinding.content` (`ContentRead`: `claim(ref)` reading a claim ref's canonical record blob, `tagFor(scope)` making the mint door's naming derivation a pure read, `tail(attemptId)` re-exposing the ledger's read path without its `append`, `file(digest, path)` reading one file out of the recorded tree the digest names) while the adapter derives the projection: tag → minting claim → holder attempt → the attempt's recorded stream → the completed `artifact:changelog` generation record → `contentFingerprint` → the recorded tree's `CHANGELOG.md` as the body, the fingerprint as §2.4's idempotency digest. The adapter holds no writable port. The tag→attempt linkage is nowhere recorded; it is derived from the claim records and the tag namespace's global uniqueness (invariant 6). A caller-supplied body and a binding-owned changelog door are rejected; the attempt without a completed changelog record — or with the file absent from the recorded tree — is `refused(reason: "changelog-unrecorded")`, a new §2.3 reason. |
| D28 | Proposed (phase 9.3 — issue #60): an explicit `absent` outcome on `VerificationOutcome` — `verifyRelease` of a release that does not exist for the recorded tag reports `{ kind: "absent" }` rather than force-fitting the union (a `transport-failure` would retry a permanently absent release; no refusal reason names a missing remote read). Absence is a determinate read; the caller's action is the publication itself (the create path is idempotent). Amends the Phase 9 contract §2.2 and ADR-0010 decision 6. |
| D29 | Proposed (phase 9.5 — this row merges as its ratification): the assembly (issue #65, ADR-0010 decision 2 as amended, `test/adapters/github/adapter.test.ts` pinning R-14): `openGitHubAdapter(binding, credentials, transport)` composes the three merged units behind the §2.6 barrel — the factory the barrel's only runtime export, the interim unit exports withdrawn. The third parameter is forced by the platform: Node has no synchronous HTTPS client, and every in-factory improvisation (curl-in-argv, token-in-temp-file) breaks the credential discipline the units already hold; the caller injects the transport at the composition root, and its lifecycle stays the caller's — like the credentials'. The git-path unit consumes none of it. The classifier's stderr pins move to one white-box suite (the contract §4's single named exception); every public door keeps its hermetic pin. |
| D30 | Proposed (phase 9 close-out — issue #66; this row merges as its ratification): the reconciliation's listing-failure semantics — `ReconciliationReport` carries one observation outcome per listing (`tags`, `releases`), each `listed` (the determinate comparison — divergences and verified tags claimed here, an empty listing being a determinate clean observation, the listing twin of D28's `absent`), `refused` (`auth-expired` \| `rate-limited` only — the operator-intervention reasons; the write-conflict reasons name writes), or `transport-failure` (unreachable, unexpected status, a body that is not a list, a compared field that is not a string — decision 7's "unexpected response"). Comparison results exist only on `listed`: over an unobserved remote, a comparison that never ran is unrepresentable as a passed one, and the caller's symmetric obligation is recorded — an unobserved listing is inconclusive, never a passed comparison. Both listings are always requested; a failed listing never preempts its sibling. The shape change is scoped to the report and its two per-listing outcome types (`TagsListingOutcome`, `ReleasesListingOutcome` with the `ReadRefusalReason` narrowing) plus the dead `ambiguous` variant leaving `VerificationOutcome`; an observation never returns `ambiguous` (a write-only class: a read cannot have landed unseen — the publication unit's own read path already classifies so), and a listing's `refused` carries the provider's refusal detail (decision 9's reset timestamp on `rate-limited`). The narrowing is a listing rule, not a universal read rule: `verifyRelease`'s `changelog-unrecorded`/`release-conflict` refusals are comparison decisions over recorded state, not provider refusals. No new refusal reason, no new domain vocabulary, no error framework: the classes stay the §2.3 table narrowed, the classification one shared table across the API-path units. | The merged implementation (#64) failed closed by throwing where ADR-0010 decision 7 promises returned values — the report shape carried no failure channel to return through, and both alternatives the issue names are worse: an empty listing on failure is the false-clean outcome the unit's documentation forbids, and bolting a reconcile-specific reason onto the write-shaped `RefusalReason` without a shape decision is the silent change this decision makes loudly. The narrowing has precedent in the shipped code: publication's read path maps status 0 after a read to `transport-failure` and reserves `ambiguous` for the post-write window. | ADR-0010 decisions 7–8 amended; the Phase 9 contract §2.2 gains the outcome classes and caller obligation, §2.3 gains the read narrowing and the one shared classification table, §2.6's barrel enumeration names the per-listing types, §4 gains scenarios 15–16; matrix R-15/R-16 added. The implementation PR re-pins scenarios 10–12 on the new shape. |
| D31 | Proposed (issue #49 — this row merges as its ratification): the per-line claim register (ADR-0011, amending ADR-0009 decision 4 and the Phase 8 contract §2.3): the binding's claim store holds one ref per release line under the claim namespace — `refs/release-craft/claims/<sha256(lineId)>` — its blob the line's claim set in canonical form (`{"claims":[…]}` sorted by the scope's canonical JSON), and every `acquire`/`release` is a compare-and-set of the whole set against the observed tip; a lost CAS re-reads and re-evaluates, never adjudicates stale. The exclusion predicate is line-local (`excludedBy` matches only shared `lineId`s), so the per-line partition is exactly the exclusion granularity: the same one-ref CAS that creates the claim checked the line's other claims — #47's scan-then-CAS window does not exist, and no two concurrent acquires on one line can both land a claim. Reached in #182 (D42, ADR-0011 decision 2): the absolute reads within one shared ref space — the one repository's own `refs/release-craft/*`; two acquires in two different checkouts of one repository hold disjoint claim refs and can both land, the divergence surfacing at the consumer's push as a non-fast-forward rejection outside the engine's verdict vocabulary — and stands verbatim within one checkout's ref space. The port is unchanged and the memory store is untouched; reads narrow to the line's own ref; the empty register persists (no delete CAS class); a non-register blob under the namespace refuses loudly (the layout change is total, pre-adoption, no migration owed — chosen over an incompatible namespace whose stale refs would read silently as unclaimed). Rejected: the issue's lease lock (banned wall clock, stale-owner cleanup, a second primitive beside the CAS it fails to remove), fencing epochs (the epoch needs the same durable CAS underneath plus new record vocabulary), a serialized single writer (out-of-store bottleneck), a per-line append-log folded at read (same arbitration, every Phase 9 seam consumer folds instead of reads), and documenting serial-only exclusion (weakens Phase 4). Folded: #69's parity pin — exclusion-path denials carry no `holderSequence`, same-scope adjudication denials carry the winner's. The Phase 9 seams follow the record: `content.claim(ref)` → `content.claims(ref): readonly ClaimRecord[]` (§2.8 amendment; publication's tag→claim derivation iterates), `refs.claims()` unchanged mechanically (register blobs), the tag door's held-claim lookup same shape over the register source. | The merged store enforces the `release-line` exclusion law as a serial scan-then-CAS over per-scope refs: two scopes on one line hash to two refs, so the CAS arbitrates same-scope races only, and two processes whose scans both precede either create can both accept on the same line — the Phase 4 invariant holds only serially (#47). The lease lock #49 proposed converts a crashed holder into a stale owner gated on the wall clock the layer bans. | ADR-0011; ADR-0009 decision 4 amended; D24 annotated as superseded on the claim mapping; Phase 8 contract §2.2–§2.3 amended; Phase 9 contract §2.7/§2.8 amended. The implementation PR rewrites the git claim store over the register, adapts the content seam and the tag door, lands the hostile-GitRun deterministic concurrency suite (two writers one move at a time; the loser re-evaluates against the diverged tip), the crash-window and foreign-blob pins, and the #69 denial-shape pins. |
| D32 | Proposed (issue #68 — this row merges as its ratification): the reconciliation's listing observes the resource's **complete** surface — a listing is not complete at page one (`listPath` hardcoded `?per_page=100` and `reconcile()` read the one page, silently truncating any remote past 100 tags or releases; the false-clean outcome the unit's own documentation refuses). The adapter follows the page chain through the response's `Link: <…>; rel="next"` header (RFC 8288) until the provider declares the end; a page that never becomes usable — refused, an unexpected status, a body that is not a list, a lying row — on **any** page of the chain unclaims the **whole** listing (`transport-failure`), never a partial comparison over the pages already read. The per-listing outcome carries the observation's completeness (`listed` gains `listed: number` — the observed rows — and `pagination: "complete" | "truncated"`, only the adapter's deliberate stop producing `truncated`, none produced today); the comparison's results are claimed only over a complete usable observation, keeping the D30 shape's promise. The truncated-listing class is a `transport-failure` (fail-closed), never a pass. | The merged unit read exactly one page: `reconcile()` issued two `transport.request` calls and fed the two bodies straight to the comparisons, so a remote holding more than 100 tags or 100 releases was compared over its first page — a report with no divergences while unadopted rows sat on page two, precisely the silently-clean outcome #66/D30 made unrepresentable. Emptying a truncated listing into `listed` would re-introduce the false-clean the observation outcome exists to refuse; returning a partial comparison would assert a comparison that never fully ran; both alternatives are worse than failing the listing. | ADR-0010 decision 8 amended — "lists the remote's tags and releases" is "across the complete surface (following pagination)"; the Phase 9 contract §2.2 gains the complete-observation sentence, §4 gains scenario 17; `ReconciliationReport`'s per-listing outcomes gain `listed` + `pagination`; matrix R-17 added. The implementation PR walks the `Link` chain in `reconciliation.ts`, re-pins scenarios 10–16 on the new shape, and lands the R-17 pagination pins (follow-across-pages, page-two failure unclaiming the listing). |
| D33 | Proposed (issue #77, as amended — this row merges as its ratification): the stable-version claim record posture exists in ADR-0009 decision 4 itself and in phase8 §2.3's release paragraph — not in the P-01 scenario (which names alpha-increments numeric precedence). The prior label was an accidental re-anchoring: P-01's body says nothing about claims, tokens, leases, or stable-version records. The fix drops the `(P-01)` label at every site that cited it for the record posture (phase8 §2.3 two sites `docs/design/phase8-git-binding-contract.md`, ADR-0009 decision 4, the class doc+release of `src/adapters/git/claim-store-git.ts`, `src/execution/claim-store-memory.ts`, the record's pins in `test/execution/claim.test.ts`, `test/execution/no-mutation-before-claim.test.ts`, `test/adapters/git/claims-mint.test.ts`, `test/adapters/git/claim-register.test.ts`, and this log's own D24 row) and re-anchors them on the deciding text: ADR-0009 decision 4 (the accept's atomic boundary and the lease-vs-record posture) and phase8 §2.3. Where the register superseded the mapping (D31/ADR-0011), the record posture stands unaffected — the register is the same record the per-scope ref held, with the same release no-op. The losing alternative — carrying the mislabel into every new doc by verbatim copy — is what this decision closes; the tag-authority label at the D24 row and in ADR-0010 decision 2's citation is repaired as its own decision, D34. |
| D34 | Proposed (issue #77, as amended — this row merges as its ratification): the tag-is-the-authoritative-record posture is owned by ADR-0009 decision 4's tag door and phase8 §2.3's tag paragraph — not by the P-01 scenario (alpha-increments numeric precedence). The phase8 §2.3 site (`docs/design/phase8-git-binding-contract.md`), the decision-log D24 row, and the ADR-0010 decision-2 citation are re-anchored on ADR-0009 decision 4's tag door (and phase8 §2.3's tag-CAS). This is a distinct decision from D33 because the posture's authoritative owner differs (the tag door, not the claim-release rule) and a reader re-tracing it should land on the tag-creation rule. |
| D35 | Proposed (issue #76 — this row merges as its ratification): the channel-transition door (ADR-0012), the execution side of PR-04/PR-01's promote path. A release moving a `Channel` pointer is an explicit canonical `channel-transition` stage between `tag` and `publish` — the one auditable door (invariant 2.8); no channel ever moves indirectly. The planner declares the planned channel moves, stream close, and promoted-from edge as content of the immutable plan (invariant 2.2's purity); execution records the transition as a write-ahead ledger step and the application applies each move through a persistent channel-store port's compare-and-set over the observed prior target (PR-04's CAS); a completed transition replays `noop`, a divergent prior target conflicts, and an undeterminable remote move records `ambiguous`, never `completed` (invariants 2.5/2.6). Concurrency is the existing release-line claim, never a new lock (invariant 2.7). The kernel stays domain-neutral: it names the stage; the application wires the port (invariant 2.1). | The release model promised channel moves as transitions (PR-04) and promotions leave evidence (PR-01), but no phase executed any of it — `grep -rn channel src/` returned only an illustrative comment, the planner carried no channel data (D19(7)), and every phase contract disclaimed the territory. A release could promote a version while channel pointers silently stayed; the Phase 10 matrix asserted channels unchanged (V4) because no door existed to move one. Alternatives rejected: a workflow engine (overkill for one state change), hooks (would bypass the claim protocol and ledger vocabulary), the kernel consuming the store directly (leaks persistence into the decision machinery), and a new locking primitive (a second source of truth beside the claim). | ADR-0012; `CANONICAL_STAGES` grows `channel-transition` (between `tag` and `publish`); `StepKey`/`MUTATING_STAGES`/`requiresHeldClaim` cover it; `ReleasePlan`/`PlanLine` gain planned channel content; the `LedgerRecord` union gains `channel-transition`; `GitBinding` gains the `channels` port. The Phase 10 V4 row re-proves: the promote run lands the planned channel moves, stream close, and promoted-from edge; the phase 6/7/8/9 disclaimers update to name this door. |
| D36 | Proposed (issue #76 — this row merges as its ratification): the planner's planned channel transitions (ADR-0012 decision 2), the planning half of D35's door. A promote (P-03: `release` with `bump: null` over an in-flight prerelease) whose stable co-mint survives (the minted stable target exists — a promote whose stable is suppressed by a sibling prerelease intent mints on the stream, moves nothing, and carries no `channels`) carries a `channels` field on its `PlanLine` as pure plan content (invariant 2.2 — the planner names moves, it never mutates a channel): the moves for the declared channels PR-01's promote vocabulary names (`stable`/`next`, in declaration order, to the promoted stable), then the promoted-from edge (the in-flight pointer → the promoted stable), then the promoted stream's close — the stream identifier read from the pointer's leading prerelease identifier, never from the registry, and the edge naming the SAME pointer the promote decision named (`pointerFor`'s, the §2.13 projection's last entry — the rebuilt state keeps the first among precedence ties, so on a build-metadata tie the two strings differ and one plan may not carry two identities for the prerelease it promotes). Channels tracking prerelease streams and other lines' channels are untouched by this line's promotion. The declared registry enters as optional `PlanningInput.channels` (`ChannelObservation`: non-empty unique id + the observed target line + version), validated at the §2.1 boundary; absence is the pre-ADR-0012 posture — a promote over an undeclared registry still names its edge and close, and no move can be named for a channel that was never declared. A declared-empty registry is a declaration, not an absence: identical planned content, a different world (a different input fingerprint and planId). The registry joins the inputs fingerprint's policy-relevant projection, so worlds differing in declared channels carry different input fingerprints — and different plan identities — even where planned content coincides (E-04's re-judgment is world-bound). Every non-promote run leaves the field absent: absent is byte-identical to the pre-ADR-0012 plan. | D35 landed the execution door, but the planner still carried no channel data — the kernel's `channel-transition` stage had no plan content to consume, and V4's "channels move exactly as planned" had nothing to re-prove. Reading D35's "the planner declares the planned channel moves" without a decision left the vocabulary open. Rejected readings: the plan also names a trusted `from` target (D35's store compares against the _observed_ prior target — a recorded `from` would age into a false conflict; the plan names `to`, never `from`); moves derived from anything but the declared registry (an undeclared channel cannot be named — D35's door applies only what the plan declared); the close keyed on a registry entry (the stream is a fact of the pointer's prerelease — a registry that names no channel after the stream closes nothing differently); and per-channel move lists authored by the caller (the same trust hole under another name). | ADR-0012 decision 2; the Phase 2 contract §2.1 gains the registry bullet and §2.11 the per-line tuple amendment; `PlanningInput.channels`, `ChannelObservation`, `PlanLine.channels`, and the `PlannedChannelTransition` union land with `plannedChannelTransitions` (derivation: moves in declaration order, then the edge, then the close); `inputsFingerprint`'s world gains the registry; the P-03 golden and the `test/planner/channels.test.ts` pins ride the implementation PR. Phase 10's V4 flip consumes exactly this content through D35's door. |
| D37 | Proposed (issue #76 — this row merges as its ratification): the channel store port's physical half (ADR-0012 decision 6, the `GitBinding.channels` wiring). Five implementation decisions the ADR left to the adapter: (1) the ref mapping is the claim register's own (ADR-0011) — `refs/release-craft/channels/<sha256 of the channel id's UTF-8 bytes>`, because channel ids are opaque strings a refname cannot carry verbatim; ADR-0012's `refs/release-craft/channels/<id>` names the channel the ref is for, not the literal bytes; (2) reads are total — a channel with no recorded ref reads as the hidden channel (S-02: it exists before its first binding), so the first move of any channel applies cleanly against an empty store and hidden is never confused with an error; (3) the store computes the content fingerprint over the state it actually observed deciding (the `hidden` sentinel fingerprints distinctly from any pointed state), so the ledger record's idempotency key (ADR-0012 decision 4) is tied to what the store saw, never to what the plan assumed; (4) `ambiguous` (decision 7) is the land-fault outcome only — a fault around the compare-and-set's write, where whether the move ran is not provable; read-side faults stay loud throws (a store that cannot observe never attempted a move, and a corrupted blob refuses rather than reading as hidden) — with one substrate gap recorded rather than fixed here: `readRef` swallows every git fault as absence, so a ref git cannot read (a broken ref file) also reads as the hidden channel (#95, shared with the claim register), and the store's own loud read boundary is the blob shape check plus the envelope↔ref identity check (a state keyed to another channel's ref refuses); (5) the port's vocabulary is the serialized `ChannelState` (id + line + canonical version string, or `null`), string-shaped like every record target — the kernel's frozen vocabulary carries canonical strings, never parsed values, and stays zero-import (invariant 2.1). | The ADR decided the port's shape and CAS semantics but not its persisted mapping, its read totality, or who computes the idempotency key — and each unrecorded choice was a silent-behavior opportunity: a verbatim `<id>` refname would crash on opaque ids; an absent-ref error or a plan-derived prior would make the first promotion unappliable or guessable; a caller-supplied fingerprint would let the record claim a prior the store never saw; an `ambiguous` on read faults would bury corruption. Alternatives rejected: `read → Channel | null` with absent-as-error (the first move of every channel could never apply); the domain `Channel` class on the port (an import into the zero-import vocabulary file, and parsed values where every record carries strings). | `src/execution/types.ts` gains `ChannelStore`/`ChannelMove`/`ChannelApplyOutcome`/`ChannelState` (the kernel never consumes them); `src/execution/channel-store-memory.ts` is the deterministic reference implementation with the injectable `ambiguous` fault; `src/adapters/git/channel-store-git.ts` is the physical half, wired into `GitBinding.channels`; `channelStateFingerprint` (identity.ts) is shared, so both implementations fingerprint identically. Pinned by `test/execution/channel-store-memory.test.ts` and `test/adapters/git/channel-store-git.test.ts` (including the hostile-`git` race chapter: the loser re-evaluates against the diverged ref and reports what the recorded state proves — `noop` or `conflict`, never a claimed `applied`), plus the read boundary's envelope↔ref identity refusal (a mis-keyed state, read or listed, throws) and the literal fingerprint digests the two reference implementations share. |
| D38 | Proposed (issue #90 — this row merges as its ratification): a promotion-shaped decision never carries a stream extension. When the promote intent wins a line's posture (D17(4)'s P-03: an in-flight prerelease pointer, nothing release-worthy pending), the concurrent `prerelease` demand(s) for that line are subsumed by the promotion: the stable co-mint stands (D17(3) does not suppress it in the promote posture), no stream entry is planned for the line, the promotion carries its full channels plan (D36's content through D35's door), and the promotion decision's `detail` names the subsumed stream(s) in input order, deduplicated — a dropped demand is never silent. Single-intent behavior is unchanged: D17(3)'s suppression keeps its posture for every non-promotion decision (a release decision's content still flows to the stream — P-04/P-05/P-07/M-08). A demand the line's admission posture also refuses keeps its D18 `refusedIntents` record: the two records describe different layers (targets-layer precedence vs. §2.8 admission) and neither replaces the other. | #90's captured run: the combined intents produced a promotion-shaped decision (`release`, `bump: null`, the P-03 detail) whose plan minted `1.2.0-rc.3` (`stable: null`, `movesPointer: true`) with no channel transitions — the decision's shape and its plan named two different actions, and invariant 2.3 (execution consumes the recorded plan) would have executed a stream extension under a promotion record. The corner was reachable because D17(3)'s suppression and D17(4)'s promotion are decided at different layers and no rule ordered them; #89's review finding F2 documented it (the suppressed-stable qualifier in D36's row, the absent-channels pin) without fixing it. Alternatives rejected: a `refused`/`operator-contradiction` record (the promote × release-anyway shape) — reserved for intents making opposite claims about one change set, while a prerelease demand names the stream whose release the promotion performs, and refusing would cancel the viable promotion the operator asked for; and an `InvalidPlanningInputError` naming the line — the planner's malformed-input shape (F3's usage), which would fail the whole pass, every other line's plan included, over a combination whose resolution is unambiguous. The subsumed mint was fully executable, not self-refusing — the base run planned the extension cleanly (`1.2.0-rc.3`, `movesPointer: true`, a `tag-absent` precondition, sorting above the observed pointer; §2.8's below-pointer refusal never had purchase): `planStreams` runs a stream toward the promotion's own mint (`bumpPatch` of the pointer), so the extension carried no change set of its own (the promotion's is inherited; the runway is quiet — a release-worthy join refuses the promotion outright, unchanged). That executability is the defect itself — the shape/plan contradiction above — and D38 supplies the ordering the two decision layers lacked. | `planTargets`' suppression and `planStreams`' demand filter gate on one promotion predicate (plan.ts); `decideLine`'s promotion record names the subsumed stream(s); D36's `targets.stable !== null` guard can no longer drop a promotion's transitions (a promotion's stable is never suppressed). The Phase 2 contract §2.8 gains the promotion-precedence clause. #89's F2 pin flipped — its suppressed-stable promote scenario is unreachable since this rule, and D36's qualifier describes the pre-#90 world; the `assemble.test.ts` door pin asserts the combined-intent plan line equals the single-intent promotion's (the planIds still differ: intents join the inputs fingerprint, D17(7)). |
| D39 | Proposed (issue #95 — this row merges as its ratification): the binding's ref read discriminates absence from every other git fault. `readRef` reports absence as a value only for the one fault shape git's own `--verify --quiet` contract names — exit 1 with **empty stderr**; every other fault a ref read can produce propagates as the GitFaultError it is — a ref git cannot read (a broken loose ref file, empty or holding garbage, exits 1 with a warning on stderr), an unexpected exit status, a spawn failure — so each consuming scope's own fault contract applies to it: the channel store's reads throw (D37(4) stands — a store that cannot observe never attempted a move, and `ambiguous` stays the land-fault outcome alone), the ledger's and attempt register's CAS loops fail closed at their opening read, the tag door faults its mint, the claim register refuses a line whose register git cannot read, and the content read's `claims` seam throws instead of reading a corrupt register as the empty set. The total read is untouched: an absent ref is still the hidden channel (S-02), the empty set, the empty history — the first move of any channel still applies cleanly against an empty store. | #95's probe: after a channel store lands `stable → 1.2.0`, overwriting the loose ref file with `not-a-commit` makes `git rev-parse --verify --quiet <ref>` exit 1 with non-empty stderr (`warning: ignoring broken ref …`), yet `readRef` returned null — `store.read("stable")` read the hidden channel, a hide-move replay classified `noop` carrying the hidden state's fingerprint although git's own `update-ref` over the same ref fails `reference broken`, and a pointed move from `null` reached the land over the phantom hidden read and classified `ambiguous` — one swallow below the reads driving three wrong classifications. The absence shape was verified against the same repository: a never-written ref exits 1 with byte-empty stderr (an empty ref file warns, so it is corruption, not absence). The `readRef` docstring had named the empty-stderr mechanism since the binding landed; the catch never consulted it. The defect is main's (observed on the channel-store branch only because that branch carries the probe's fixture). Alternatives rejected: keying the discrimination on the exit status alone (a broken ref faults with the same exit 1 as an absence — the stderr emptiness is the only shape git distinguishes them by) and mapping read-side faults to `ambiguous` in the channel store (D37(4) refuses it — a store that cannot observe never attempted a move, and an `ambiguous` on every read would let one corrupted pointer stall every resume against that channel). | `readRef` checks status and stderr (git-refs.ts); `refExists`, `firstParentHistory`, and every CAS loop that opens with a read inherit the discrimination without a change of their own. Call-site sweep: the channel store (`read`, the `applyTransition` read boundary, and a listed ref that stopped being readable throw — the false-`noop` replay over a broken ref now refuses); the claim register (`readRegisterAt`/`readRegister` throw for a register git cannot read, so `acquire` faults at its opening read); the ledger and attempt register (their CAS opening reads fault one step earlier than the refused land they already failed at — no outcome class changes); the tag door (the existence check faults before the create attempt, whose own failure already threw); the content read's `claims` seam (a throw, its own documented broken-git vocabulary). Out of the fix's reach: every `for-each-ref` walk — git's enumeration skips a broken ref silently, so a corrupted register is invisible to the all-register walk, `list()`, and the refs-read seam; direct ref addressing (`read`, `acquire`) is the loud path. Pinned by the three-shape `readRef` unit (valid · absent · broken) in `shared.test.ts` and the broken-ref chapters of `channel-store-git.test.ts` and `claim-register.test.ts`, with the land-fault `ambiguous` pinned beside them so the discrimination cannot grow into swallowing land faults either. The hermetic floor strips every `GIT_TRACE*` export — trace diagnostics would ride the exit-1 stderr and read a healthy absent ref as corruption. Two shapes stay inside git's own blindness, recorded as residual: a ref file the process cannot read (a permission denial) and a directory sitting at the ref's path both exit 1 with empty stderr and still read as absent — no `rev-parse` spelling separates them. |
| D40 | Proposed (issue #108 — this row merges as its ratification): the binding's content read discriminates absence from every other object-store fault, the read-side twin of D39. `ContentRead.file` reports a path the recorded tree does not hold as `null` only for the one outcome git's own `cat-file blob <oid>:<path>` spells for it — exit 128 with the byte-exact message `fatal: path '<path>' does not exist in '<oid>'` — and git spells that same absence a second way, `fatal: path '<path>' exists on disk, but not in '<oid>'`, whenever a file of the asked-for name sits in the working tree the binding never reads (ADR-0009 decision 4): both spellings are the null. Every other `GitFaultError` the read can produce propagates as what it is — a blob the recorded tree names that is corrupt or pruned, an unreadable object store, a path that names a directory, an unexpected exit status, a spawn failure — so a broken object store can never wear the recorded-tree-without-the-file answer the projection maps to `changelog-unrecorded`; the corruption of the tree the digest names stays caught one step earlier by the door's own `cat-file -t` gate (D27's documented throw). | #108's probe (git 2.55.0): a loose blob truncated behind a live tree entry exits 128 with `fatal: loose object <oid> … is corrupt` (three `error: header … too long` lines riding it), and the old catch-all returned `null` — publication read a broken store as `changelog-unrecorded` and retried a release whose substrate was corrupt; a pruned blob behind a live entry, an unreadable (EACCES) object, and a directory at the path all fault with `fatal: git cat-file <oid>:<path>: bad file`, none of them absence. The absence shape was verified in both spellings, in a bare repository (no working tree, the plain message only) and under the de/fr gettext catalogs (both messages ship untranslated). Alternatives rejected: keying the discrimination on the exit status alone (every fault class probed faults with the same exit 128 — the message is the only discriminator git gives) and keying on a stderr substring (the byte-exact match keeps a future rewording of either message loud — a fault — rather than letting new corruption wording wear absence's clothes). | `content-read.ts` matches status and stderr against the `PATH_ABSENT_SHAPES` pair; pinned by the two absence chapters and the corrupt-store chapter of `content-read.test.ts` (the corrupt leg is deterministic — the fixture chmods and truncates the loose blob — no hostile-PATH shim needed, the fault being store state rather than an interception window). The discrimination reaches exactly as far as git can spell, recorded as residual and failing closed both ways: a path holding a control character is rendered `?` in the message and a future rewording of the messages would each fault loudly where a refusal was due — never the reverse, never a false null; the hermetic floor pins `LC_ALL: "C"`, so the shapes are read under git's own locale whatever the environment carries (the review's hardening — the locale residual this row first recorded is closed), and the one production consumer passes the constant `"CHANGELOG.md"`. |
| D41 | Proposed (issue #111 — this row merges as its ratification): the human abort becomes durable ledger evidence (ADR-0013). The ledger union gains an `abandonment` record — the abort's `reason` verbatim plus its `attribution` (`{ attemptId, actor }`, the kernel's `AbortOutcome.attribution` the door used to drop), keyed to the attempt at the top level like a resolution's; the boundary's `.abort` door appends exactly one and is the record's only writer (phase 11 contract §2.9); `classifyResume` reads the tail first — a tail carrying the record is terminal from the ledger alone (E-09), refusing the resume as the same thrown protocol violation the terminal-state guard raises, under any process-local attempt value; and the boundary's fresh `.run` scans the plan's derived attempt identities (`attemptIdentity(planId, ordinal)` up to the fresh ordinal, phase 4 §2.1) through the ledger's existing `tail` door and returns `refused` quoting the recorded evidence before claim acquisition and before any ledger write — stated precisely: the fresh ordinal is already allocated when the scan runs (the register has no read door), so a refused run burns one ordinal, durably in the git binding. Two scope lines: the kernel's retry posture stands (phase 4 §5 obligation 3 — a kernel-driven retry still gets a fresh ordinal; E-08 untouched) — the reshaped row is the boundary's `.run`, where a human-facing request enters; and the scan declines to become the plan-keyed attempt index (phase 11 contract §4 question 6's deferred door). | The defect (#111): `.abort` moved the process-local attempt and dropped the attribution; no ledger record was appended (phase 4 §2.8 recorded the state machine's half only — no edge out of `abandoned` — and the ledger union named no kind that could carry the evidence), so a fresh assembly over the same durable stores saw an attempt-free plan and silently re-executed it. Mutation-probed on the phase 10 fixtures: with the abort door's durable append disabled, the fresh `.run` ran the aborted plan to `published`. Alternatives rejected: trusting the attempt state alone (process-local bookkeeping, lost on restart — the defect itself); reusing a `failed` step or a resolution record (a crash classifier would offer resume over the tail; a resolution re-arms — an abandonment ends, and the record must name its own verdict); making the fresh run throw (§2.8's table returns values; the fresh-run caller is the party the evidence must reach); releasing the claim on abort (claim mechanics answering a doctrine question, re-opening the silent re-execution); a plan-keyed attempt-index port door now (§4 question 6 deferred it as its own reviewed change). | `LedgerRecord` gains the `abandonment` variant (`src/execution/types.ts`) — no ledger implementation change: the top-level `attemptId` rides the existing default branches of `tailAttemptId`, the memory ledger's deep-freeze, and the git binding's append extraction, and both bindings reload it byte-exact; `classifyResume` gains the leading tail check (`src/execution/resume.ts`); the boundary's `doAbort` appends and its fresh `.run` gains the derived-ordinal scan refusal (`src/app/engine.ts`, reading only). ADR-0013 is the doctrine; the phase 11 contract's §2.6 abort bullet and §2.8 `abandoned` row are amended to name it. Residuals recorded in the ADR, not waved through: the abort door's durable half is not write-ahead (a ledger fault in the one-commit window between the state move and the append throws loudly, terminal in-process, evidence-free on disk — the kernel offers no check-only door, and append-first could record an abandonment for a move that then throws); the scan's bound is one-writer by construction (a foreign writer of the public `append` port could place a record above the fresh ordinal); no snapshot across the classification's reads (the existing posture); the scan is O(N) tail reads per fresh run. Pinned by `test/execution/abandonment-record.test.ts` (routing, deep-freeze, classification from the tail alone over an open attempt value, byte-exact git reload, double-run determinism), the `test/app/outcomes.test.ts` restart-visibility chapter (a new assembly over the same stores: resume refused, re-run refused quoting actor + reason + attempt id, drives empty, nothing written to the ledger — including the harsher fixture sharing only the ledger, where a fresh register re-derives the aborted attempt's identity), and the `test/app/git-assembly.test.ts` restart pin over a real binding (abort, re-open the repository, the fresh run refuses the recorded evidence). |
| D42 | Proposed (issue #182 — this row merges as its ratification): the claim arbitration's reach is one shared ref space (ADR-0011 decision 2 as amended; stated one layer up by the CLI's claims law, phase 12 §5, and the Action's posture, phase 13 §2.9): the register's exclusion is compare-and-set over the one repository's own `refs/release-craft/*` and extends exactly that far — two runs of the same line in two different checkouts of one repository hold disjoint claim refs (a standard clone fetches only `refs/heads/*` and `refs/tags/*`; the adapter never fetches remote claim state — ADR-0010 decision 3), so both acquire, both mint locally, and the divergence first surfaces at the consumer's push as a non-fast-forward rejection, outside the engine's verdict vocabulary. Serializing across checkouts is a declared precondition of every surface above the binding, not a mechanism the register provides; enforcement (a fetch posture or a remote backstop) is deliberately out of scope — the stabilization freeze chooses the declare-and-pin half of #182's either/or, and the enforcement half is tracked as its own feature request (#237). D31's operative absolute is annotated with the reach clause; the boundary is pinned by a negative capability test. | The repro, executed first-hand (the issue's sketch was marked NOT YET EXECUTED): two standard clones of one throwaway repository, the same `release-line` scope acquired through the git claim store in each — both acquisitions returned a claim with distinct tokens; each clone's register listed only its own record; neither token verified in the other checkout; the first clone's push of the register ref landed and the second's was rejected non-fast-forward (`fetch first`); a fresh clone's `refs/release-craft/` was empty; both clones' `remote.origin.fetch` carried only the heads refspec. | ADR-0011 decision 2 amended (the scope and the named failure mode) and its Consequences name the pin; ADR-0010 decision 3 records the never-fetch clause's scope consequence; phase 8 §2.3's register amendment names the CAS's reach; phase 12 §5's claims law and phase 13 §2.9's inventory state the precondition one layer up; D31's absolute carries the reach annotation. Enforcement stays open as #237. |
| D43 | Proposed (issue #185 — this row merges as its ratification): the git-backed producer's `HEAD`-stability is a declared precondition of the binding (ADR-0009 decision 6 as amended; phase 8 contract §2.5). The digest input is fixed as the recorded tree of the commit `HEAD` names at the call — one atomic `rev-parse` read per invocation, the producer's one ambient-`HEAD` read, never the commit itself (the same tree content under a different commit digests identically, pinned) — and the precondition reads: `HEAD` names one commit for the span of an attempt; moving it in the consumer's checkout mid-attempt is a caller violation of the binding whose failure mode is loud at the binding's only surface — the digest changes exactly as any recorded-content change does, so under the declared precondition a digest change is a recorded-content change and no downstream identity comparison can read the drift as continuity. Attempt-scoped pinning — the stabilization plan's lean — was weighed and rejected as architecturally wrong at this seam: the producer is stateless per call by contract (ADR-0008 decision 2), the seam hands it identity only and never the ledger (ADR-0008 decision 7), so pinning the input to an attempt-scoped commit needs either a port widening (the engine layer's own reviewed change, out of the binding's reach) or a stateful first-call-wins cache keyed on the attempt id — the latter makes the input shape the digest, breaching decision 7 verbatim, and silently swallows a real content change inside the attempt it pins, the worse lie. The empty-repository failure stays as it was: loud, environmental, documented. | The repro executed first-hand (the issue's steps were marked NOT YET EXECUTED): in a temporary repository, an identical producer input digested `git-tree:4b825dc…`; after a second process checked out another branch and committed content, the same input digested `git-tree:56a0f94…` — the silent drift observed; an empty commit on the moved branch (same tree, different commit) digested identically to the moved read — the input is the tree, never the commit; checking the attempt's branch back out restored the original digest. Pinned by two producer tests: the same-tree/different-commit identity (mutating the input source to the commit oid goes red) and the mid-attempt `HEAD` move changing the digest and restoring it on checkout back. | ADR-0009 decision 6 amended; phase 8 contract §2.5 gains the digest-input sentence and the precondition; the producer's docstring names both. No port change, no engine change; the binding's ambient-read surface is unchanged (the one `HEAD` read it already had, now declared). |
| D44 | Proposed (issue #185 — this row merges as its ratification): the git ledger's appends are content-idempotent at the binding layer (ADR-0009 decision 2 as amended; phase 8 contract §2.2's fifth persistence guarantee, content identity): the write door absorbs an append whose canonical bytes the stream's current tip already holds — the write is already satisfied, the stream grows no commit — so the crash-restart overlap of two live writers on one attemptId (a stale writer re-appending the record a concurrent winner already landed) lands one record, not two, the plan record's written-once posture extended to every record through the port's door. The check is tip-relative, deliberately: identity is the tip's canonical content, never a step key and never the stream's history — a byte-identical record the stream carried but moved past is a new positional fact (a re-armed hook start, a crash window's re-issued start) and lands, as does any record differing in a field (`recordedAt`, evidence, claim token, digest); the record vocabulary has no position field, ordering is positional by construction. The first implementation carried the check history-wide and the V7 recovery verticals falsified it the same day — observed, not inferred: the re-armed `hook:sign` start and the crash window's re-issued `artifact:changelog` start were swallowed and a step read `completed` where the crash window wrote `started`; the rule narrowed to the tip and the verticals returned green. Layer division, stated so the two do not blur: the engine's start-identity discipline (#195's write-ahead dedupe, landed on main as PR #212 — the engine layer's files untouched here) lives at the engine layer; this guarantee lives at the persistence layer, on the port's only write doors, where it serves every caller of the port. Declaring attempt-level single-writer as an upstream guarantee was rejected: within one checkout concurrent append is the operative model (D42's one-shared-ref-space reach — the CAS exists because two live writers on one repository are real), so the identity check rides the CAS's classify step and re-evaluates against the moved tip on every retry round. `noteExternal` keeps its last-wins fold (duplicates are already semantically invisible there) and is out of scope. | The repro executed first-hand (the issue's steps were marked NOT YET EXECUTED): two fresh `GitLedger` writers over one repository — the crash-restart overlap shape, the second with a cold cache — appending byte-identical write-ahead starts for one attemptId: pre-fix the ledger ref's history held three commits (plan, started, duplicate started) and the tail carried two step records for one step; post-fix the second `appendStart` is absorbed — two commits, one step record, the caller's returned record byte-identical, classification unchanged. Pinned by tests that go red under the carry-check-removed mutant (the overlap duplicate lands again), the history-wide mutant (the re-issued start is swallowed — the V7 failure reproduced at unit scale), the always-absorb mutant (a record the tip does not hold fails to land), and the field-difference mutant (over-absorption by keying). | ADR-0009 decision 2 amended; phase 8 contract §2.2 gains the content-identity guarantee; `ledger-git.ts`'s `append` classifies with the tip carry check and `appendStart` finds its stored record by canonical bytes instead of assuming the tip (defensive — the absorbed record sits at the tip by definition; the find keeps the return exact if a third writer moves the tip inside the read window, and the genuine-loss guard stays). The reference `MemoryLedger` is untouched (the engine layer's file set): through the engine's doors the backends classify identically, and the byte-level tail differs only in the overlap this guarantee exists to absorb — the persistence layer's own guarantee, recorded here. |
| D45 | Proposed (issue #181 — this row merges as its ratification): the binding refuses a **shallow repository** ahead of the runner's first command on a repository (`git rev-parse --is-shallow-repository`; opening itself spawns nothing — the CLI builds bindings on paths a run may never touch, and fixtures bootstrap `git init` through a fresh runner — so the guard rides the first command and re-arms until it has seen a repository), the runner being the one spawn point every door shares (ADR-0009 decisions 2 and 7; phase 8 contract §2.2). In a truncated clone — `fetch-depth: 1`, the common CI posture — `rev-list --first-parent` exits 0 at the shallow boundary, so a recorded stream's walk returns a _prefix_ of the records: resume classification and planning run over missing history, and appends chain onto the truncated tip (first-hand repro: a 3-commit stream in a shallow clone read as 1, exit 0). Byte-exact reload is the durability invariant the ledger exists for, and no prefix of a stream can honor it; the declared fault at the open boundary (phase 12 §2.4's declared-lie posture, one layer up: the caller chooses the fetch posture and the declared world must be complete) is the only honest option. Support-vs-document was rejected: documenting a precondition would leave the failure mode silent for exactly the consumers most likely to hit it. | First-hand repro: `git clone` + `git fetch --depth 1` of a recorded namespace produced `.git/shallow` with the tip, `--is-shallow-repository` = `true`, and `rev-list --first-parent` returning only the tip commit with exit 0; the same repository unshallowed walks 3/3. `readRef`-keyed absence shapes (D39) are unaffected — the probe is a separate, positive check ahead of the runner's first command. The first implementation probed eagerly when `openGitRun` was called, and the suite falsified it the same day (D44's pattern): the CLI's construction-opens-nothing pin and the fixtures' `git init`-through-a-fresh-runner bootstrap leg went red together — four failures — so the guard moved to the runner's first command and re-arms until it has seen a repository; that survivor mechanism is the one the mutations re-show red. | The guard lives in `openGitRun`'s first command (`probeSubstrateShape`, `git-run.ts`), so every entry point — the binding, the claim store, the channel store, the fixtures — is covered by one probe, and the CLI's construction-opens-nothing laziness pin and the fixture bootstrap flows keep working; a probe's own failure (corrupt repository, missing binary) propagates as the GitFaultError it is. The clean pass is a declared precondition of the runner's lifetime, not a live proof: a substrate that changes underneath a live runner afterwards — a concurrent `--depth` fetch in the same checkout, a grafts plant, a `.git` swap — proceeds unguarded, the caller's ownership of the checkout's shape while the binding runs (D42's posture); phase 8 contract §2.7 names it. Pinned by `substrate.test.ts` (red under a mutation disabling the probe). phase 8 contract §2.7 records the precondition. |
| D46 | Proposed (issue #181 — this row merges as its ratification): **replace objects are disarmed, not refused** — the hermetic floor sets `GIT_NO_REPLACE_OBJECTS=1` on every binding and fixture spawn (ADR-0009 decision 7's law restated: a spawned git reads the repository's recorded objects, never a substitution). With `refs/replace/*` present, an honoring git substitutes the replacement's object at every read — `git show <record-commit>:record` returned the replacement's bytes first-hand — which is the one outcome the byte-exact reload guarantee cannot survive. With the variable set, every spawn reads and walks the recorded objects themselves; the substitution is never honored, so a repository carrying replace refs (for history surgery on branches the binding never touches) stays fully usable. Refusing their presence was rejected: the disarm makes the binding's reads honest regardless of which objects a replacement covers, so refusal would only narrow compatibility without protecting any invariant. `git replace --graft` — the modern graft spelling — is a replace ref and rides the same disarm. | First-hand repro: planting `refs/replace/<record-commit>` over a stream commit made `git show <commit>:record` return the planted bytes; with `GIT_NO_REPLACE_OBJECTS=1` the same read returned the recorded bytes. `GIT_NO_REPLACE_OBJECTS` does **not** cover the `.git/info/grafts` file channel (D47). | `hermeticGitEnv()` gains the variable next to the #180 `GIT_NAMESPACE` strip (#238's texts kept intact); the binding and the test fixtures inherit it from the same floor. Pinned by the floor leg plus the two behavioral legs of `substrate.test.ts` (both red when the variable is removed). phase 8 contract §2.7 records the disposition. |
| D47 | Proposed (issue #181 — this row merges as its ratification): the binding refuses a repository carrying an **`info/grafts` file** ahead of the runner's first command on it (`git rev-parse --git-path info/grafts` + existence). A graft rewrites recorded parentage, so the first-parent walk a recorded stream reports silently changes shape — a parentless graft on a mid-stream commit truncated a 3-commit stream to 2, exit 0, first-hand. The file channel cannot be disarmed: `GIT_NO_REPLACE_OBJECTS=1` does not affect it (verified first-hand — the walk truncates identically with the variable set), and git has no graft-disabling variable; unlike replace refs (D46) there is no way to make the reads honest, so refuse. The probe is presence-based rather than content-based: graft files are deprecated (git 2.55 advises `git replace --convert-graft-file`), carrying one is a history-surgery posture, and per-entry reachability analysis over unbounded attempt streams is not a cost the open boundary should pay to police a substrate shape it can simply refuse. The modern `git replace --graft` spelling is covered by D46's disarm and does not refuse. | First-hand repro: `.git/info/grafts` holding a mid-stream commit's oid made `rev-list --first-parent` return 2 of 3 commits with exit 0, both with and without `GIT_NO_REPLACE_OBJECTS=1`; `git replace --graft` over the same commit produced the identical truncation but is fully disarmed by the variable. | `probeSubstrateShape` probes the grafts path once per runner, ahead of its first command on a live repository; pinned by `substrate.test.ts` (red under a mutation disabling the probe). phase 8 contract §2.7 records the precondition. |
| D48 | Proposed (issues #181 and #184 — this row merges as its ratification): the binding refuses any **object format other than sha1** ahead of the runner's first command on a repository (`git rev-parse --show-object-format`), and `git-refs.ts`'s all-zero CAS constant stays the sha1 width. On a sha256 repository the 40-zero expected-old value never matches the 64-zero current value, so every first write — `casCreateRef`, the first `casAppendCommit` on every stream — fails git-side with `not a valid old SHA1`: loud, but a substrate-shape fact wearing a corruption label, unreachable by the callers' refusal mappings (#184 leg 1, first-hand repro). Refuse-at-open was chosen over support-by-derivation (`--show-object-format` → zero width 64): the derivation is a one-line fix for the CAS but claims width-neutrality the rest of the layer has not been audited for — the fault-shape discriminations (D39/D40's byte-exact stderr shapes), the deterministic object-id pins (COMMIT_ENV pure-function fixtures), and the certification fixture are all certified on the sha1 world; git's default remains sha1 and no consumer demands sha256. The derivation is recorded here as the support path when a consumer needs it, its own reviewed change. | First-hand repro: `git init --object-format=sha256`, then `update-ref <ref> <target> <40 zeros>` → `fatal: ... not a valid old SHA1`; the 64-zero spelling succeeds, proving the CAS itself is the only width-dependent primitive. | `probeSubstrateShape` probes the format first (every oid in every later fault line is width-dependent); `ZERO_OID`'s docstring records why the sha1 width is total. Pinned by `substrate.test.ts` (red under a mutation disabling the probe). phase 8 contract §2.7 records the precondition. |
| D49 | Proposed (issue #184 — this row merges as its ratification): the mint door classifies the unresolvable target **inside the door** (ADR-0009 decision 4; phase 8 contract §2.6): the quiet `rev-parse --verify --quiet <target>^{commit}` faults with exit 1 and empty stderr — the same one shape `readRef` keys absence on (D39) — and the door catches exactly that shape, raising its own `GitFaultError` (`status: null`, argv `rev-parse --verify <target>^{commit}`, stderr `the mint target <target> does not resolve to a commit`). The defect was control flow, not intent: the old code threw that classified fault only after a `.trim()` on output the runner had already thrown on, so the branch was unreachable and the raw runner fault (exit 1, git's own stderr) escaped unclassified. The classification stays a **fault, not a returned refusal**: phase 13 §2.8 names the mint's `rev-parse` as the site where a target mismatch faults (exit 70 at the surface), and that is phase 12 §2.4's declared-lie posture — a caller that names a commit the declared world does not hold has lied about the world, which is distinct from the returned refusal classes (`namespace`/`unclaimed`/`foreign-token`), which name policy races; every other fault (unreadable store, spawn failure) still propagates as what it is, D39's discipline. | Control-flow reading was exact pre-fix (the throw preceded the trim it guarded); the quiet fault shape was verified first-hand (exit 1, byte-empty stderr; unquiet wording `fatal: Needed a single revision`). A returned-refusal reading was rejected: it would widen the pinned `TagMintResult` union and re-classify a caller lie as a policy outcome the ledger would record. | `tag-door.ts` catches the absence shape and re-raises the declared fault; pinned by the `claims-mint.test.ts` leg asserting `status: null`, the argv, and the declared message (red under a mutation removing the catch). phase 8 contract §2.6 records the door's fault law. |
| D50 | Proposed (issue #176 — this row merges as its ratification): the `absent` verdict is discriminated before it is claimed — a release read's 404 is ambiguous between the release's determinate absence (D28) and a repository the credential cannot observe, so `verifyRelease`'s 404 read probes the repository itself (`GET /repos/{owner}/{repo}`): an observable repository (probe `200`) makes the absence the determinate read D28 records; an unobservable one is `refused(reason: "unobservable-remote")` — a new §2.3 refusal reason, an operator-intervention class — with the probe's other statuses classified by the one shared table (401 → `auth-expired`, a denial 403 → `permission-denied`, the rate shapes → `rate-limited`, the rest → `transport-failure`). The probe is the absence claim's discriminator alone: `publishRelease` pays no probe — its create path never claims absence — and the create's own 404 is the same `unobservable-remote` refusal, a determinate non-land that retires the issue's retry loop (a follow-up create that 404s was `transport-failure`, the retryable class, looping on a permanent misconfiguration). A repo-scoped listing's 404 is the same refusal directly (the tags/releases collections exist whenever the repository is observable, so a listing's 404 has no resource-level reading and needs no probe), replacing the `transport-failure` the shared table's old catch-all gave it. Re-reading every 404 as unobservable was rejected: D28's caller action — the publication itself — is reachable only through the 404 → create path, and the idempotency-check create would die; reporting absence without the probe was rejected: it is the defect. | GitHub answers 404 — not 403 — for resources invisible to the caller (an inaccessible private repository, a wrong owner/repo, a moved repository, a token without read scope); the merged `verifyRelease` mapped any release-read 404 to `{ kind: "absent" }` unconditionally (`publication.ts`), so a verification against an unobservable remote reported a determinate absence about a remote the adapter never observed, and the follow-up create 404'd into a `transport-failure` loop. No test distinguished a repository-invisible 404 from a release-missing one — both shapes were untested. The discrimination is GitHub's own recommended probe; no path-local body spelling separates the two 404s. | Phase 9 contract §2.2 gains the probe rule (and its absence-claim scope), §2.3's table gains the 404 rows; ADR-0010 decisions 6 and 9 amended; `RefusalReason`/`ReadRefusalReason` gain `unobservable-remote` (the listing refusal set widens accordingly); the `absent` pins re-pinned over a probe-`200` fixture (`adapter.test.ts`, `publication.test.ts`), the unobservable pin red under a mutation restoring the unconditional `absent`. The probe fires only on the 404 path — the common verified-release read pays nothing. |
| D51 | Proposed (issue #178 — this row merges as its ratification): the refusal classifier's collapses are split so each GitHub shape lands in its contract class — refused vs `transport-failure` stays the load-bearing boundary (a refusal is determinate and non-retryable), and the refusal reasons name the operator's actual intervention: (1) a 403 with `Retry-After`, or whose body message names the secondary/abuse limit (the rate-limits reference's "Exceeding the rate limit" section documents the answer as "a `403` or `429` response and an error message that indicates that you exceeded a secondary rate limit", with the `Retry-After` header only conditionally present), while the primary budget stands is the **secondary** rate limit — `refused("rate-limited")` with the `Retry-After` detail, or the wait-at-least-one-minute guidance when the header is absent (was `auth-expired`; the round-1 review's minor 2 extended the predicate from the header alone, after a headerless body-phrase shape read `permission-denied`) — and the reference's other documented secondary shapes (a secondary response carrying `x-ratelimit-remaining: 0`, a secondary arriving as a `429`) are pinned through the primary leg: the same `rate-limited` class, the primary-worded detail; (2) any other 403 is `refused(reason: "permission-denied")` — a new §2.3 reason: the credential authenticated and the request is not authorized (the fine-grained-token "Resource not accessible by personal access token" answer), grant the scope — rotating a valid token, which `auth-expired` told the operator to do, fixes nothing; (3) the create's determinate refusals — 422 (`already_exists`, the duplicate-create answer observed on the wire — the reference page documents the endpoint's 422 only as "Validation failed, or the endpoint has been spammed": the write raced another publisher, the benign race-loss) and 409 — are `refused(reason: "release-conflict")` with the provider's own words in the detail (was the detail-less retryable `transport-failure`); a caller honouring "transport-failure is retryable" looped on a permanent refusal, and race-loss was indistinguishable from misconfiguration; the recorded conflict decision's caller action — the idempotent re-run — resolves a raced duplicate (`ok` on match, the conflict on divergence), which the V8 verticals already performed over the mis-classified outcome; (4) the git-path classifier anchors on structured stderr shapes, not bare status substrings: GitHub's valid-credential push denial (`Permission to <repo> denied to <user>`) and the http transport's structured 403 (`returned error: 403`, `HTTP 403`) → `permission-denied`; the rejected-credential phrases and the structured 401 → `auth-expired`; a bare `403`/`401` substring — git's own progress counters ("Total 403 (delta 0)") — classifies nothing. Statuses classify only where git prints them structurally. The issue's second git-path leg — a push rejected in the create window (`[remote rejected]`) whose re-list still reads absent — stays `transport-failure` unchanged, deliberately: nothing landed, and the re-list proves the remote still lacks the ref, so the retryable reading names the right caller action; the classifier never sees a status there to re-classify. | Code-path audit (#178's three items, all observed in main): `response.ts`'s `readFailure` classified only 429 and 403-with-`x-ratelimit-remaining: 0` as rate-limited, so a secondary limit and a permission denial both read `auth-expired` (`ReadRefusalReason` had no permission member at all; tests covered only 401 and the spent-budget shape); `publication.ts`'s create special-cased only 201 and status 0, so 422 `already_exists` landed in the detail-less retryable class; `remote-git.ts`'s `classifyGitFailure` regexed `/401|403/` over the whole stderr, so a push's progress line containing "403" wore an authentication fault while a git-transport rate limit whose body git does not surface missed the "rate limit" phrase and read `auth-expired`. | Phase 9 contract §2.3's table restated (the corrected rows above) and §4 gains scenarios 18–21; ADR-0010 decisions 7 and 9 amended; `RefusalReason`/`ReadRefusalReason` gain `permission-denied`; matrix R-18–R-21 added. The V8 verticals' first-outcome pins re-classified (the raced create's `refused("release-conflict")` replaces the `transport-failure` pin — the re-run legs and both-accept refusals unchanged). Pinned red under reversion mutations: the probe-less `absent` (D50's joint pin), the Retry-After shape → `permission-denied`, the 403 fallback → `auth-expired`, the create 422 → `transport-failure`, the listing 404 → `transport-failure`, and the bare-substring git regex (the progress-line pin); the fold round adds the body-phrase pins (publication and listing) and the documented spent-budget/`429` pins, red under a mutation dropping the phrase disjunct. |
| D52 | Proposed (issue #183 — this row merges as its ratification): the compare-and-swap classifies the **contended-ref-lock loss** instead of faulting it (ADR-0009 decision 2's amendment). The loser outcome of a refused `update-ref` is decided inside `casAppendCommit`, in two steps: the re-read decides first, shape-independently (a ref that moved inside the race window is the loss — unchanged); and the one window the re-read cannot yet discriminate — the winner still holding the ref's `.lock`, the ref not yet at its tip — is positively identified by the files backend's byte-exact EEXIST spelling (`cannot lock ref '<ref>': Unable to create '<path>.lock': File exists.` — the first line alone; the advisory after it varies by git version and `core.lockfilePid` state) and retried with bounded patience: at most three whole compare-and-swaps, each spawn riding git's own `core.filesRefLockTimeout` (100ms default). A released lock lands; a ref moved under the retries classifies as the loss (`null`); a lock that never frees exhausts the budget and fails closed. Every other failure spelling — a permission denial (no trailing period), the old-value refusals (`is at … but expected …`, `reference already exists`), a spawn failure — propagates immediately: the window's classification can never downgrade a real substrate fault to a silent loss. The issue's literal suggestion (map the lock-held shape to a plain `null` loss) was rejected with cause: `GitClaimStore.acquire` and `GitChannelStore.applyTransition` loop unbounded on `null`, so a stale lock fed to them as a loss would spin forever — the bounded patience lives inside the primitive, keeping every caller's ratified classification untouched (the channel store's `ambiguous` land-fault outcome and the ledger/register 3-round budgets counting only moved-tip losses are unchanged; ADR-0011's "contention costs retries, not failures" is now true in the contended window instead of only in the moved-tip one). A longer git-side lock wait (`-c core.filesRefLockTimeout=N`) was rejected too: it is a no-op on gits predating the setting and never discriminates anything. | First-hand on git 2.55.0: a 200-round two-process contention loop produced 200 ref-moved losers and zero lock-held failures (the 100ms internal retry absorbs a live winner's critical section — a 50ms hold loses no race, a 500ms hold reaches the EEXIST shape at ~129ms, `core.filesRefLockTimeout=0` reaches it immediately); the EACCES spelling carries `Permission denied` with no trailing period. On the reftable backend the shape never appears (first-hand, 2.55.0, `--ref-format=reftable`): a 40-way parallel CAS on a pre-created ref left 39/39 losers with the `is at … but expected …` refusal and zero EEXIST — the contended window is the files backend's alone. The matcher is structural, not a byte span — the sentence's fixed prefix and suffix anchor it and the lock path between stays opaque — so a quote inside a repository path (reachable only through an operator's path; the binding's own ref components are percent-encoded) cannot break the recognition and revert a benign loser to a fault. Version reach: the EEXIST first line verified in git's source at v2.34.0 and v2.55.0 (`unable_to_lock_message`; the files backend's `cannot lock ref '%s': %s` wrap); `core.filesRefLockTimeout` documented from v2.29.0 through v2.55.0 — on older gits more contentions reach the shape, and the bounded retry is version-neutral by construction. The residual is recorded: contention outlasting the budget (a suspended winner indistinguishable from a stale lock) faults loudly, fail-closed. | `isRefLockContention` and the bounded retry in `git-refs.ts`; pinned by the `shared.test.ts` classification suite — the contended-window legs red under a mutation disabling the recognition, the quoted-path leg red under a revert to the quote-fragile byte span, the ref-moved-spelling leg red under an over-broad matcher, the stuck-lock leg pinning the budget as an invocation count, the unrecognized-shape leg pinning the fail-closed residual (deliberately mutation-immune). phase 8 contract §2.2 item 6 records the law. |
| D53 | Proposed (issue #179 — this row merges as its ratification): the listing walk's `pagination` verdict is claimed from **evidence the page chain ended**, never from a header's absence, and a chain with no honest end is a fault, not an observation. (1) Completeness evidence: the requested page size (`per_page=100`, the pagination reference's documented maximum, docs.github.com/en/rest/using-the-rest-api/using-pagination-in-the-rest-api — "the response includes no more than the maximum number of results per page") is the most any page returns, so a final page **under** that size with no `rel="next"` declared is the end's only observable proof and the listing reads `complete`; a final page **at** full size with no `next` is ambiguous — GitHub omits the header whenever "all results fit on a single page" ("Using link headers"), byte-identical to a transport or proxy stripping it — and reads `pagination: "truncated"`: an honest partial observation that still claims the comparison its rows really earned (the observed divergences and verified tags are real), its label denying the clean-bill reading over the rows never seen — amending D32's classification, which failed the detected-truncated listing to `transport-failure` and so discarded real divergences and hid the ambiguity (D32's unusable-page rule is unchanged). Two boundary shapes are declared permanent readings (round-1 review minor 2), not corners to be revisited: a repository whose row count is an exact multiple of the page size (100×k) ends on a full-size page with no `next` on **every** reconciliation — the ambiguity is the reading's, permanently, and `truncated` is its honest label (verified: no consumer of the listing retries on it — the observation still claims its real divergences and verified tags); and a page returning **more** than the requested size also reads `truncated`, the conservative direction (round-1 review nit 6) — a page over the documented maximum is itself off-contract, and the walk treats it as a page whose end it cannot vouch for. (2) Cycle: the walk keeps a seen-path set; a `next` target it already requested faults the whole listing `transport-failure` before the repeat request — the door never hangs. (3) Malformed next: `nextLinkPath` is split into `nextLink`'s three readings — `absent` (no `rel="next"` declared; other relations such as `first`/`last` are never faults), `next` (a usable API-relative path), `malformed` (declared but conveys no page: an empty target — RFC 8288 §3.1's target IRI is what the angle brackets convey — or no API-relative path: whitespace/control characters, or an absolute URL, the transport's own header form; the transport contract's "`path` is API-relative; the transport applies the credential and the base URL" puts that conversion on the transport's side) — and the malformed reading faults the listing loudly: never a silent stop that reads as the end, never a blind follow into a request error. The follow's trust posture, declared (round-1 review nit 5 — declaration chosen over endpoint-prefix pinning, which would only re-draw the same trust line one level down): a usable target is followed **as given** within the API-relative shape — the walk pins no endpoint prefix — because the `Link` header arrives through the same trusted transport as the rows it paginates; the boundary the adapter polices is the transport contract's (API-relative request paths), not the header's content. Both faults are `transport-failure`, the fail-closed class — the loudest signal the no-throw law (ADR-0010 decision 7) leaves: a throw would violate decision 7, a silent stop re-creates the defect. (4) The no-throw law is enforced, not remembered: every `transport.request` call in publication and reconciliation crosses `response.ts`'s `guardedRequest`, which converts a throwing transport into the status-0 response — a read's status 0 classifies `transport-failure` through the one §2.3 table; the create's is §2.3's `ambiguous` window (a response lost mid-write may have landed; nothing the door can observe separates a thrown transport from a lost connection) — and conformance tests drive a hostile transport against every door (both listings, the idempotency read, the create, the verification read, the repository probe). (5) Decision 8's opening wording ("discovered at open time"; "On `openGitHubAdapter`") is repaired to the implementation it always described: discovery is the `reconcile()` door's walk — the factory opens the doors; nothing reconciles at open time. | Observed on main: `listAll` looped `while (path !== undefined)` over `nextLinkPath` with no seen-path set, and `comparedTags`/`comparedReleases` hardcoded `pagination: "complete"` — the truncation repro (a hermetic transport answering one `per_page`-sized page, no `Link` header) reported `listed, listed: 100, pagination: "complete"` with its divergences claimed as full observation; a cycling fake (page two's `next` pointing back at page two) never terminated — observed bounded: the walk was still re-requesting when the fake's request-count bound threw, and the throw escaped `reconcile()` itself (the no-throw law unenforced); `<>; rel="next"` read as the chain's end (the `[^>]+` target regex made the declared link invisible) with a full page's rows read as `complete`; an absolute-URL target was followed verbatim. Inferred (and labeled as such): that a short final page cannot have a successor — the page-size maximum is GitHub's documented contract, not an observation; that a full-size final page with no header is ambiguous — the two producers of the identical observable cannot be told apart, which is why the guard keys on the page body, never on the header's presence. | Phase 9 contract §2.2 gains the completeness-evidence rule and the chain faults, §2.3 the guarded-boundary sentence, §3's "Failures are values" law its enforcement, and §4 scenarios 22–23 (17 amended); ADR-0010 decisions 7 and 8 amended (8's open-time wording repaired); `PaginationCompleteness`'s doc restated; matrix R-22/R-23 added. The implementation: `response.ts`'s `nextLink`/`guardedRequest`, `reconciliation.ts`'s seen-set walk and evidence rule, `publication.ts`'s four guarded calls. Eleven pins were red pre-fix (the truncation repro read `complete`; the cycling walk escaped through the fake's bound; the empty target read as the chain's end; the absolute URL was followed; both hostile transports threw past the doors) and green post-fix, red again under mutations dropping the seen-set, the final-page-size check, the malformed-target validation, or the guarded boundary. The twelfth — the full-page-**with**-declared-`next` precision pin (scenario 22's followed-chain reading) — was already green on unfixed main (the old loop followed a declared chain fine; the defect was the header-absence reading, not the following); it stays green under those four mutations and goes red only under a walk-stops-early regression — the walk ignoring the declared `next` and stopping after page one. Round-2 (PR #243 review minors 3–4): the guard's thrown words survive into the outcome's optional `detail` on every door (six pins), and the raw-target screen faults a whitespace-padded and a zero-width-space (Cf) target without laundering them — each red under its own mutant (dropping the thrown-word detail, six red; restoring the trim-before-classify, one red; dropping the Cf screen, one red). |
| D54 | Proposed (issue #186 — this row merges as its ratification): the certification fixture's memory/git pairing is **executable, not cited**. Cell `git-15` drives one declared world through BOTH assemblies — the memory engine and the git binding — and asserts three independent facts: the identity chain's sha256 values committed as literals (`inputs_sha256:…` on the document, `plan_sha256:…`, `attempt_sha256:…` on ordinal 1 — the channel-store `content_sha256` discipline applied to the planner's identity); the engine's attempt id equal to the repository's own ledger ref (the raw `refs/release-craft/ledger/<percent-encoded id>` spelling decoded fixture-side); and the git ledger's tail equal to the memory ledger's, in append order, after each side's claim token is projected to `CLAIM` — the git side read with the fixture's OWN raw git spellings (`rev-parse --verify --quiet`, `rev-list --first-parent` reversed, `show <commit>:record`), never the product `GitLedger`. With that oracle in place the durability cells' ledger reads move fixture-side (`git-06`/`git-08`/`git-09`/`git-12`, `x-01` — the `ledgerAttemptIds` pattern generalized), so a product serialization change can no longer move write and read together under green pins; ONE product cross-check leg is deliberately retained (`git-08`'s byte agreement between the raw reader and the product reader) so the two readers cannot drift silently — an agreement pin, never the oracle. The three provenance sites that cited the pairing as proof (`manifest.ts`'s `memory-10` row, `drive.ts`'s `gitDoc` docstring, `git.test.ts`'s header) now name `git-15` instead. Alternatives rejected: narrowing the provenance wording to unproven form (the rows would still cite what no test performs — the executable pairing is strictly better); re-implementing the canonicalizer fixture-side for byte-level tail comparison (R3's second engine — the pairing compares structurally, `toStrictEqual`, over key-order-agnostic projected records); leaving the durability cells on the product reader (the defect: one `GitLedger` rename re-greens every pin). | The gap was demonstrated by mutation: renaming the ledger record's tree entry in BOTH `casAppendCommit`'s `mktree` and `commitRecord` — a change invisible to every product-side pin — reds exactly `git-06`, `git-08`, `git-09`, `git-12`, `git-15`, and `x-01`, all through the fixture's raw readers (`fatal: path 'record' does not exist`), everything else green. The literal oracle reds at its first assertion (~80ms) under a canonicalizer key-order reversal in `identity.ts`, ahead of the subprocess-based byte-pinned cells it shares that mutant with. The fixture reader is load-bearing in both directions: a dropped-record mutant in `rawLedgerTailBytes` reds `git-08`'s agreement leg AND `git-15`'s pairing AND `git-06`'s tail-count pin. | The `git-15` cell plus its manifest row (kind `posture`, class `ledger projections`, provenance phase 14 §4.3 and #186); census counts 46 rows / 34 live. Red under the three recorded mutations: the serialization rename, the key-order reversal, the dropped-record reader. phase 14 contract §4.3 and §10 amended to name the executable pairing; the 90-second budget holds (the cell costs ~1.2s; the suite's measured wall is recorded in the PR). |
| D55 | Proposed (issue #177 — this row merges as its ratification): the composed adapter's two remote identities are compared at the composition root — `openGitHubAdapter` performs an **open-time identity agreement** before any door exists: it reads the binding's origin the way the sync transports it (`git remote get-url origin` — the effective URL git rewrites `insteadOf` configuration into, so the check and the sync share one source of truth), parses it by git's own URL grammar (the "GIT URLS" section of git's `Documentation/urls.adoc`: the `ssh://`/`git://`/`http[s]://`/`ftp[s]://` URL forms, the scp-like `[<user>@]<host>:/<path>` form with the section's own recognition rule — "This syntax is only recognized if there are no slashes before the first colon" — and the local forms `/path/to/repo.git/` and `file:///…`, plus the remote-helper `<transport>::<address>` colon form), normalizes to host + owner + repo (one trailing `.git` stripped, surrounding slashes trimmed, case-insensitive comparison across all three — a differently cased spelling of the same repository is the same repository; exactly two path segments read as an identity), and **refuses to open** — `GitFaultError` thrown, naming both identities and the remedy, never a warning — unless the origin names the credentials' repository. Fail-closed is the check's whole point: an origin that cannot be proven to name it refuses — local paths, `file:` URLs, the ssh `~` home expansions, paths of any other depth, remote-helper forms, unknown schemes, malformed URLs, the empty string, and every non-`github.com` host (GitHub Enterprise origins and the `ssh.github.com:443` alias included), because the `GitHubCredentials` shape carries no host: the only repository any expressible credential addresses is a path on github.com, and GHE support is the credential type's amendment to make (a host field, compared the same way), never the check's guess. A repository with no origin configured opens — one identity, no agreement to break — and the missing origin stays the sync door's own environmental fault at use time. The refusal is the environmental fault thrown at open, not a new outcome class: decision 7's classes are remote-operation outcomes and no door has run; the fault is the repository's own misconfiguration, the family of the sync door's missing-origin `GitFaultError`. Rejected: the check in each door (fragmented four ways, silent for the doors that forget, and inside units whose interiors this contract assigns elsewhere); comparing only at sync time (the API doors keep publishing and verifying against the credentials' repository — the divergence half-fixed and still silent for callers that never sync); and leniency over unknown shapes (assume github.com, warn and continue — a no-op for exactly the origins most likely to be wrong). The grammar's scheme set is exactly git's own — the urls.adoc transports plus `file:` (the round-1 review's fold): npm's `git+*` conventions are not git's grammar, and a URL carrying them is one git itself cannot transport, so it refuses like every other unnameable shape instead of being parsed by guessing past the scheme; the agreement's fault claims no exit status (the origin read succeeds whenever the fault fires — the refusal is the agreement's, never a dressed-up read failure); and the check's residuals are recorded in §2.9 (percent-encoded path segments read literally and can false-refuse; a query or fragment rides the origin unjudged and faults at use; the no-origin tolerance reads every nonzero exit, with the doors faulting loudly at use regardless). | Observed on main (the issue's repro, executed first-hand as a hermetic fixture — a fork origin `https://github.com/fork-owner/release-craft.git`, credentials `ecoma-io/release-craft`): the factory opened, `publishRelease` returned `ok` with the release URL under `ecoma-io/release-craft`, `verifyRelease` returned `verified`, and `syncRemote` reported every ref `pushed` — all against `fork-owner`'s mirror, which the pushes demonstrably reached (the tag present at the recorded target); every `verified` outcome was true of a remote that is not the binding's, and no test distinguished the identities. Nothing in `sync.ts`, `publication.ts`, or `reconciliation.ts` compares them — each resolves only its own address. | Phase 9 contract gains §2.9 (the agreement, the grammar, the fail-closed rule, the residuals) and §4 scenario 24; ADR-0010 decisions 2 and 3 amended. The check lives in `adapter.ts`'s open path only — `sync.ts`/`publication.ts`/`reconciliation.ts` untouched; `remote-identity.ts` carries the pure parser (white-box suite alongside the failure classifier's, the one barrel-rule exception family). Pins: the fork repro refuses at open through the public factory; the local-path origin refuses; the agreeing origin in every spelling opens with no false refusal (including a repository with no origin); red under reversion mutations — removing the open-time check turns the fork repro back to the silent divergence (publish `ok` + `verified` + sync `pushed` against the fork) and dropping the host comparison turns the GHE pins green. |
| D57 | Proposed (issue #189 — this row merges as its ratification): the certification fixture's process transport constructs the subprocess's environment instead of inheriting it, and the provisioning fixture's installs stop validating cached metadata against the registry. The process transport's child environment is the invocation script's outer line (phase 13 §4) mirrored: exactly `PATH` (the ambient one, read through the binding's own `hermeticGitEnv()` floor — the fixture's own modules still name no environment, so the isolation probe holds unamended) and `HOME` (a fresh empty directory per invocation, removed with it — the CLI writes nothing, its own §4 law). Allowlist, never blocklist: ambient `NODE_OPTIONS`, locale variables, agent markers, and `GIT_*` context reach nothing past the construction, where the old wholesale inheritance carried all of it. The provisioning fixture (`test/action/provisioning.test.ts`) adds one fixture-only flag — `--prefer-offline`, pnpm 11.25's "skip staleness checks for cached data, but request missing data from the server" — to both install legs, pinned as exact argv; the composite's run line is unchanged. Alternatives rejected: a strip list on top of inheritance (the provisioning fixture's own marker-strip history shows a strip list is a maintenance treadmill — an allowlist needs no list); an `env` escape hatch on `runBin` (no cell needs one); an isolation-probe exception for a planted-ambient guard inside the fixture (none is owed — the guard lives outside, where the scan does not reach). | The inheritance was demonstrated live: planting `NODE_OPTIONS=--bogus-release-craft-pin` in the worker ambient kills every inheriting child at node startup (exit 9, "is not allowed in NODE_OPTIONS" — observed), the lethality leg now committed in the guard suite `test/certification-env.test.ts`, which lives OUTSIDE the fixture because the isolation probe's token scan judges the suites too and failed the guard's first home live during this slice. The goldens did not move: the full certification comparison is byte-identical on the constructed environment (the CLI's §4 static scan bars it from reading any environment, and the binding's floor already pinned the locale and stripped the git context), so no regeneration was owed. | phase 14 §8's no-environment-reads row gains the declared construction; phase 13 §2.2 records the fixture-only `--prefer-offline`; `runBinEnv` factored and pinned in the census (key set exactly `HOME`+`PATH`, `PATH` equal to the floor's), the spawn-to-construction coupling pinned textually beside it (the probes' own style), the plant-lethal and planted-run legs in the guard suite; the provisioning argv pinned on both legs. Certification wall 20.57s on the inheriting spelling → 20.05s on the constructed one (same tree, minutes apart, 5 files / 51 tests each way) with the four new pins inside (the 90-second budget holds). |

| D58 | Proposed (issue #188 — this row merges as its ratification): the barrel-seam scanners exist on every consumer layer, and a relative specifier is classified by the target it resolves to. `cli` and `adapters-github` gain closed allow-list suites modelled on the adapters-git one — cli: the five `type-cli` barrels spelled exactly, `./` siblings, `node:fs` in `world.ts` alone (the `--world` read), dynamic `import()` banned; adapters-github: the adapters-git barrel spelled exactly, `./` siblings, `node:child_process` in `remote-git.ts` alone, dynamic `import()` banned — each walking its whole layer directory so a new module inherits the gate without being named; and the planner suite no longer skips `../` spellings as within-layer (a cross-project relative import from planner read as local) — each relative specifier is resolved against the layer root: an edge that stays inside `src/planner` is a same-project sibling, one that escapes it is a cross-project import and must name a barrel. The review's round 1 found the same bypass wearing the sibling prefix — `"./../…"` starts with `./` while resolving outside — so the resolved-target law now governs every relative specifier in the `cli` and `adapters-github` scanners and, extended the same hour, in the execution and adapters-git scanners (app's exact-name allow-list has no `./` shortcut to bypass); and the scanners' import regex reads both quote spellings with whitespace optional and one-token specifiers, so `from'…'` and `from"…"` are seen while a quote that merely closes a string value is not mistaken for one. An allow-list is closed on purpose: a legal future import is a reviewed widening of the list, never an ambient permission. | The issue's probe, re-executed on this branch: with `import { openGitRun } from "@ecoma-io/release-craft/__internal__/adapters/git/git-run.js"` planted in `remote-git.ts`, `pnpm arch` exits 0 — right tag pair, wrong module, invisible to the tags — and the planted probes (that spelling verbatim, cli's `__internal__/planner/plan.js`, planner's `'../app/engine.js'`) fail each layer's real-tree scan with the violation named, file and specifier. The planner pin is red against the restored `../` skip — exactly 1 test, the scanner reporting no violation over the planted edge — and green under the resolution, the real trees scanning clean throughout. Full sanitized suite on the head: 98 files / 1443 tests passed (main `cc6b0d8`: 96 / 1433 — +2 files, +10 tests; no pre-existing test loosened). The round-1 bypass matrix, re-executed with planted probes and removed after: the reviewer's `"./../git/binding.js"` in `src/adapters/github` and `"./../app/engine.js"` in `src/cli`, single-quoted deep imports in both layers, `from"…"` without whitespace, `export … from` and `?query` edges out of the layers, and a dynamic import — each fails the real-tree scan naming file and specifier, while a clean sibling control passes unnamed. | ADR-0001 §8 amended (the scanner half of the seam is closed on every consumer layer); the executable record is `test/cli/isolation.test.ts`, `test/adapters/github/adapter-isolation.test.ts`, and the relative-edge and spelling pins in those suites and the planner's; `src/execution/index.ts` and `src/execution/moon.yml` spell the planner seam as the `@ecoma-io/release-craft/planner` alias (#165) instead of the stale `../planner/index.js`. |
| D59 | Proposed (issue #187 — this row merges as its ratification): the self-dogfood's certification judgment is a machine's, in the dispatching job itself, over exactly what the job mechanically persists — and its boundary is declared, not fudged. `scripts/dogfood/judge.mjs` runs as a `dogfood.yml` step after the evidence capture, `if: always()`, reading the `outcome` output, the invoke step's conclusion, and the local ledger refs and tags (run 34470662759 persists zero artifacts — there is nothing else to read). The judge asserts row by row: the envelope class (2) in full — parse, the pinned shape inventory, the carrying fields, the product-boundary invariant; the ledger-projection class (3) in full — the recorded tail projected against the envelope's own drives, not the process's claims; the Action leg's conclusion row (4); and the exit-code class (1) at the dispatch's declared posture (`intents: release` — whose only rendered kind is `published`, the conclusion table's sole exit-0 kind, so a success conclusion is exactly the exit the table demands there). What no machine inside the job can read prints as an explicit NOT ASSERTED row, never a waived pass: the byte-for-byte equality of the survivor with the invoke step's log relay (class 4's byte half — a running job cannot read its own step log through the API), and the exact nonzero exit value of a stop-band kind (unreachable at this posture; the failure-path leg closes it). The judge's conclusion-table vocabulary is deliberately partial — the band partition and the exit-0 set only — and is cross-pinned to `action/invoke.mjs`'s table and the promote walk's cells to the matrix's `git-01` stdout, so a drift on either side reds the judge's own suite. The crash-resume leg and the failure-path leg stay scheduled open workstreams (phase 14 §7), named, not built; `dogfood.yml` joins the README and AGENTS inventories as the fifth workflow outside the three governance layers by design — capture-and-judge, never a gate. | The positive evidence is the certified run's own bytes: run 34470662759's survivor reconstructed byte-exactly (6170 bytes, one trailing newline) from the run's `od -c` dump and its 19-record tail from the same log, replayed into a scratch repository — the judge returns CERTIFIED with every row PASS but the two NOT ASSERTED rows. The judge bites: the same survivor with its trailing newline stripped (the exact corruption a `$(cat …)` transport inflicts) reads the class-4 preserving-write row FAIL and exits 1 — NOT CERTIFIED; the suite (`judge.test.mjs`, 22 tests) holds the same posture at unit scale — planted negatives (a stop-band envelope under a success conclusion, a non-JSON survivor, a forged drive record, a tail missing a cell, a robbed claim, a foreign ledger ref, a missing tag — each red individually, exit 1) and the cross-pins over `invoke.mjs` and `git-01`. | phase 14 §7 gains the judgment-boundary paragraph; `dogfood.yml` gains the judge step — a judge FAIL fails the job, the certification's machine leg, and the NOT ASSERTED rows stay a human log read until the failure-path leg's log-reading judge closes them; README and AGENTS name the outside-workflow inventory. One honest limit recorded: no real dispatch has run the judge step yet — the next `workflow_dispatch` is its first hosted execution. |

| D60 | Proposed (issue #197 — this row merges as its ratification): the planner's two phase-2 residuals are declared and pinned instead of carried as dangling citations. (1) The same-line M-03 duplicate-identity — an origin change and its cherry-pick both pending on ONE line — is pinned end-to-end through the `plan()` door: extraction resolves the legal cherry-pick chain (no surfaced conflict — the refusal is never M-05's shape), attribution keeps both members pending, `ChangeSet.of` refuses the construction (`two members share the change id` — one identity is one member), and `decide.ts` classifies the fail-closed `kernel-rejection` refused record naming the duplicated identity. A coverage pin of the loud path exactly as it stands — no behavior change; the record mints nothing (the refused line contributes no plan line) and its evaluated range keeps both commits inside the un-released span, so the operator's repair is an ordinary re-plan. (2) The streamless-prerelease pointer limit — an admissible tag whose prerelease suffix does not compose the kernel's minting shape (`target-identifier.sequence`: a lone identifier or a non-numeric tail) counts for the pointer — and, through the pointer, for the stable target the in-flight rules compute (never for `stableBase`, which admits only suffix-less releases) — yet surfaces in no stream key, and the frozen `LineState` has no field to report it — is recorded as a known phase-2 limit in phase2-planner-contract.md §2.13, and the `state.ts` comment cites that section instead of the dangling "(reported)". D59 was reserved while this branch was in flight by sibling #249 (next-free-plus-one) and has since landed on main; this row is the next free number after it. | The pin lives in the M-03 scenario suite (`test/planner/maintenance-decisions.test.ts`): F authored on main, F′ cherry-picked back onto main, both inside the un-released span. Green on this head; red — the pin plus the two decide-layer pins of the same classification (3 failed of 42 across the two suites) — with the classification temporarily disabled in `decide.ts` (the kernel rejection re-thrown), then reverted byte-identical (`git diff src/planner/decide.ts` empty). Full suite on the head: 98 files / 1444 vitest tests passed (main `c09104bc`, the rebased base: 98 / 1443 — +1 test, no file added or removed); `pnpm check` exit 0. The `state.ts` edit is comment-only — behavior byte-identical under the unchanged suite. Round-1 review folded one clause: the counter-evidence was executed against `rebuildLineState` directly (`2.3.0` / `2.3.1-alpha` / `2.3.1-alpha.beta` → pointer `2.3.1-alpha.beta`, `stableBase` stays `2.3.0`, streams `[]`), and the stable-base half of the original wording was dropped — this row and §2.13 now say what the code does. | phase2-planner-contract.md §2.13 carries the limit; `src/planner/state.ts`'s KNOWN PHASE-2 LIMIT comment names the section (and #197) instead of an untracked "(reported)"; the same-line kernel-rejection path is covered by an executable pin in the scenario suite. Surfacing streamless tags stays deferred — a `LineState` shape revision, tracked by this row and #197's closure. |

| D61 | Proposed (issue #191 — this row merges as its ratification): the human surface carries the same discipline as the machine surface at three terminal edges. (1) The plan door's human projection renders the stop-shaped decision records — `decision <lineId> <kind> <cause>`, then the record's own `detail` — because a blocked or refused line contributes no plan line (D18): a bootstrap-blocked `plan` exited 0 and named nothing a human could act on while the operator's next move (the `bootstrap-required` decision's S-02 sentence, `decide.ts`) rode `--json`'s `decisions[]` alone; the amendment adds human lines and no machine bytes — the envelope still parses to the value verbatim. (2) `--help`/`-h` are answered in the command position before the grammar dispatches: the closed grammar plus the intent spellings on stdout, exit 0, nothing on stderr, no door reached — `helpText` is built from `usageText`, one grammar text never duplicated, and a help spelling stays in no command's inventory, so `run --help` remains the exit-64 fault that prints the same synopsis and the usage-fault path's semantics are untouched. (3) The Action's declared input inventory is closed in the invoke program: the runner passes undeclared `with:` keys through (documented composite behavior), so a misspelled key (`intent:` for `intents:`) arrived as an unknown injected `INPUT_` name while the declared default silently took over — the consumer's intent never arrived and nothing said so; the program now reads `INPUT_` key NAMES (never a value — the interpolation stays the only channel a value rides) and refuses an unknown name as the pre-invocation fault it is, naming the key and the declared set. The phase 13 §2.3 law becomes "one validation of its own: the key inventory; the grammar remains the values' validation", and §4's planted-lie pin splits accordingly — a planted lie with a declared name still changes nothing. Round 2 closes the copy problem the amendment itself introduced: the help block's intent spellings had been a second hand-maintained list, so the declared spellings are hoisted into the grammar module (`INTENT_SPELLINGS`) as the one copy — the parser's fault names it, help renders it, neither can go stale behind green pins — and the input inventory's remaining boundaries are declared rather than implicit: a foreign workflow-level `INPUT_*` name is refused by the same arm (fail loud, by design), and a bare `INPUT_` is ignored as no input's name. | The live misspelled-input drive, before and after, through the invocation script as a subprocess in the artifact harness's stand-in-bin shape: before, with `INPUT_INTENT` planted the script spawned the bin, relayed its stdout, wrote the `outcome` output through the preserving write and concluded — the typo appears nowhere in the transcript and the projected argv carries the defaulted empty `--intents`; after, exit 1, stdout empty, the outputs file never written, stderr naming `undeclared action input "intent"` and the eight declared inputs. The control drive — declared names planted, including `working-directory`, which reaches no flag — runs the full pipeline unchanged, before and after. Mutants, each red on the head's pins then reverted: deleting the decision-record render turned exactly the two new decision pins red (the `--json` pin stayed green); emptying `HELP_FLAGS` turned the help pins red — and exposed a vacuous loop pin, strengthened to name the two spellings and the flag table exactly; disabling the unknown-key check turned exactly the new undeclared-key pin red. Full suite on the head: 98 files / 1455 tests passed (base `4ba532f`, the rebased head: 98 / 1444 — +11 pins over the two review rounds, no file added or removed, no pre-existing test loosened); `pnpm check` and `pnpm check:policy` exit 0. Round 2 folded three findings: the help block's hand-copied spellings became the declared list itself (`INTENT_SPELLINGS` in the grammar module — the parser's fault names it, help renders it, and the pin ties both renderings to the list; a stale hand-copied block reds exactly that pin); the bare-`INPUT_` boundary is declared, not silently swallowed (an env var named exactly `INPUT_` carries nothing after the prefix, so the empty-name arm ignores it — dropping the arm reds exactly that pin); and the foreign-name refusal — a workflow-level `env:` variable named `INPUT_<something>` reaches the same scan, fail-loud by design — is written into §2.3 and the program's own doc. | `src/cli/render.ts` (the decision rows), `src/cli/grammar.ts` (`HELP_FLAGS`, `INTENT_SPELLINGS` — the declared spellings' one copy — `helpText`), `src/cli/intents.ts` (the fault names the declared list), `src/cli/index.ts` (the command-position answer), `action/invoke.mjs` (`DECLARED_INPUTS`, the refusal, the declared boundaries), `action.yml` (the law documented at the step that owns it); pins in `test/cli/render.test.ts`, `test/cli/grammar.test.ts`, `test/action/invocation.test.ts`; phase 12 §2.2/§3.1 and phase 13 §2.3/§4/§6 amended; the CLI isolation suite's closed import allow-list unchanged (the one new import, intents → grammar, is a same-layer sibling edge the resolved-target law already permits). |

| D62 | Proposed (issue #190 — this row merges as its ratification): the consumer front door is a docs slice — README gains a "Using release-craft" section and the adopter journey lives in a new `docs/adopters.md`, machine-checked. Four gaps, one unit. (1) The journey is the whole arc: install → first release — including the expected bootstrap refusal, S-02's "the first version is the operator's call", which a first-time adopter is guaranteed to meet and which #191's human rendering now surfaces at the plan door — → Action wiring. (2) The pinned Action usage, `uses: ecoma-io/release-craft@<full-sha>`, appeared nowhere consumer-facing; the page documents pin-by-SHA (phase 13 §2.1) with the current main head's 40-character SHA as the example and states why — the pinned commit carries the metadata, sources, lockfile, and grammar table, one commit one behavior. (3) The world document's authoring guidance existed only in internal contracts; the page gives a minimal example and — rather than hand-writing a second schema that can drift — the example IS the machine-checked one: the suite (`test/docs/adopters.test.ts`) extracts the page's fenced world document and the page's merge-in additions (the operator's bootstrap `version`/`who`/`when` plus the declared component) verbatim in document order, passes both through the CLI's own world reader (`readWorldDocument`, the module the `--world` value goes through — the same reader the Action's invoke step reaches by passing the built bin the path) and the planner's memory `plan` door, and pins the first document to the engine's own `bootstrap-required` detail string, so a planner rewording and a stale page fail the same suite. (4) The five-primitive vocabulary (ReleaseLine, Attempt, Claim, Ledger, Generation/DecisionRecord) is taught consumer-facing for the first time, each definition faithful to its owning contract: ReleaseLine to release-model.md's locked vocabulary + ADR-0002 §4, Attempt and Claim to ADR-0005 decisions 2–3 and 4–5 (+ADR-0011), Ledger to ADR-0006 decisions 1–3, DecisionRecord to release-model.md's chosen model + ADR-0002 decision 1, Generation to ADR-0008 decision 5 and phase 7 §2.3. No new semantics invented, no capability claimed that the README's status section does not already promise: the page's honest limits state there is no npm package, no world-reader product slice (close-world.mjs stays caller-side tooling), exactly one hosted consumer (the self-dogfood), one line per Action step, and a process-per-invocation posture. The Round-1 review folded four findings into the page and this row in one follow-up commit: the machine-check claims scoped to what the suite actually owns (the documents, the refusal detail, the quoted digests, the stop row, and the SHA pins — not the transcripts' prose, which stays illustrative and says so), every `plan_sha256:` digest and the run stop row pinned to engine-computed values, the git-assembly step made literally followable (a scratch clone of the adopter's repository, the world re-declared over that clone's real head), and the `bin` sentence corrected to the direct `node dist/src/cli/index.js` invocation (the package is not installed, so the command name does not resolve). | Every command transcript and exit code on the page was executed on this head: `--help` (exit 0, the exact stdout above), `run --help` (exit 64, the closed-inventory fault naming `--help`), the bootstrap-blocked `plan` over the page's first world (exit 0, human and `--json`, the `blocked`/`bootstrap-required` record byte-verbatim), the planned `0.1.0` after the operator's record, the `published` git run minting tag `0.1.0` and recording `refs/release-craft/{claims,ledger,register}`, the re-run's `denied` naming its holder (exit 11), the second release `0.2.0` over a re-closed world, and the invoke program's unknown-input refusal with `INPUT_INTENT` planted (exit 1, stdout empty, outputs never written, the exact stderr line quoted). The page's machine check is the suite itself: `test/docs/adopters.test.ts` (7 tests) green on the head and red on four mutants that were reverted byte-identical — a reworded `bootstrap-required` detail, a truncated Action SHA, an altered quoted plan digest, and an altered run stop row — proving the page's documents, digests, and pins cannot drift from the engine behind green pins (the transcripts' prose stays illustrative, and the page says so). Full suite on the head: 99 files / 1462 vitest tests passed (base `98c8ec2`, the rebased head: 98 / 1455 — +1 file, +7 tests, no pre-existing test loosened); `pnpm check` and `pnpm check:policy` exit 0. One honest limit repeated in the docs: the page's transcripts were executed on a scratch repository, not on the hosted runner — the Action's one certified hosted run remains the self-dogfood, and the page says so. | README gains the consumer-path section; `docs/adopters.md` is the adopter journey (world-document authoring table, the refused-first-run walkthrough, pin-by-SHA Action wiring, the vocabulary, the honest limits); `test/docs/adopters.test.ts` is the machine check that keeps the page's world documents, quoted digests, stop row, and SHA pins from drifting from the engine (for the world documents, the strongest form the anti-drift rule permits: a fixture a test parses with the real world reader); decision-log D62 records the slice. |

| D63 | Proposed (issue #259 — this row merges as its ratification): the self-release is a second hosted consumer beside the read-only dogfood — the repository's own release minted on origin through the repository's own Action, with the Action's surface unchanged. (a) The surface (`self-release.yml`): `workflow_dispatch` the only trigger (the dogfood's own law — a release surface never mints on a push or PR trigger), a demanded `expect-kind` input (a posture is declared, never guessed; an unknown kind is a usage fault in the origin leg), a concurrency group with `cancel-in-progress: false` (the second dispatch's conflict is evidence, not a cancellation), `permissions: contents: write` and nothing else, the same caller-side world closure (`close-world.mjs`), the same pinned Action at a full 40-character SHA, and evidence capture plus both judgment steps under `if: always()`. (b) The credential decision takes the contracts' own declared route: the Action gains no token input (phase 13 §2.8's decided-empty journey; the eight-input inventory is closed and unknown names are refused — D61), so the origin write is the caller's transport — the workflow's publish step spends its own step-scoped `${{ github.token }}` through `env:` (secrets never interpolate into a `run:` line), and `publish-mint.mjs` spawns git with the inline credential helper answering from the spawned process's own environment behind the empty-helper reset — exactly the in-tree precedent's class (phase 13 §2.8, `src/adapters/github/remote-git.ts`): never persisted (the checkout's `persist-credentials: false`, enforced on every workflow by `check:workflows`), never ambient, never an argument, never a URL. (c) The publish is evidence-driven, never declaration-driven: authority to push is the envelope's rendered kind — a `published` verdict's exact (refname, sha) pairs (the run's local mint diffed against the before-snapshot) ride origin in one atomic, no-force push (a moved ref is a loud rejection, the caller-side analog of the claim CAS discipline — #183, D52's classification law); any other verdict pushes nothing, and a published verdict whose mint is empty or omits its named tag faults loudly. (d) The origin leg (`verify-origin.mjs`) reads origin back against the run's claims: a `published` verdict must find every minted pair present on origin, the tag among them, with origin itself cross-checked as the dispatching repository (D55's credentials-origin agreement's spirit, and #177's cross-check concern, carried at the caller layer); any other verdict — or an envelope that never arrived — must find origin byte-identical to its before-state; a missing tag or ref FAILS the job — the green-but-unminted silent failure the leg exists to catch. (e) The judgment reuses `judge.mjs` unchanged — its declared degraded posture already judges a stop-band dispatch (the conclusion row asserted through the conclusion table, whose vocabulary the origin leg imports rather than re-copies — #176's refusal vocabulary carried by the envelope; the exact exit value, the carrying fields, and the ledger projection print as named NOT ASSERTED rows) — no judge row weakened, the judge's own suite untouched; the honest replay shape is declared: a fresh checkout's claim namespace stays empty (phase 13 §2.9), so a same-tag re-dispatch lands `blocked` (the tag-absent precondition, exit 12), not `denied` (exit 11, the holder named — the same-ref-space shape), and the first live dispatch observes and pins the real kind. Alternatives rejected: a token input on the Action (§2.8's decided-empty journey; the closed inventory), an ambient token environment (never-ambient), the token inside a push URL (never-a-URL), forced pushes (a moved ref is evidence, not something to overwrite), and a judge extension for denial postures (its existing degraded posture already names what a running job cannot settle). | 30 new `node --test` tests pin the two caller-side scripts over real temporary repositories whose origin is a local bare clone — the push is real, the transport a filesystem path, so the helper's mechanism class stays pinned where it lives (`remote-git.ts`) while the ref selection, the atomic no-force posture, and every row's bite are pinned here — and pin the workflow's own law as text shapes over the real file (dispatch-only trigger, the demanded no-default posture, the no-cancel group, the minimum contents token, the env-only credential channel, the `always()`-guarded evidence and judgment, the eight declared inputs exactly, the full-SHA pin). The publish pushes exactly the minted pairs and nothing on a stop-band verdict; refuses an empty mint, a partial mint, an empty or non-JSON envelope, and protocol violations (exit 64); never forces (a ref origin already holds is a loud rejection with the recorded namespaces absent — nothing half-pushed under the atomic push). The reset ahead of the inline helper is pinned behaviorally — a hostile ambient helper configured in the spawn's own `HOME` and a logging git shim ahead of the real git on `PATH`: the recorded argv carries the empty reset immediately before the inline helper, and the token appears in neither (it rides the child's environment, never a process listing). The origin leg's comparison is pinned sha-exact: the tag present on origin at a wrong object among correctly landed refs fails the row by the sha, named (one of four, the tag) — a failure a refname-only comparison could never see. The origin leg HOLDs a published verdict whose whole mint is on origin and a stop-band verdict over an unchanged origin (an envelope that never arrived claims nothing, the reason named); BREAKs on a minted ref that never landed, a tag at a different object, origin moving under a no-mint verdict, and an origin that is not the dispatching repository. Red-on-mutant proofs, each executed then reverted byte-identical: dropping the empty-helper reset from the push argv leaves exactly one publish pin red; loosening the origin leg's pair comparison to refname-only leaves exactly one origin pin red; flipping `cancel-in-progress` turns the workflow law suite red. Recorded honestly — the gate does not bite on the checkout line: `check:workflows` regexes the raw file and the workflow's own comments carry the literal, so every deletion shape of `persist-credentials: false` in this file leaves the gate green; the red teeth for the true shape are the workflow law suite, which reads the comment-stripped shape and runs under the ruleset-required `test` check (the gate's comment blindness is filed as issue #262). Gate suite 81 → 111 tests (`pnpm exec moon run gate-scripts:test --force`: 111 pass / 0 fail — +30: 19 over the two caller-side scripts, 11 over the workflow law; no pre-existing test loosened); the vitest suite unchanged at 99 files / 1462 tests. One honest limit, stated in the workflow's header and here: no live dispatch was driven from this PR — the workflow's first hosted execution is the campaign's next step, and it is the leg that observes the replay kind the docs declare. | `.github/workflows/self-release.yml`; `scripts/dogfood/refs-snapshot.mjs`, `scripts/dogfood/publish-mint.mjs`, `scripts/dogfood/verify-origin.mjs` (caller-side, `node:` imports only — never the package it serves); `scripts/dogfood/self-release.test.mjs` and `scripts/dogfood/self-release-workflow.test.mjs` (the 30 pins); phase 14 §7 gains the self-release surface subsection; README, AGENTS, and `docs/adopters.md` name the second hosted consumer; decision-log D63. |
| D64 | Proposed (issue #263 — this row merges as its ratification): the planner's recorded state honors the tags the world observes, and the released-version replay refuses at the planning boundary — the observed-tag leg only; the sibling two-checkouts race stays #237's territory, and no remote backstop is added. (a) The root cause is an input-spelling miss, not a state defect: the hosted world closures declare observed tags under their full git refnames (`refs/tags/0.1.0` — `close-world.mjs` quotes origin), §2.13's projection parsed the name through the kernel's strict grammar, the refname refused as a version, the tag surfaced foreign, the line's rebuilt pointer stayed null, `assemble`'s birth target fell back to the recorded §2.1 bootstrap verbatim, and the walk re-minted the existing tag — origin stayed byte-identical only because the caller-side partial-mint refusal (`publish-mint.mjs`, D63) held. The fix is at the projection: a leading `refs/tags/` prefix strips before the kernel parse; every other name passes through untouched; the kernel's own rejections (`v` prefixes included) are unchanged; the admitted entry keeps the name the world declared, verbatim — the explanation data quotes the world, never a rewrite. (b) The replay record: §2.9's `blocked` cause `released-version-observed` stands when a release-shaped pass (a `release` demand, no admissible `prerelease` demand for the line) meets the fully released evaluated range — the latest admitted tag's commit IS the feed-ref head — of the line whose birth the recorded state carries. The discriminator is the recorded bootstrap claimed **by identity**: the bootstrap is run-level input with no line id, so the line whose own projected history admits the birth version (kernel identity, `compare === 0`) is the line born through the tool, and re-demanding a release over its released head is the replay of that birth — one identity is one member (#197). A line whose history predates the tool carries no recorded birth and never claims one: its quiet released head stays the recorded no-op under the same demand — the maintenance posture M-01's `main`, M-07's quiet line, and S-05's maintenance line pin; an observed tag the line's band keeps foreign never anchors a replay, whatever commit it sits on. The explicit-override records keep their precedence (`release-anyway` over a quiet line is still `forced`; the promotion and operator-contradiction refusals fire first), a passive pass stays the no-op, and an admissible prerelease demand keeps the ladder's no-op-plus-streams posture — the NEXT version's sequence is a new identity, never a re-release. A `release-as` demand naming a version the line's projected history already observes blocks by the same cause. (c) The engine's plan step (phase 11 §2.5 step 1) maps a `blocked` decision for the executed line to the `blocked(cause)` row with the plan named, `handle` null, `drives` empty: the attempt never opens, the claim never acquires, nothing mints, no second attempt is taken; a walk-time block resolves through `.resolve` and resumes, a planning-boundary block has no loop to resolve — the world's release state answers it. The outcome's cause composes the decision's cause token with its detail sentence, naming the observed tag, version, head, and recorded birth. (d) Reported, deliberately unchanged: the validate stage still fabricates `holds: true` for every plan precondition (phase 11 pins that the fixtures drive the observations), so the `tag-absent` guard never consulted the world on the hosted runs — the replay now never reaches validate, and the fabrication is a finding for its own slice, not this amendment's cargo. Alternatives rejected: admitting tags by ancestry as a second admissibility gate (contradicts S-03's locked side-branch history — tags on a release branch the feed head cannot reach are still the line's history, and the gate flipped four goldens), keying the replay on any fully released range without the recorded birth (flips the same goldens to blocks), a claim-namespace fetch or any origin read at planning (the planner is pure, invariant 2; the race is #237's), and loosening the caller-side partial-mint refusal (it stays, defense in depth). | 14 new pins, no existing pin loosened: the §2.13 projection admits the refname-spelled tag under its declared name while the `v`-prefix ban stands, and the derived range bounds at it (2, `test/planner/history.test.ts`); the decision level pins the exact hosted shape blocked with the observed tag, version, head, and recorded birth named, the passive pass over the same head a no-op, the positive counterpart — the observed tag at an older commit with release-worthy pending — releasing the next version, a release demand over a chore-only runway past the release a no-op, a band-foreign birth never claimed, the release-as observed-version block beside the honored unobserved release-as, and the prerelease carve-out (8, `test/planner/decide.test.ts`); the engine pins the blocked outcome — plan named, `handle` null, `drives` empty, the register's first ordinal never allocated — and the replay's determinism (2, `test/app/outcomes.test.ts`); the git assembly pins the hosted shape over a real temporary repository — blocked before any walk, no tag minted (1, `test/app/git-assembly.test.ts`); the built bin pins exit 12 in both transports with the human `cause released-version-observed:` line (1, `test/cli/exit-codes.test.ts`, whose header's unreachable-rows list `blocked` left). Red-on-mutant proofs, each executed then reverted byte-identical (sha-verified), counted with the built bin rebuilt first — the way CI runs it, since the built-bin pin drives `dist` and a stale build would mask a source mutant (the proofs' first recording ran the suites without the rebuild and under-counted; the correction re-executed all three and this text records the rebuilt numbers): disabling the refname normalization leaves exactly 8 red (both history pins, the decide replay and release-as-observed pins, both engine pins, the git pin, and the built-bin pin); disabling the replay precedence leaves exactly 4 red (the decide, engine, git replay, and built-bin pins); loosening the bound to any released range leaves exactly 2 red (the older-commit release pin and the chore-only runway pin — the built-bin pin stays green, its bounds coincide). Vitest suite 99 files / 1462 → 1476 tests, green on the branch's original base (`e6e8546`); rebased mid-review onto `main` (`ad7aea1`, which itself added 242 tests) the head runs 107 files / 1718, all green; the gate suite unchanged at 111. | `src/planner/history.ts`, `src/planner/decide.ts`, `src/planner/types.ts`, `src/app/engine.ts`; `test/planner/history.test.ts`, `test/planner/decide.test.ts`, `test/app/outcomes.test.ts`, `test/app/git-assembly.test.ts`, `test/cli/exit-codes.test.ts`; phase 2 §2.13/§2.9, phase 11 §2.5/§2.8, phase 14 §7, and `.github/workflows/self-release.yml`'s dispatch-shape comments trued to the rendered posture; decision-log D64. |
| D65 | Proposed (issue #274 — this row merges as its ratification): the shared claims register is per-line state the caller integrates before the run — a substrate-posture fix, the product unchanged. (a) The observed failure is a substrate shape, not a product defect: the register is ONE ref per release line (`refs/release-craft/claims/<sha256 of the lineId>`), so its refname is stable across every release on the line, while a hosted checkout materializes no `refs/release-craft/**` (branches and tags only); release 1 created the ref on origin, release 2's runner built the register from empty, its whole-set CAS append (`casAppendCommit`, base null) landed a root commit with no ancestry with origin's tip, and the publish step's atomic, never-forced push was rejected non-fast-forward on that one ref — taking the minted ledger, register, and tag down with it (run 34625474965; the ledger ref is attempt-addressed and the register ref plan-addressed, so only the claims refname collides, but `--atomic` is all-or-nothing). (b) The decision: `self-release.yml` gains one caller-side step, immediately after the checkout and before the world closure, fetching the namespace into the local ref space (`git fetch --no-tags origin '+refs/release-craft/claims/*:refs/release-craft/claims/*'` — unauthenticated, the snapshot step's own posture, the checkout keeping no credential; `--no-tags` so the fetch materializes exactly the claims namespace and the tag set the world closure declares stays the checkout's). The engine discovers nothing and the planner stays pure: the fetched register's commits are off the line's ancestry, so `close-world.mjs`'s closure filter declares none of it and plan identity is unchanged — D64's rejected "claim-namespace fetch at planning" is not this; no origin read enters planning, and the Action's input inventory stays closed. (c) The consequences, verified two-sidedly on a scratch origin: with the ref present locally, the claim store's CAS append builds a child of the shared tip and the mint's push of that ref is a fast-forward; `local-refs-before.txt` is captured after the fetch, so the publish diff sees the moved tip and pushes it. Release 1's record is version-scoped (`stable-version … 0.1.0`) and excludes no different version's acquisition, so release 2 acquires normally; the engine never releases a completed claim (no release call site in `src/app/`), so the register accumulates one record per released version — the shared register becomes the cross-checkout released-version record, which is correct and load-bearing. (d) Scope: two CONCURRENT checkouts still race — a fetch integrates state, it does not serialize contenders — so #237 stays declared, and the fix makes SEQUENTIAL releases publishable only. The dogfood is untouched (read-only by design: `contents: read`, no persisted credentials, zero remote writes proven by hashing `git ls-remote` before and after). Weighed and rejected: a forced push (the never-forced law; it would overwrite a contender's claim), deleting and re-creating the claims ref (breaks the register's append-only forward-only law), fetching inside the claim store or the binding (the caller closes the world; the engine discovers nothing — ADR-0009's isolation), and reading origin at planning (invariant 2's purity; D64's own rejected list). | The issue's evidence, reproduced exactly: `git ls-remote` on origin shows `refs/release-craft/claims/0d6e4079…` at `e1bd422a` with release 1's single `stable-version … 0.1.0` record; `claimRegisterRefFor("main")` over the built product recomputes that refname; an unauthenticated fetch of the namespace into a scratch clone lands it (exit 0). A two-sided rehearsal on a scratch bare origin (labeled: the claims leg, the publish, and the origin leg run the REAL built `GitClaimStore`, `publish-mint.mjs`, and `verify-origin.mjs`; the ledger, register, and tag mints are mimicked with git plumbing in the binding's own commit shape) — without the fetch: the store builds the register from empty (root commit), the publish exits 1 with `! [rejected] … (non-fast-forward)` on the claims ref and `atomic push failed` on all four refs including the tag, origin byte-identical before/after, `verify-origin` BROKEN exit 1; with the fetch: `local-refs-before` carries the shared tip, the append's parent is the shared tip, the record holds both released versions, the publish exits 0 pushing all four refs, `verify-origin` HOLD exit 0. Gates: `pnpm check`, `pnpm check:policy`, `pnpm check:workflows` green. The workflow-law suite (`self-release-workflow.test.mjs`) pins the integration — the step exists, fetches the exact refspec with `--no-tags`, and sits before the snapshot capture — with its red-on-mutant proof run in this PR: deleting the step turns exactly the three new pins red (3 fail of 14), restoring it greens the suite byte-identical. The remaining proof leg is the live re-dispatch (a hosted `published` run landing all four refs), not this PR's to run. | `.github/workflows/self-release.yml` (the integration step, the header comment, and the snapshot step's comment trued); `docs/design/decision-log.md` D65. The claim store, the engine, the binding, and `scripts/dogfood/*.mjs` are untouched — product behavior is unchanged. Racing stays out of scope (#237); sequential releases are made publishable. |
| D66 | Proposed (issue #271 — this row merges as its ratification): the compatibility matrix is trued to what the archkeep shadow run recorded — a docs-only narrowing; no product, test, or workflow file changes. (a) The recorded run: release-craft built at `ad7aea1` planned ecoma-io/archkeep — a real consumer that has never adopted the tool and releases through `googleapis/release-please-action@v5` / release-please 17.6.0 — over its read-only main head `0c2a848` (tag `v0.28.1`, npm dist-tag `latest: 0.28.1`, exactly two commits since the tag, both non-release-worthy under the `default` mapping: `docs` + `chore`). Every invocation over that quiet runway fails to reach a mint: the default plan returns `no-op` / `no-release-worthy-changes`; `release-as:0.28.2` is refused `operator-contradiction` (S-01 — `policy.bumpMappingId` accepts only `default`, so no declared world can make a docs commit release-triggering); `release-anyway` records `forced` but assembles an empty `plan.lines` — no version, no tag; the run door refuses the empty plan at exit 10. Over the same repository with a release-worthy pending range, `release-as:0.28.2` is honored (`line main 0.28.2 (tag v0.28.2)`) — the refusal is keyed purely on the quiet runway, and the forced-mint posture itself stays deliberately unclassified declared-policy territory (adjacent to D2; not decided here). (b) The amendment is exactly the recorded delta: row A3 (Release-As) drops `PARITY` and row A4 (operator-forced release) drops `RC-STRONGER`, both to `PARTIAL`; row B4's release-craft cell stops claiming the forced intent covers the observable outcome; the three rows carry the `POLICY DIFFERENCE` difference class, cite the #271 run as their evidence, and move dogfood to `shadow` — the matrix's first recorded shadow comparison, at the decision-level subset class its shadow law defines — and the "no shadow run has been recorded yet" sentence is retired. The counts table moves with the rows (`PARITY` 7→6, `RC-STRONGER` 14→13, `PARTIAL` 16→18); no other row's text changes. | Re-verified on the amendment branch from the issue's own text, nothing invented: the minimal self-contained world (two commits, one bare `0.28.1` tag, one `main` line, manifest `0.28.1`) reproduces all four outcomes against the built CLI — the default plan exits 0 rendering no line and no decision row at `plan_sha256:aa8b09bd8c9080e68bccd5686dead1c25b9ae55d74d67336d109e73e044eb58c` (the issue's digest, exact); `--intent release-as:0.28.2` exits 0 with `decision main refused operator-contradiction` and the issue's S-01 detail sentence verbatim; `--intent release-anyway` exits 0 at `plan_sha256:9be33fd3dfb01a0c9335d5c7dd9c8178a2d836fa109a3624d671257df45cc207` (the issue's digest, exact) with no line; the run door over that plan in a throwaway repository exits 10 refusing "assembles no line main — the request's lineIds name a line the plan does not carry", the ref count unchanged across the run. The full-scale 588-commit archkeep legs (plan ids `606be377…` / `7c595bf1…`, the world-A end-to-end `published v1.0.1` stopped at verify) are NOT re-executed here — they stand as the issue's recorded run, cited verbatim by the matrix rows. Gates on the branch head: sanitized `pnpm check` exit 0 (docs-only delta; suite counts equal to main's), `pnpm check:policy` exit 0. | `docs/compatibility/release-please.md` (rows A3/A4/B4, the dogfood-status paragraph, the counts table, the [#271] link definition); `docs/design/decision-log.md` D66. The engine, the tests, the workflows, and every other matrix row are untouched. The capability gap itself is recorded, not fixed: giving a quiet line an operator path to a release remains an open product decision, held open by this row. |
| D67 | Proposed (issue #272 — this row merges as its ratification): the planning input refuses overlapping declared `versionBand`s — the duplicate-id refusal's loud posture, at the input boundary; `decide.ts` is untouched. (a) The defect: §2.13's projection admits a tag into every line whose declared band contains it, and D64's birth-identity discriminator ("the line whose own projected history admits the birth version is the line born through the tool") claims per line — so two lines whose bands share a version space (the issue's `1.x` line beside a `1.2.x` line, a recorded bootstrap of `1.2.0` inside the overlap) both carry the birth claim, and a replay-shaped demand blocks both. The outcome is conservative — the version really is observed — but D64's law, the bootstrap records the birth of exactly one line, is unenforceable in that configuration. (b) The predicate is the real version-space one, not a spelling comparison: two bands overlap when some version satisfies both grammars — equal declared major and at least one of the two unpinned on the minor, or both pinned to the same minor. So `1.x` overlaps `1.2.x`; disjoint majors never overlap; the same major with distinct pinned minors (`1.2.x` vs `1.9.x`) does not; identical bands overlap trivially and are refused as their OWN case — not the duplicate-id arm, which stays about ids (identical bands on distinct ids are legal ids carrying an illegal shape, and the overlap violation names both, so the two arms never double-report). The comparison joins only bands whose declared values carry a numeric `major` and, when present, a numeric `minor`; a band outside that grammar is that line's own defect, reported on its own field by the doors that own band shape (the manifest door refuses a missing or non-integer `major`/`minor` and unknown band keys; the CLI world reader refuses non-numbers), and it joins no comparison — `input.ts` itself still does not shape-check `versionBand`, an asymmetry this row reports rather than silently widens (a direct `PlanningInput` caller handing over a malformed band is the caller contract violation the boundary posture already declares). A band-ABSENT line is not a band and joins no comparison: the single-line namespace admits every admissible tag (§2.13), and the vertical matrix's unbanded `main` beside banded maintenance lines stays legal — the refusal compares declared bands only. (c) The shape follows the duplicate-id arm exactly: one violation per overlapping pair, in input order (the earlier index ascending, the later index ascending within it), on the later line's field `lines[j].versionBand`, the problem naming BOTH lines and BOTH bands in §2.13's own series wording (`major 1`, `major 1, minor 2`) and closing with the D64 law the disjointness enforces — collected with every other violation, in the fixed traversal order, never just the first. The ambiguous configuration becomes unreachable past the planning door, so `decide.ts`'s projection needs no change: enforcement at the input boundary is the cheaper, louder posture. (d) Doors: `plan()` (assemble.ts) calls `normalize` first — the planner's single entry (§2.6) — so every world-ingress door that plans is covered: direct `PlanningInput` callers; the CLI's `--world` document, whose reader (`readWorldDocument`) is structural by declared contract ("a semantically wrong document is the caller's declared lie") and whose value reaches `engine.plan` → the planner door, where the refusal fires; and the Action, whose declared eight-input inventory (D61) carries the world to the same CLI door — the Action declares no manifest input. `parseManifest` (config.ts, the authoring door) shape-checks bands but not overlap; its output is consumed only composed into `plan()`, which re-validates, and it carries no production call site — the authoring door stays as-is, the planner door is the enforcement point. Alternatives rejected: scoping the birth claim to one deterministically-picked line in `decide.ts` (two lines still carry one version space; the ambiguity moves from a refused configuration to a silent pick, and the recorded law stays unenforceable), refusing a band-absent line beside banded lines (flips the vertical matrix's legal single-namespace posture; absent is not a band), and shape-validating `versionBand` in `input.ts` inside this slice (a second fault class riding an overlap fix; reported instead). | The pins live in the input-boundary contract suite (`test/planner/input.test.ts`): the issue's exact shape refused with both line ids and both bands named on `lines[1].versionBand`; disjoint majors accepted; the same major with disjoint pinned minors accepted; identical bands refused as the overlap arm with the duplicate-id arm absent; the band-absent line beside banded lines accepted unchanged (the vertical-matrix posture); a three-line world reporting both overlapping pairs in input order — the determinism pin on the exact violation-field sequence; a band outside the grammar joining no comparison (the reported asymmetry, pinned as the boundary's declared residual); and one pin through `plan()` (assemble) proving the #272 world is refused at the planning door before any decision is reached. Red-on-mutant proof: the overlap arm disabled in `input.ts`, exactly 4 tests red (the issue-shape pin, the identical-band pin, the determinism pin, and the `plan()` door pin), the arm restored byte-identical (`git diff` empty); the predicate forced always-true (`bandsOverlap` returning a constant true), re-run over the FULL suite — 107 files, 1726 tests: 30 failed, 1696 passed, across 6 files: the three acceptance pins (disjoint majors, disjoint pinned minors, band-absent) in `input.test.ts`, plus 27 pre-existing tests over banded multi-line worlds (`assemble` ×6, `line-policy` ×6, `maintenance-decisions` ×7, `plan.golden` ×2, `scenario.golden` ×6) — restored byte-identical again (`cmp`-verified against the saved pristine `input.ts` at the rebased head — sha1 `3bcfef76f5321334d2653c61e26d74c491b5dfd3` before and after; the re-run at the rebased bytes reproduces the same counts). An earlier revision of this row recorded "exactly 9 tests red" for this mutant — a count taken over two files only, not the suite; the reviewer's independent full-suite run and this re-run agree on 30, and the row records the reproducible number. Finalized on this branch's ready-for-review head (rebased onto main `6c6f943`): the sanitized gate `env -u AI_AGENT -u CLAUDECODE -u CLAUDE_CODE_CHILD_SESSION -u CLAUDE_CODE_ENTRYPOINT -u CLAUDE_CODE_SESSION_ID -u CLAUDE_CODE_EXECPATH pnpm check` exit 0 — 107 test files, 1726 tests, 0 failed — and `pnpm check:policy` exit 0; the live CLI proof over a force-rebuilt bin drove the `--world` door: the issue's overlapping world refused with exit 70 and the D67 violation verbatim (`lines[1].versionBand: overlaps line "1.x"'s band (major 1) — line "1.2.x"'s band (major 1, minor 2) … the bootstrap records the birth of exactly one line (D64) …`), and the disjoint control (`1.2.x` beside `1.9.x`) passed the input door to a semantic outcome (exit 10, `refused` / `ambiguous-attribution` — the fixture's own pending commit, not the band arm). Two pre-existing fixtures (M-02 and M-03 in `test/planner/maintenance-decisions.test.ts`) declared `main` with the major-2 band beside `2.2`'s pinned band — precisely the refused shape — and now ride the band-absent single-namespace idiom the vertical matrix pins; every assertion those decisions pin (pointers, mints, conflict data) is unchanged, verified by the green gate above. | `src/planner/input.ts`; `test/planner/input.test.ts`; `test/planner/assemble.test.ts`; `test/planner/maintenance-decisions.test.ts`; phase 2 §2.1 amended with the disjointness law and the D64 exactly-one-line birth law it enforces; decision-log D67. `decide.ts`, `config.ts`, `src/cli/world.ts`, and the Action are untouched. |
| D68 | Proposed (issue #269 — this row merges as its ratification): the validate stage's recorded evidence names its derivation — the honest bound, not a world re-proof. The walk's validate mapping recorded every plan precondition as `holds: true` unconditionally (#269), a hold no check derived, under a comment claiming a re-proof; the recorded evidence could contradict reality — the #263 replay's `tag-absent` rode validate as holding with the world's tags never consulted. Two shapes were weighed and the smaller, more truthful one chosen. (A) real re-proof was investigated first and refused as structurally larger than a small slice: `EnginePorts` carries no world-read door — the binding's read seams (`refs`, `content`) are not wired by decision (phase 11 §2.3, "their consumer is the adapter, not the boundary"), so evaluating `tag-absent` at validate means reversing a recorded boundary decision, plumbing a tags read through both assemblies, and re-proving the interruption windows over a walk that now reads the world mid-stage. (B) the honest bound, landed: every validate row still rides the plan's own recorded precondition content, but now names `derivation: "plan-recorded"` — a new optional, closed-vocabulary field on the kernel's `PreconditionObservation` — and the kernel's guard builder emits it as the passed guard row's `detail`, so the durable ledger record itself says where the hold came from; a passed row without a derivation keeps its exact pre-#269 shape, and the schema change is additive (the judge is class-shaped and never asserts guard details; no test pinned the passed row's bytes outside the certification fixtures). The closed vocabulary fails closed at the door, not just at compile time: `requestStep` refuses a `holds: true` row whose derivation token is outside the set — `refused("precondition-derivation-unknown: …")` naming the token — because a foreign token falling through the guard builder would land a bare unevaluated hold on the durable record, byte-identical to the fabrication this vocabulary exists to close. The stage comment, phase 4 §2's stage tour, phase 4's `step.ts` guard-table note, the field docs, and phase 11 §2.5 now state exactly what validate does — it records the caller's precondition observations, it never re-observes — and name the refusal half that already existed at the kernel door: a `holds: false` observation is `blocked(precondition-delta)` (E-04), the walk stopping in order; today no boundary path produces one, a world-side delta being refused upstream at planning (#266) or downstream at the mint door's own CAS, and the residual is recorded in the PR rather than silently closed. | The new pin (`test/app/validate-evidence.test.ts`, 3 tests): the boundary's published beta run carries at least one validate precondition guard row and every row's `detail` names `plan-recorded`; through the kernel door, a derived hold advances with the composed detail byte-pinned, a failed hold blocks `precondition-delta` with its cause — the exact classification the walk would stop on — and a foreign derivation token (built as a JS-boundary payload arrives, untyped through the wire) is the named `refused`, the detail byte-pinned. Red-on-mutant proofs, each executed then reverted byte-identical (`git diff` verified): dropping `derivation` from the walk's mapping turned exactly the derivation pin red (1 failed, 2 passed of 3); deleting the door's vocabulary check turned exactly the foreign-token pin red (1 failed, 2 passed of 3) — the silent bare hold the check exists to close is the mutant's own recorded shape. The three hand-rolled fixture walks (`test/vertical/matrix.ts`, `matrix-ledger.ts`, `matrix-git.ts`) were given the same derivation so the generalization law's record-for-record equality (`toStrictEqual` on drives) held untouched — no assertion weakened, the fixture evidence simply became as honest as the boundary's. Certification fixtures regenerated per the phase 14 §4.3/§5 protocol (throwaway `gen.test.ts` → `generateAll()`, deleted after): exactly 3 of the 6 expected files drifted — `git-01`, `git-02`, `memory-02` — 4 insertions across the 4 validate-bearing scenarios (git-01 and git-02 one each; memory-02 two, one per scenario), each insertion the `"detail":"plan-recorded: …"` string on the validate precondition guard row; `memory-01` (plan-only) and the non-validate scenarios byte-identical; no cell gained, lost, or renamed. Full sanitized suite on the landed head: 108 files / 1729 vitest tests, exit 0 (the test task forced past the Moon cache; base `6e35a4d`: 107 / 1726 — the delta is this slice's new file and its 3 tests, no pre-existing test modified or loosened; one V2 git-vertical timeout on a first gate run was parallel-suite contention, green in isolation and on the re-run); `pnpm check:policy` exit 0. | `src/execution/types.ts` (the `derivation` field and the trued field docs), `src/execution/outcome.ts` (the detail emission and the door's vocabulary refusal), `src/execution/step.ts` (the guard-table note), `src/app/engine.ts` (the mapping and its comment), `test/vertical/matrix.ts`, `test/vertical/matrix-ledger.ts`, `test/vertical/matrix-git.ts` (the same derivation in the fixture walks), `test/app/validate-evidence.test.ts` (the pins), `test/certification/expected/{git-01,git-02,memory-02}.json` (the reviewed bytes), phase 11 §2.5 (the validate clause), phase 4 §2 (the stage tour), decision-log D68. The world-read port that would make validate self-sufficient stays a named boundary amendment — §2.3's wiring row moves first; the `verify` stage's own "re-proving external state" guard detail is a separate finding, its own issue. |
| D69 | Proposed (issue #281 — this row merges as its ratification): six contract passages are trued to the post-#275 surface split of the claims-namespace world — a docs-only narrowing; no product, test, or workflow file changes. (a) The drift: #275 (D65) made the self-release workflow fetch `refs/release-craft/claims/*` caller-side before the run and serialize dispatches through its concurrency group, and six passages still describe the pre-fetch world as universal — phase 13 §2.9's named failure mode ("two `workflow_dispatch` runs … in two separate checkouts each acquire … the divergence first surfacing at the consumer's own push"), ADR-0010 decision 3's scope consequence ("because nothing fetches `refs/release-craft/*` into a checkout"), ADR-0011 decision 2's two-checkout sentence ("do not share local claim refs … each acquires in its own ref space"), phase 8 §2.3's "writers in different checkouts … hold disjoint claim refs", phase 14 §7's caller-side step inventory ("the two steps the dogfood refuses by design" — trued to the three caller-side steps the read-only dogfood has no counterpart for, the integration fetch #275 added, the publish, and the origin verification, two of which touch no origin state, with the publish alone counted as "the one step the dogfood refuses by design", the workflow header's own wording; a first draft's inventory wrongly enumerated the concurrency group as counterpart-less, which the dogfood's own identical `cancel-in-progress: false` declaration refuted — corrected before merge), and the compatibility matrix's J-row ("Claim refs, CAS per ref" — the store holds one register ref per release line with a whole-set compare-and-set per that ref, phase 8 §2.3's amendment; "separate checkouts each acquire" unscoped). (b) The amendment makes each passage surface-conditional without deleting a true clause: the claim-mechanism statements stand, scoped to the no-fetch surface (a bare clone, or an Action consumer whose caller performs no claims fetch — the two-standard-clone shape the negative capability test pins, `test/adapters/git/claim-register.test.ts`), and the self-release surface is named as the divergent posture — its caller-side fetch plus `cancel-in-progress: false` concurrency group, under which the sequential second dispatch adjudicates against the fetched register (a released-version replay blocking at the planning boundary, never a claim denial; the never-forced atomic publish remaining the refusal guarding the shared ref) — while the CAS-reach clause stays verbatim and #237 stays declared in every passage that declared it. Alternatives rejected: deleting the pre-fetch passages as falsified (their bare-clone half is observably true and pinned), amending the two-clone fixture (it pins the no-fetch surface, which behaves as documented), and widening any passage into enforcement language (the Action's own-hand law is untouched; the fetch is the caller's posture, D65). | The drift verified against main `6e35a4d`'s text and the recorded runs, nothing invented: the six passages quoted as they stood; `.github/workflows/self-release.yml` fetches the exact refspec with `--no-tags` between the checkout and the world closure and declares the non-canceling concurrency group; run 34635157220 (conclusion `success`) fetched the existing register ref into its checkout, acquired against it (the claim step advancing, `claim-held` passed), and pushed the minted 0.2.0 plus the register's child tip — the sequential cross-checkout path the pre-fetch passages deny; run 34635275253 (conclusion `failure`, the demanded stop-band posture's expected conclusion) logged the replay's `blocked` / `released-version-observed` outcome verbatim — no claim, no mint, no second attempt. Gates on the branch head: sanitized `pnpm check` exit 0 (docs-only delta; suite counts equal to main's), `pnpm check:policy` exit 0 including `check:docs`. | `docs/design/phase13-github-action-contract.md` §2.9; `docs/adr/0010-github-adapter.md` decision 3; `docs/adr/0011-claim-line-register.md` decision 2; `docs/design/phase8-git-binding-contract.md` §2.3; `docs/design/phase14-certification-fixture-contract.md` §7; `docs/compatibility/release-please.md` J-row; decision-log D69. The engine, the tests, and the workflows are untouched — the docs gain the surface split; #237 stays declared everywhere it was declared. |
| D70 | Proposed (issue #279 — this row merges as its ratification): the verify stage's durable guard detail names the derivation its check performs — a recorded-sequence projection, not a world re-proof. The walk's verify step recorded a passed `tag-boundary` row whose detail read `re-proving external state demands the tag boundary behind it (§2.9)` — a re-proof the check never ran: `tagBoundaryStands` reads recorded step completions only (its own comment: "by sequence `verify` is last, so this is provably true through the door"), and the wording shipped verbatim on a live record (run 34635157220's verify cell). The guarantee was real and is unchanged — a concurrent tag mint is caught by the mint door's create-if-absent CAS (same-target re-mint idempotent, a different target `conflict`), which runs downstream of the verify record — but the recorded evidence named a derivation that happened at a different layer, if at all; durable evidence must not claim a check the recording code did not perform. The fix is wording-only: the tag-boundary row's detail now states the projection and names the CAS's layer — `the tag boundary stands by the walk's recorded sequence — every prior stage's completion recorded, no world re-observation here; where a mint door is wired, its create-if-absent CAS is the world's re-check (§2.9)` — and the sibling wording moved with it: the kernel guard-table note (`step.ts`) and phase 4 §2.9's parenthetical ("the stage that re-proves external state") dropped the re-proof claim, and the `claim-verified` row's detail was trued from "token re-verified against the store immediately before this record" to "token re-verified against the store before this stage's advance verdict" — on the channel-transition stage the store's own CAS lands the planned moves between the advance verdict and the record's append, so the immediacy was marginally overstated for exactly that stage; the guard's evaluated fact (the token re-verified during classification, before the verdict) is unchanged for every stage. No semantic guarantee expands: the refusal, the row's `passed` derivation, the guarantee class, and the verify stage's walk position are what they were — only the recorded wording became truthful, the same truthing treatment D68 applied to validate. | The new pins: `test/execution/outcome.test.ts` byte-pins the verify advance's guard row through the kernel door (a `toStrictEqual` on the single `tag-boundary` row carrying the new detail, over the full recorded prefix), and `test/app/verify-evidence.test.ts` pins the same row through the boundary's beta run — reverting the wording turns exactly them red. Red-on-mutant, executed then reverted byte-identical, the bin rebuilt first (the certification suites spawn the built bin): the full sanitized suite under the mutant read 6 failed / 1725 passed of 1731 tests across 5 files of 109 — the 2 pins this slice adds, plus the 4 certification cells that pin the same durable bytes through the process/Action transports (`git-01`, `git-02`, `memory-02`, and `action-01` replaying git-01's stdout) — data companions of the same pin, red because the regenerated expected files carry the reviewed new bytes; no other red. Clean head: 109 files / 1731 tests, all passing (base `255a6ae`: 108 / 1729 — +1 file, +2 tests, no pre-existing test modified or loosened); `pnpm check:policy` exit 0. Certification fixtures regenerated per the phase 14 §4.3/§5 protocol (throwaway `gen.test.ts` → `generateAll()`, deleted after): exactly 3 of the 6 expected files drifted — `git-01`, `git-02`, `memory-02` — and the structural diff shows only guard-detail strings moved (per mint-bearing scenario: five `claim-verified` rows and one `tag-boundary` row; git-01's second scenario and every other expected file byte-identical); no record field, fingerprint, drive, or cell gained, lost, or renamed. The judge is class-shaped (`verify: ["tag-boundary"]`) and asserts no details — no judge change. | `src/execution/outcome.ts` (the two details and their why-comments), `src/execution/step.ts` (the guard-table note), `docs/design/phase4-execution-contract.md` §2.9 (the parenthetical), `test/execution/outcome.test.ts`, `test/app/verify-evidence.test.ts` (the pins), `test/certification/expected/{git-01,git-02,memory-02}.json` (the reviewed bytes), decision-log D70. D68's row stands as written — its tail named this finding "a separate finding, its own issue", which #279 is; #227's claims/takeover territory is untouched. Residuals, recorded not closed: verify still evaluates nothing about the world (the unchanged class — the CAS is the world's arm where a door is wired, and the memory assembly's `mint: null` posture keeps the boundary with the caller, as the manifest's R2 row already declares; the detail's CAS clause is a placement statement, true of the architecture, riding door-less records only in that sense); phase 4 §2.9's mutating-stage list still omits `channel-transition` — a pre-existing docs drift of a different class than this slice's false-derivation wording, left for its own issue. |
| D71 | Proposed (issue #285 — this row merges as its ratification): the node-workspace adapter refuses a member dependency whose name equals the root manifest's `name` at detection time — a `WorkspaceDetectionError` naming the declaring file, the dependency field, and the dependency, fired inside `extractEdges`'s one shared loop so all four dependency fields get it — instead of silently classifying the edge external (the C-posture: the smaller proven surface). The root package's name is an intra-workspace target the member graph cannot represent: the root manifest is read only for `workspaces` evidence and never joins the member-name map, so a member declaring it fell through the external-skip branch with no refusal and no edge (the executed repro: member→root `"workspace:*"` detected clean with zero edges while the member→sibling contrast refused through the D16 grammar path). The refusal is range-independent — no range makes a root-named dependency representable — and does not fire when the root is itself discovered as a member (the `.` glob), where the member-edge path applies unchanged. The widening is declared and pinned: reading the root manifest for its `name` adds one failure case — a root manifest that exists but cannot be read or parsed now refuses (`(file)` read error) even where the pnpm evidence alone sufficed at main (`readRootPackageName`'s docstring declares it; the unreadable-root-manifest pin test proves it). Alternative declared, not taken: the A-posture (recognizing the root as a workspace member so the edge is representable and propagation-capable) is the larger design change and stays open. | Reproduced first-hand against the built dist at main `255a6ae` (issue #285 fixtures A and B); the real occurrence is loom's `templates/{analytics,starter,workspace-settings}` each declaring `@ecoma-io/loom: workspace:*` against the publishable root package 0.5.0. The tests bite, measured at the landed base `542a903` (counting basis: that base's `detection.test.ts` carries 37 tests; this slice adds 9, no new test file): with the base `detection.ts` bytes restored into the working tree (mutant), exactly the six refusal-asserting tests fail (6 failed / 40 passed of 46) — the pin among them, since base never read the root manifest — then byte-identical restore; with the fix the focused file passes 46/46 and the full suite 1740/1740 (109 files, forced real run); gates on the rebased branch head, forced and uncached: `pnpm check`'s target list exit 0 (25 Moon tasks), `pnpm check:policy` exit 0, `pnpm check:docs` exit 0, `prettier --check .` clean. | `src/adapters/node-workspace/detection.ts` (root-name read + the `extractEdges` refusal; module and function docstrings trued — #290's docblock lines deliberately untouched); `test/adapters/node-workspace/detection.test.ts` (four-field refusal, range-independent refusal, sibling contrast, external positive control, root-as-member guard, unreadable-root-manifest pin); decision-log D71. Row written as D70 pre-rebase; renumbered D70 → D71 when #296's landing took D70 first — every sibling row byte-identical. |
| D72 | Proposed (issue #292 — this row merges as its ratification): the planner isolation gate refuses locale-sensitive reads — `Intl` and the `toLocale*` family join the forbidden nondeterminism classes. (a) The defect: the gate's forbidden-globals list named the clock (both spellings), randomness, `process` and `globalThis`, but no locale surface — a planner module reading `Intl` or calling `toLocaleString` / `toLocaleDateString` / `toLocaleTimeString` / `toLocaleUpperCase` / `toLocaleLowerCase` produced output that moves with `TZ`, `LC_ALL` and the runtime's ICU build, and passed the static half of the gate; the changelog renderer's purity guarantee (§9) rests on that scan. (b) The decision: six entries join `FORBIDDEN_GLOBALS` in the suite — `Intl` at the bare-word granularity the `globalThis` entry uses (the namespace cannot leak out as a value past a property-access scan, and the gate's fail-closed law already refuses a name found in prose), and the five `toLocale*` methods at the call-site granularity the `Date.now` / `Math.random` entries use. The list stays a token scan, not an AST walk; the residual is stated in the gate's own comment rather than hidden: `value.localeCompare(other)` is a locale-sensitive read of the same class and is not on the list yet — recorded so the gap stays a decision rather than an oversight. (c) The consequence is a hardened gate and nothing else: the live `src/planner` tree trips none of the new entries (the renderer reads no locale data), and no `src/` file changes — product behavior is unchanged. | Gates at the rebased branch head (rebased onto main `dcc9348` after #296/#295 landed — the renumber this row records): sanitized `pnpm check` exit 0 — 109 test files / 1742 tests against the landed base's 109 / 1740 (as measured on #295's landing; counting basis: the base carries 1740, the slice adds exactly its two new tests), `pnpm check:policy` exit 0. Red-on-mutant proof, run in the PR and re-run at the rebased head: with the six entries removed from `FORBIDDEN_GLOBALS`, exactly the two new planted tests go red — `test/planner/isolation.test.ts` 2 failed / 6 passed of 8, the six pre-existing tests untouched; the file restored byte-identical (sha1 `e5ae476d08baa0cbfe7fa6c5d1386dd2ce6f1dd3` before and after, working tree clean) and the focused suite re-run green at 8/8. The planted pins follow the #188 pattern — the scan functions called directly on inline rogue source strings with exact rendered violations asserted — one pin per entry, plus the bare-value read pin (`const locale = Intl`) and a negative control pinning that the locale-free siblings (`toString`, `toUpperCase`) stay clean. | `test/planner/isolation.test.ts` (the six entries, the granularity-and-residual comment, the two planted tests, the positive scan's message naming the new classes); `docs/design/decision-log.md` D72. No `src/` file changes — the gate hardens, the product is untouched. Row written as D70 pre-rebase; renumbered D70 → D72 when #296/#295 took D70/D71 first — every sibling row byte-identical. |
| D73 | Proposed (issues #283, #284, #297 — this row merges as their ratification): three contract passages trued to the recorded reality in one docs-only slice, the D69 truing convention applied three times; no product, test, or workflow file changes. (a) Phase 14 §7's closing sentence still ran the self-release workflow's first hosted execution ahead of it — "the workflow's first hosted execution is still ahead — no live dispatch was driven from #259's PR" — while the same paragraph two sentences up cites #259's campaign runs (34597572065, 34597897169) as live dispatches, and D65/D69 record the later executions: the sentence now retires the limit rather than restating it, stating the first hosted executions are behind the workflow and citing the two later dispatches with their recorded outcomes, worded to D69's evidence cell and nothing further — run 34635157220 (the changed cycle under the shared claims-register fetch, #274/D65; conclusion `success`; the minted 0.2.0 plus the register's child tip pushed) and run 34635275253 (the released version's replay; the demanded stop-band posture's expected `failure`; the `blocked` / `released-version-observed` record at the planning boundary — no claim, no mint, no second attempt). (b) Phase 12 §5's claims-are-the-concurrency-control bullet was the seventh unconditioned passage of the #281 class — the one #282's six-passage truing left: "two processes in two different checkouts … each hold their own (#182) — both acquire the same line, both mint locally …" stood universal. The bullet now carries #282's idiom so all seven passages speak with one voice: the two-checkout shape is scoped to when neither checkout holds the namespace fetched into it (the no-fetch caller's shape), and the self-release surface is named as the hosted caller answering the precondition from its own caller-side hand — a hand this law does not govern (the claims fetch before the run; the `cancel-in-progress: false` group serializing dispatches; the sequential second dispatch adjudicating against the fetched register; the never-forced atomic publish staying the refusal guarding the shared ref; D65) — while the CLI's own-hand law, the (#182) citation, and the closing serializing sentence are untouched. (c) Phase 4 §2.9's two mutating-stage enumerations omitted `channel-transition` — the residual D70's own row records ("left for its own issue") — while the guard table they document enforces five mutating stages (`MUTATING_STAGES` in `src/execution/step.ts`, lines 31-37: `prepare`, `commit`, `tag`, `channel-transition`, `publish`; ADR-0012 decision 1): §2.5's stage walk gains the stage between its `tag` and `publish` clauses (a mutation by the same law — it repoints a consumer-facing channel pointer, and no pointer moves without the held release-line claim, invariant 2.7), and §2.9's guard-table sentence enumerates it in the tuple; the verify-guard sentences #296 re-trued are byte-identical (the inserted cell lands on the preceding line at 96 columns, under prettier's print width, so no downstream byte moves). Alternatives rejected: folding each fix into the PR whose review surfaced it (#282's diff was already frozen; #296 landed without this omission) — three stale passages review as one docs slice and take one row. | The drift verified against the recorded material, nothing invented: the three passages quoted as they stood at main `dcc9348` (byte-identical at the rebased base `2e4974a` — #298 touched none of these files) against what they describe — phase 4's enumerations against `MUTATING_STAGES` (`src/execution/step.ts`) and ADR-0012 decision 1; phase 12's bullet against `.github/workflows/self-release.yml` (the caller-side fetch, line 135; the `cancel-in-progress: false` group, line 82) and #282's six landed passages (the conditional-then-caller-posture shape reused, not paraphrased); phase 14's closer against its own paragraph's live-dispatch sentences and the D65/D69 run records. Gates on the branch head, sanitized and forced (Moon's cache does not count as a run): `pnpm check`'s exact target list exit 0 (25 Moon tasks), the suite 109 files / 1742 tests all passing — equal to the rebased base `2e4974a`'s counts exactly (`dcc9348` measured 109/1740 before #298's two tests landed), the docs-only delta adding zero tests; `pnpm check:policy` exit 0 (six sub-gates; `check:pr-description` no-ops locally without PR_BODY, so the hosted policy job judges the PR description); `pnpm check:docs` exit 0; `prettier --check .` clean. | `docs/design/phase14-certification-fixture-contract.md` §7 (the closing sentence), `docs/design/phase12-cli-contract.md` §5 (the concurrency-control bullet), `docs/design/phase4-execution-contract.md` §2.5 and §2.9 (the two enumerations); decision-log D73. #237 stays declared where it was declared; #182's scope statement stands, now conditioned; the no-fetch surface keeps the failure mode the negative capability test pins; D70's recorded §2.9 residual closes here. Row written as D72 pre-rebase; renumbered D72 → D73 when #298 landed D72 first — every sibling row byte-identical. |
| D74 | Proposed (issues #286 #290 — this row merges as its ratification): the node-workspace adapter's docblocks are trued to the recorded reality and its refusal evidence completed — documentation and tests only, no behavior change. The module docblock no longer claims its output feeds `planPropagation` and the updater: the adapter is a public-surface export with no in-repo production caller yet, and its refusal contract (the D16 grammar, the root-named-target refusal of D71) is enforced at the boundary regardless of caller — the same phantom-consumer clause in the adapter barrel and the project manifest's header comment moved with it. The evidence completes #286's five gaps: member-target D16-grammar refusals pinned across all four dependency fields (`workspace:*`, `latest`, `workspace:^1.0.0`, `workspace:~1.0.0`, `workspace:>=1.0.0`, and bare `*` in the three fields that lacked it) beside #285's untouched four root-target pins; positive external-shape controls (`catalog:`, an `npm:` alias, `>=21`, `~4.13.0` — the audited org's real shapes) detect with zero edges and no refusal; one seam run drives a real three-member manifest through detection → `toComponentMeta` → `planPropagation` asserting the propagation edge, the topological order and the `notMoved` negative evidence in one plan; and the compatibility matrix's D4 row records that the ecosystem's real intra-workspace form is `workspace:*` — refused at detection per the declared C-posture — while the tested propagation grammar describes the external-range world. | Caller grep at the base: `grep -rn detectNodeWorkspace src/ test/ --include=*.ts` piped through `grep -v adapters/node-workspace` → zero hits (53 total, all inside the adapter and its suite). Gates at the branch head, forced and uncached: the `pnpm check` target list (25 Moon tasks) exit 0 — full sanitized suite 109 files / 1770 tests against base `2e4974a`'s 109 / 1742 (+28 tests, no pre-existing test's assertions touched; the focused adapter file 46 → 74), `pnpm check:policy` exit 0, `prettier --check .` clean. Red-on-mutant, executed then restored byte-identical (sha1 `5cbefe316ecea371a544a43b5603fbf0fe56501c` before and after): with only `validateRange`'s body neutered — the grammar-refusal branch alone, not #285's code — exactly 31 tests go red (the 23 new member-target cases plus the 8 pre-existing member-target grammar tests), while #285's six root-target refusal tests stay green (its six: the four field pins, the canonical-range pin, the unreadable-root-manifest pin — distinct from those eight). | `src/adapters/node-workspace/detection.ts` (module docblock only — the #285 code bytes byte-identical, verified in the diff), `src/adapters/node-workspace/index.ts` and `src/adapters/node-workspace/moon.yml` (the same clause trued; a scope extension disclosed in the PR), `test/adapters/node-workspace/detection.test.ts` (+28 tests), `docs/compatibility/release-please.md` (the D4 row's reality note, its evidence-cell addition, the new link definition; sibling rows' text byte-identical, cell padding realigned by prettier); `docs/design/decision-log.md` D74. Row written as D73 pre-fold; renumbered D73 → D74 when #300 landed D73 first — every sibling row byte-identical. |

## Decisions unresolved

cross-scope exclusion invariant under the git-backed claim store — is an
open decision. Its ADR and implementation PR are tracked in issue #49 and
are not part of Phase 9. The current contract (ADR-0010, D25) documents
the serial-only limitation and does not assume stronger guarantees.

All Phase 0 questions remain settled: U1/U2/U3 by D4, U5 by D5, U7 by D6,
U4 by D7, U6 by D8. New questions beyond #49 enter through a synthesis or
review artifact, not ad hoc.

## Carried obligations (from the Phase 1 adversarial review, D9)

Findings accepted as correct-as-is or deferred, recorded so they are not
re-litigated and not missed by the phase that owns them:

- **Empty groups are decision records, never mints (Phase 2).** The planner
  converts `ChangeSet.empty()` into a `no-op`/withheld decision record
  (PL-06, S-01 — the latent version is minted nowhere) and never derives a
  version from the empty group's neutral `"patch"`. Construction-time
  rejections (`InvalidChangeSetError` and kin) surface as decision records
  at the planning boundary too — invariant 4's "never an exception" clause,
  re-worded in `release-model.md` to reconcile PL-01's "an error".
- **The prerelease-pointer convention is Phase 2 line policy.** M-08 moves
  the released pointer to a prerelease; P-02/P-07 publish streams with the
  pointer silent. The kernel records either; the planner picks one and
  records it, and must resolve P-02's ladder-override branch, which
  publishes a version that sorts below the pointer and therefore cannot
  move it (the kernel's highest-by-precedence reading).
- **M-01 needs an invariant home (Phase 2 contract).** Line attribution by
  ancestry with fail-closed refusal is the planner's first job and no
  invariant's stress list names it; the Phase 2 contract either adds it to
  invariant 4's stress list or extends invariant 6's sentence.
- **PL-04's self-reference exclusion needs a named rule (Phase 2 contract).**
  The planner's own bookkeeping commits must be deterministically
  identified and never auto-classified as release input; no invariant
  sentence carries it yet (fork 8 adjacent).
- **Plan supersession may terminate in a decision record (Phase 2).** A
  regeneration that lands on PL-06 emptiness closes an open plan with a
  no-op successor — invariant 5's recorded relation must admit mixed-kind
  endpoints.
- **Invariant 10's final clause tightens at the execution contract.**
  "Never publishes new bytes under an existing version identity" becomes
  "never publishes _unrecorded_ new bytes" — PR-02's deliberate rebuild,
  recorded as a new generation, is the sanctioned escape the invariant's
  own generation clause already implies.
- **Kernel values are projections of tag history (Phase 2 loader).** The
  loader rebuilds line/channel values from tags at plan time; stored values
  are caches, never a second source of truth (invariant 6 × invariant 8 ×
  E-11's execute-time re-read).
- **Nightly would widen the kernel (fork 15, only if resolved line-side).**
  `1.5.0-nightly.20260905.abc123` is a valid `Version` but not a
  `PrereleaseStreamState` (non-numeric position); adopting nightly as a
  line release is an ADR-0002 vocabulary amendment, never a silent field.
  Per-stream lifecycle (P-02 "open/completed", P-05 "orphaned") stays
  policy data over the value's keys.
- **The change→released-versions relation has no vocabulary term (only if
  persisted).** M-03/M-04/M-06/M-09 need it as planning-side data
  recomputable from tags; persisting it under a new name requires an
  ADR-0002 amendment. Invariant 1's clause list and §5's S-01/S-04 split,
  invariant 9's "identity matching over recorded provenance" phrasing
  (M-05's consume-lineage decision is planning-side), and `Bump`'s
  provenance note for ADR-0002's vocabulary table are recorded wording
  observations only.

## Rejected alternatives

| #   | Alternative                                                       | Why rejected                                                                                                                                     |
| --- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1  | Adopt release-please's internal architecture as the domain model. | Its shapes are a working codebase's history, not a domain analysis; it would become a dependency of the core. (To be sharpened by the 0B audit.) |
| R2  | Implement primitives directly, skip discovery.                    | Issue #13: invents prerelease/branch/hook concepts ad hoc — the drift this phase exists to prevent.                                              |

## Workstream dependencies

```text
0A audit ──────────┐
0B release-please ─┼─► 0D scenario matrix ─► 0E model comparison ─► 0F vocabulary ─► 0G complexity budget ─► PR-A
0C taxonomy ───────┘        (3 proposers + reconciler)      (3 evaluators + synthesis)
```

## Scenario coverage

Tracked in `release-scenarios.md` once reconciled. Every accepted
architectural invariant must name the scenarios that stress it.

The synthesis (`release-model.md`, ADR-0002) passed an independent adversarial
review over six surfaces (invariant falsification, vocabulary integrity,
complexity-budget honesty, the twelve failure patterns, evidence consistency,
implementability) before PR-A: the model decision stood — 12/12 failure
patterns pass, all six identity separations hold — and every finding was
fixed in the documents (the invariant-1 phase-claim split, the
residual-inheritance row completing the compatibility boundary, the D6
amendment scheduling, and the minor citation/wording corrections).

## Risk register

| Risk                                                           | Likelihood               | Impact | Mitigation                                                                  |
| -------------------------------------------------------------- | ------------------------ | ------ | --------------------------------------------------------------------------- |
| Vocabulary drift between docs and later code                   | High                     | High   | Vocabulary locked in Phase 0F; every Phase 1 contract must use it verbatim. |
| Model chosen by convenience, not by rejection                  | Medium                   | High   | Phase 0E requires explicit rejection of weak models with scenario evidence. |
| Planning engine quietly acquires side effects                  | Medium                   | High   | Purity gates + adversarial planner review in Phase 2H.                      |
| ADR-0001's single-file alias seam conflicts with kernel growth | Certain (it must change) | Medium | Phase 0A audit names the exact seam changes; ADR amendment in Phase 1 PR.   |
| Scenario matrix too shallow to be a design asset               | Medium                   | High   | Three independent proposers with different lenses + reconciliation.         |
